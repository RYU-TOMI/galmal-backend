# BACKEND.md — 백엔드 세션 작업 방식

> **소유: 백엔드 세션.** 다른 세션은 읽기만 한다.
> 공통 규칙은 `galmal-plan/SESSIONS.md`(이 저장소 `CLAUDE.md` 첫 줄이 가져온다), 저장소 규칙은 `CLAUDE.md`.
> 계약의 **정본**은 이 저장소 `contract/v1/` · `collector/dests.py`, **이유**는 `galmal-plan/CONTRACT.md`·`DECISIONS.md`,
> 전체 지도는 `galmal-plan/PROJECT.md`. **이 문서는 "백엔드가 어떻게 일하는가"만 다룬다.**
>
> ⚠️ **§8은 한 저장소 시절 기록이다**(경로 `site/`·「사용자를 통해 전달」 같은 절차). 저장소 분리 기록은
> [`MIGRATION.md`](MIGRATION.md)로 옮겼다. 낡은 서술과 지금 규칙이 부딪히면 **위 두 파일과 코드가 우선한다.**

## ▶ 지금 여기 — 새 세션은 이 절부터 (2026-09-21)

**돌아가는 것**: **하루 두 번 예약** — 23:10 KST(14:10 UTC) 주 실행 · 03:10 KST(18:10 UTC) 예비 — 크론 → 수집·판정
→ `docs/v1/` 발행 → `api.galmal.kr` → 프론트에 `repository_dispatch`. **예비는 오늘 발행이 이미 있으면 즉시 끝난다.**
실제 시작은 GitHub 큐 사정으로 2~3시간 늦는다(2026-09 중앙 121분) — **지연은 못 줄이고 여유로 견딘다.**
**노선 43 · v1 응답 47 · 딜 약 140 · 테스트 221건.** 계약 정본은 `contract/v1/` + `collector/dests.py`.

- 확인은 초록불이 아니라 **API `meta.generated` == 사이트 `build.json.api_generated`, 둘 다 당일.**
- 손으로 크론을 돌릴 땐 `skip_side_effects=true` — 안 그러면 메일함이 소비되고 구독자에게 실제 메일이 나간다.
- `.env`는 **이 폴더에만** 있다. 없이 재발행하면 제휴 링크가 조용히 빠진다(BB30).

### 최근 챕터 (기록은 §16)

| 챕터 | 내용 |
|---|---|
| BE9~BE11 | 테스트 시계 고정(BB34) · `generated` 필수(BB35) · 계약을 `contract/v1/`로(R8) · `/v1/vocab.json` 발행 |
| BE12 | 중간 점검(코드 전수 읽기) — 발견 BB36~BB41 · README 전면 개편. **코드는 안 고쳤다** |
| BE13 | 크론을 20:10 UTC로 + 자정 근접이면 잡 실패(BB36) · 구독취소 회신(BB37) · 죽은 코드(BB40) |
| BE14 | `date.today()` 0(BB25) · 고아 노선 파일 삭제 · 발행물 없으면 테스트 실패 · `concurrency`·`pydantic` |
| BE15 | 메일 방어 — 도메인 허용 목록 + Gmail `dmarc=pass` + 수집 상한 20통(BB39) · 파싱 실패를 잡 실패로 |
| BE8 1차 | 노선 36 → 43(새 출발지 3 · 김포 **실직항** 3 · ICN-PQC). 2차 후보 6개는 효과를 본 뒤 |
| BE16 | 이 문서 정리 — 백로그 상태 정정(열린 것 12 → 6) · 분리 기록을 `MIGRATION.md`로(2,153 → 1,256줄) |

### 지금 열린 것 — **무엇을 기다리나**

| 무엇 | 기다리는 것 |
|---|---|
| **BE15 저장 경로 실물 확인** | **항공사 메일이 오는 날의 크론 로그.** 09-21은 안 읽은 메일 0통이라 「받아들여 저장」을 못 밟았다. `저장: …` 줄이 그 확인이다(주 3~4통) |
| **BE6** 구독 파서 개편 + 안내 회신 | 기획의 `COPY.md` 회신 문구. 결정은 나 있다(§6 「BE6에 묶인 결정」) |
| **BB33** 메일을 읽자마자 소비 | 순서 — 위 BE15 확인 뒤에 같은 코드를 건드린다 |
| ⏸ **BE8 2차** — **보류**(2026-09-21 서치콘솔 실측). 노선이 아니라 **색인**이 병목이다 — 아래 「SEO 실측」 |
| **BB41 잔여** | `.env` 파서 5벌(언제든) · 중앙값 3벌(**기획이 정본을 먼저 정한다**) · `LICENSE`(**사용자 결정**) |
| BB3 · BB24 | 보존 정책은 아직 이르다 / MAU 5만은 트래픽이 조건이라 막혀 있다 |

**사용자 몫으로 부탁해 둔 것**: 서치콘솔 CSV · **받은편지함의 항공사 메일을 지우지 않기**(메일 원문의 유일한 보관처다) · (선택) Gmail 필터.

### 🔴 SEO 판정 (2026-09-21) — **BE8 2차 보류의 근거**

사용자가 서치콘솔 데이터를 줬다. **수치는 여기 적지 않는다**(공개 저장소 — `CLAUDE.md` §보안). 숫자는 노션에 두고,
여기엔 **무엇을 판단했나**만 남긴다. 다시 재려면 서치콘솔 → 실적 / 색인 생성 → 페이지.

**판정: 병목은 노선 수가 아니라 색인이다.**

- 색인되지 않은 페이지의 이유가 **「발견됨 - 현재 색인이 생성되지 않음」 하나뿐**이다(다른 이유 0).
  🔴 **「발견됨」은 「크롤링됨」이 아니다** — 구글이 URL은 알지만 **아직 가보지도 않았다.** 페이지 품질 판정을 받은 게 아니다.
  처음 세운 가설(「본문이 얇고 페이지끼리 비슷해서 거부됐다」)은 **틀렸다.** 이 줄을 남기는 건, 다음에 같은 화면을 보고
  또 「콘텐츠를 고치자」로 뛰지 않게 하려는 것이다. 두 상태는 **할 일이 정반대**다(기다린다 / 고친다).
- **기술 쪽은 전부 정상**(라이브 실측): `robots.txt` `Allow: /` · canonical 자기참조 · sitemap에 `lastmod`·`priority`·`changefreq` ·
  제목이 노선마다 다름 · **홈에서 노선 페이지로 가는 링크가 정적 `<a>`**(JS 생성이 아니라 크롤러가 바로 본다) · 노선끼리도 서로 링크.
  → **고칠 기술 항목이 없다.** 남은 변수는 **도메인 나이(2026-09-05 등록)와 외부 링크 0**뿐이다.
- **그래서 BE8 2차를 보류한다.** 대부분의 노선 페이지가 아직 크롤링 대기인데 **비슷한 페이지를 더 만드는 것은 역효과**다.
  재개 조건: 색인이 실제로 늘 때(「발견됨」이 줄 때), 또는 노출이 지금 몰려 있는 한 목적지 밖으로 번질 때.
- **건진 것**: 유입 질의 패턴이 `{출발지} {목적지} 항공권`이고 「비행기표」·「비행기값」·「평균 가격」 변형이 있다.
  지금 페이지 제목이 그 패턴과 맞는다. 제목·문구는 프론트·기획 구역이라 **건드리지 않았다.**
- 다시 볼 값(색인이 는 뒤): 모바일과 데스크톱의 평균 순위 차이 · 순위는 높은데 노출이 적은 노선.

### 최근 실측 (다시 재지 않아도 되게)

- **2026-09-21 크론**(run 35541522287, 시작 22:23:10Z · 9스텝 success): `routes/index` 43 · 새 7개 7/7 · v1 47 ·
  고아 파일 0 · 딜 138 == 제휴 링크 138 · API == 사이트 `2026-09-21T07:23:55+09:00` · `galmal.kr/routes/TAE-CJU.html` 200.
- **새 7노선 첫날**[`summary.n`·trend·months(n≥3)·weekdays]: PUS-BKK 53·1·6(5)·7 / PUS-HAN 13·1·4(3)·7 /
  TAE-CJU 20·1·3(3)·7 / GMP-SHA 64·1·6(4)·7 / GMP-TPE 54·1·5(5)·7 / GMP-NGO 19·1·2(2)·6 / ICN-PQC 73·1·3(2)·7
  → **추이만 1점이고 월·요일은 첫날부터 선다**(그 둘은 수집일 창이 없는 집계라서다).

## 왜 이 문서가 있나
백엔드는 `deals.json`의 **생산자**다. 여기서 두 가지가 따라온다.

1. **내 실수는 프론트에서 터진다.** 계약을 어기거나 데이터가 0건이 되면 화면이 죽는다.
   그래서 "돌아가더라"가 아니라 **계약과 실패 경로를 검증**해야 끝난 것이다.
2. **사용자가 백엔드 코드는 리뷰할 수 있다**(백엔드 전공). 프론트와 달리 보고를
   화면이 아니라 **코드·데이터 기준**으로 낸다 — 무엇을 왜 그렇게 짰는지, 숫자가 어떻게 바뀌었는지.

---

## 1. 작업 단위와 스코프 잠금

`CLAUDE.md`의 3계층(챕터 → 태스크 → 곁가지)을 그대로 따른다. 백엔드 고유 사항만 적는다.

- **챕터 ID는 `BE0`~**, 곁가지는 **`BB1`~** (프론트의 `CH*`/`B*`와 겹치지 않게).
- 챕터 시작 시 **건드릴 파일 목록을 못 박는다.** `collector/` 안이라도 목록 밖 파일은 안 고친다.
- 곁가지는 아래 §6에 한 줄로 적고 넘어간다. **"이왕 하는 김에" 금지.**

### 데이터 로직에만 있는 함정
- **파이프라인은 한 줄로 이어져 있다.** 수집 스키마를 건드리면 판정·`deals.json`·노선 페이지까지
  줄줄이 영향이 간다. 태스크를 쪼갤 때 **어디까지 파급되는지 먼저 적고** 시작한다.
- **`data/prices.db`는 되돌릴 수 없다.** 매일 크론이 커밋해 축적되는 실데이터다.
  스키마 변경은 `ALTER TABLE`/`IF NOT EXISTS`로 **기존 행을 살린 채** 한다. 테이블을 지우지 않는다.

---

## 2. 챕터 진행 5단계

1. **킥오프** — 목표·완료 조건·**범위 밖**을 명시하고 승인받는다.
2. **설계** — 데이터 모델·**계약 영향**·실패 모드·롤백을 먼저 정한다. **여기서 코드는 0줄.**
   계약이 바뀌면 기획이 `CONTRACT.md`에 결정을 쓰고 → 백엔드가 `contract/v1/`을 고친다(커밋에 결정 인용) →
   `SendMessage`로 기획·프론트에 직접 알린다. 사용자를 전달책으로 쓰지 않는다.
3. **태스크 쪼개기** — 태스크 1개 = 커밋 1개. 목록과 건드릴 파일 승인 → 스코프 잠금.
4. **구현·검증** — 태스크마다 §3 DoD 전 항목.
5. **핸드오프** — main 병합 → §6 백로그 갱신 → §7 양식으로 종료 보고.

---

## 3. 태스크 DoD (전 항목, 하나라도 비면 미완료)

- [ ] **정상 경로** 실행 확인 — 실행 명령과 출력을 보고에 붙인다
- [ ] **실패·빈 경로** 처리 — 수집 0건 / API 오류 / 결측 필드 / 과거 날짜에서 죽지 않는가
- [ ] **계약 영향** 판단 — `deals.json` 스키마가 바뀌었나? 바뀌었으면 `CONTRACT.md` 먼저
- [ ] **검증 수단** — 테스트를 추가했거나, 없다면 재현 명령을 명시
- [ ] **문서** — 주석(한국어)·이 문서 갱신·결정이면 `DECISIONS.md` 초안
- [ ] **커밋** — 한국어로 무엇을 왜. `Co-Authored-By: Claude` 포함

> 2번과 5번이 평소 빠지는 항목이다. 체크리스트로 강제한다.

---

## 4. 확인 방법

```bash
python collector/publish.py                         # v1 전체 재발행 → docs/v1/ (유일 진입점, .env 필요)
python collector/discover_data.py                   # 딜 건수만 찍는다 — 파일은 안 쓴다
python -m http.server 8000 --directory docs         # http://localhost:8000/v1/meta.json
```
> 2026-09-15(M3 T3)까지는 `build_site.py`가 진입점이었다. 지금은 없다 — 화면은 프론트 저장소가 만든다.

### 산출물을 커밋할 때 주의
`python collector/publish.py`를 돌리면 `docs/v1/*`(meta·deals·vocab·routes/index·routes/{code})가
그날 DB 기준으로 **전부 다시 써진다.** 이건 백엔드 산출물이지만 **프론트가 URL로 받아 굽는 입력이기도 하다.**
(화면 파일 `index.html`·`routes/*.html`·`sitemap.xml`은 이 저장소에 없다 — 프론트 저장소 몫이다.)

⚠️ **딜 목록은 시각에 따라 달라진다** — `build_deals_json()`이 `now_kst()` 기준으로 신선도를
자르므로 오후에 재빌드하면 아침 크론과 다른 건수가 나온다(2026-09-11 실측 138 → 129).
**검증 목적의 재빌드는 커밋하지 않는다.**

- 코드 변경 검증 목적으로만 돌렸다면 → `git checkout -- docs/v1` 로 되돌린다.
- 산출물을 의도적으로 갱신했다면 → **딜 건수 변화를 보고에 적는다**(예: 103건 → 94건).
- 크론이 매일 같은 일을 하므로, 산출물만 바뀐 커밋은 굳이 만들지 않는다.

### 데이터 사실 확인
숫자를 추측하지 않는다. `data/prices.db`를 직접 조회해 근거를 만든다.
```bash
python -c "import sqlite3;c=sqlite3.connect('data/prices.db');print(c.execute('SELECT MIN(fetched_date),MAX(fetched_date),COUNT(*) FROM broad_offers').fetchone())"
```

---

## 5. 코드 규칙

- **스택 고정**: Python 표준 라이브러리 우선. 외부 의존성은 `anthropic`(메일 파싱) 하나뿐이며,
  **새 의존성 추가는 사용자 승인 사항**이다. Node/npm 금지. — `CLAUDE.md`
- **발행 진입점은 `publish.py` 하나.** 새 응답도 여기에 붙인다(`generated`를 반드시 넘긴다 — BB35).
  크론은 수집 뒤 이것만 호출한다. 백엔드는 HTML을 만들지 않는다.
- **기존 스타일을 따른다**: 모듈 docstring(한국어)·표준 라이브러리·`sys.path.insert`로 형제 모듈 import.
- **시간대 함정**: Actions 러너는 UTC라 `date.today()`가 KST와 하루 어긋난다.
  판정 기준일은 `detect_deals.py`처럼 **DB의 `MAX(fetched_date)`** 를 쓴다.
- **계약 필드는 항상 채운다**: `null`이 허용된 필드(`ret`)를 빼면 키를 생략하지 않는다.
  0건인 날도 `{"schema":..,"generated":..,"origins":{},"deals":[]}` 형태는 유지한다. — `CONTRACT.md`
  (`updated`는 v1에서 `generated`(ISO 8601 + 오프셋)가 됐다.)
- **가짜 데이터 금지**: 없는 데이터를 그럴듯한 값으로 채우지 않는다. 신뢰가 유일한 자산이다.
  — `DECISIONS.md`(가짜 가격 그래프 제거, 2026-08-06)
- **PII·시크릿 금지**: 구독자 이메일은 해시만, 메일 본문은 `emails_raw.db`(로컬 전용), `.env` 커밋 금지.
- **크롤링 금지**: 타 비교사이트 DB 수집 금지. 공식 API·항공사 공지·제휴만. — `CLAUDE.md`

---

## 6. 챕터 로드맵

| # | 챕터 | 범위 | 상태 |
|---|---|---|---|
| BE0 | 검증 기반 | 순수 함수 유닛테스트 · `deals.json` 계약 검증기 · CI test job | ✅ 완료 (테스트 73건) |
| BE1 | 파이프라인 신뢰성 | 수집 실패 감지 · 산출물 보호 · 날짜 라벨 · 크론 알림 | ✅ 완료 (테스트 107건) |
| BE2 | 데이터 품질·신선도 | `low`/`obs_days` · 검증기 자동화(BB19) · `median` 재정의(BB4·BB12) · `when` 라벨(BB7·BB8) · 예약 링크(BB16) | ✅ 완료 (테스트 120건) |
| BE3 | 발견 데이터 확장 | 목적지 사전·좌표 검증 · 프론트 요청 필드 대응 | 대기 |
| BE4 | SEO·페이지 생성 | 노선 페이지 개선 · 구조화 데이터 · 목적지 페이지 | 대기 |
| BE5 | 수익·제휴 | Trip.com 재신청 대응 · Flight Search API 신청(MAU 5만 조건, BB24) · 링크 정확도 검증 | 대기 |
| BE6 | 알림 v2 | 구독·발송 재개(현재 IA에서 v2로 미뤄짐) — 🔴 **열기 전에 아래 「BE6에 묶인 결정」을 읽을 것** | 대기 |
| BE7 | 배포·운영 | 커스텀 도메인(`galmal.kr`) · 서치콘솔 · 운영 지표 | ✅ 완료 (테스트 175건) |

- 🔴 **BE6에 묶인 결정** (기획 `DECISIONS.md` 2026-09-20 (8) · `CONTRACT.md` §subscribe 주석 — **지금 바꾸지 않는다, BE6에서 한 묶음으로**):
  1. **구독 노선은 정규식이 아니라 아는 코드(`config.ROUTES`)를 본문에서 글자 그대로 찾는다.** `ROUTE_RE`를 없앤다. 계약 필드 변경 없음 —
     프론트 검사가 「본문에 `route_token` 자리의 코드가 있다」만 보면 되므로 프론트 테스트의 `ROUTE_RE` **사본이 사라진다**(프론트 B63).
     없는 노선(`ICN-XXX`)으로 「영원히 안 울리는 구독」이 생기던 것도 같이 사라진다. 근거 실측(2026-09-20): 구독·해지 메일 통틀어 2통·발신자 1명,
     **변형 표기(`→`·`~`·공백)가 들어온 사례 0** — 2026-07-16의 관대함은 짐작이었다.
  2. **`route or "ALL"`을 끊는다.** `ALL`은 명시적 `전체` 키워드일 때만. 코드도 키워드도 없으면 **구독을 만들지 않는다**(조용한 과잉 → 눈에 띄는 실패).
  3. **반쪽으로 고치지 않는다** — 실패가 운영 로그에만 남으면 보낸 사람에겐 여전히 조용한 실패다. **안내 회신과 한 묶음**으로 구현한다.
     회신 문구는 새 문자열이라 **기획이 `COPY.md`를 먼저 채운다 → BE6 착수 전에 기획에 알릴 것.**
  ⏸ **2026-09-21 사용자 보류: 「구독 관련 기능은 최대한 미룬다 — 결정할 부분이 남았다」.** 기획이 `COPY.md` 문구까지 채웠지만
     **착수하지 않는다.** 아래 측정은 표본이 있을 때 미리 재 둔 것이다(다시 재려면 사용자가 메일을 또 보내야 한다).
  📏 **인증 분포 실측(2026-09-21, 읽기 전용 PEEK · 집계만)** — 기획 질문 「국내 메일이 `dmarc=pass` 로 찍히나」의 답:
     - **네이버 발신 구독 메일: `spf=pass` · `dkim=pass` · `dmarc=pass` · `header.from` 일치 → `mail_guard.authenticated()` 통과.**
       사용자가 실제로 보내 준 한 통이다. **「정상 국내 사용자를 막는 문지기」 우려는 네이버에 대해선 해소.**
     - 받은편지함 전체: 항공사 10개 도메인 37통 전부 `dmarc=pass`(이스타항공만 DKIM 없이 SPF 정렬로 통과) · google 계열 4통 pass.
     - 🔴 **남은 위험 하나: `gmail.com` 3통은 `Authentication-Results` 헤더가 아예 없다**(spf·dkim·dmarc 전부 「없음」).
       같은 Gmail 계정끼리 주고받으면 외부 SMTP 를 안 거쳐 헤더가 안 붙는 것으로 보인다. 구독 테스트 2통이 여기 해당한다.
       → 인증을 구독에 그대로 걸면 **그 메일들은 구독도 회신도 안 된다.** 기준을 정할 때 이 경우를 어떻게 할지가 핵심이다.
       백엔드 제안(**열 때 다시 볼 것, 확정 아님**): 「통과면 구독+회신 / 없거나 실패면 구독은 만들되 **회신은 안 보낸다**」.
       🔴 **이 안에는 구멍이 있다**(기획 지적 2026-09-21): 헤더 없는 **정상** 사용자가 「처리하지 못했습니다」 회신도 못 받는다 —
       조용한 실패가 그 갈래에만 남는다. 회신을 없애는 것이 곧 안전은 아니다. 열 때 이 구멍부터 메울 것.
     - DNS 조회: naver·daum·hanmail·kakao·nate·gmail·outlook 은 DMARC 레코드 **있음**(전부 `p=none`) · icloud `quarantine` ·
       yahoo.co.kr `reject` · **korea.com 은 레코드 자체가 없다**(그런 도메인은 헤더에 `dmarc=` 가 안 실릴 수 있다).
  4. 같은 챕터에서 볼 것: 구독 스캔이 INBOX 전체를 돈다 · From 위조로 남의 주소를 구독시킬 수 있다(double opt-in 없음) ·
     회신 제목을 안 고치면(`Re: (광고) …`) 해지로 인식조차 안 된다(BB37 잔여) · `ROUTE_RE`를 고칠 땐 **프론트에 먼저 알린다**(코드 주석).
- 순서 근거: **안전망(BE0) → 안 죽게(BE1) → 데이터를 믿게(BE2) → 그 위에 기능.**
  BE1·BE2가 전부 기존 데이터 로직을 건드리므로 회귀를 잡을 수단이 먼저 있어야 한다.
- **순서 변경 1건(2026-08-22)**: `seen` 필드 생산을 BE2에서 BE0 T3으로 앞당겼다.
  계약(`CONTRACT.md` 2026-08-22)이 이미 `seen`을 선언했는데 생산자가 안 내보내고 있어,
  계약 검증기(T4)가 첫 실행부터 실패 상태로 태어나는 걸 피하기 위해서다.
  BE2에는 나머지(BB4·BB7~BB11)가 남아 있다.
- 챕터를 건너뛰지 않는다. 순서를 바꾸려면 이유를 여기 남긴다.
- **순서 변경 2건(2026-09-05)**: BE5(수익·제휴)·BE6(알림 v2)를 건너뛰고 **BE7을 먼저** 연다.
  BE5의 두 항목이 **둘 다 트래픽이 선행 조건**이기 때문이다 — Trip.com은 3개월 트래픽,
  Flight Search API는 MAU 5만(BB24). **그 트래픽을 만드는 게 BE7(SEO)이다.** 순서를
  뒤집는 게 아니라 의존 방향대로 가는 것이다. BE6은 IA에서 v2로 미뤄져 있다.
  SEO는 효과가 나기까지 2~6개월 걸리는 유일한 채널이라 가장 일찍 시작해야 한다.
- ⏸ **보류(2026-09-09)**: 사용자가 **이전 자체를 미뤘다**. M1까지 끝난 상태로 멈춰 있다.
  현 상태·재개 절차·보류 중 지킬 것은 **`MIGRATION.md` §12**. 재개 전에 `MIGRATION.md` §12부터 읽을 것.
- 🔴 **중단(2026-09-08)**: 사용자가 저장소 분리를 결정해 BE8 이후가 멈췄다.
  멈춘 자리·재개 전제·이전과 부딪히는 곁가지는 **`MIGRATION.md` §11**에 있다. 재개 전에 `MIGRATION.md` §11부터 읽을 것.

---

## 7. 곁가지 백로그 (BB)

> 작업 중 발견했지만 **스코프가 아니라서 안 고친 것.** 해결되면 지우지 말고 `~~취소선~~ → 해결(커밋)`.
>
> 🔎 **지금 열린 것은 여섯뿐이다**(2026-09-21 BE16 T1에서 코드로 재확인 — 나머지 33개는 해결·이관):
> **BB3** DB 보존 정책 · **BB22** 노선 커버리지(29%, 유지 하한은 기획 `BE8-1`) · **BB24** MAU 5만(막힘) ·
> **BB33** 메일을 읽자마자 소비 · **BB38** 메일 산출 소비자 없음(기획 결정: 쌓기만) · **BB41** 소소한 것 잔여.
> 아래 본문은 **왜 그랬나**의 기록이라 길다 — 열린 것만 보려면 이 줄이면 된다.

### BE1 — 파이프라인 신뢰성
- ~~**BB18. `continue-on-error` 스텝의 실패를 알 방법이 없다.**~~ → **해결(2026-09-01, BE1 T5)**:
  `collect.yml` 마지막에 **상태 점검** 스텝을 뒀다. 커밋 *뒤*에 두어 데이터는 먼저 남기고,
  세 스텝 중 하나라도 실패했거나 `deals.json`이 보존됐으면 **잡을 실패로 표시**한다
  → GitHub가 저장소 소유자에게 메일을 보낸다. 요약은 `$GITHUB_STEP_SUMMARY`에 남는다.
  보존 신호는 `build_site.py`가 `GITHUB_OUTPUT`으로 넘기며, 로컬 실행에는 영향이 없다.
  (원문)  ⚠️ **T4 이후 중요도가 올랐다** —
  하한선 방어(BB1)가 들어가면서 수집 실패가 **화면상 조용해졌다**(어제 데이터가 오늘 얼굴로
  나온다). 기획이 화면 쪽은 갱신 시각 상시 노출로 받기로 했지만(C-15), **운영자가 알 방법**은
  여전히 없다. 사이트가 안 비는 대신 아무도 모르는 상태가 오래갈 수 있다. `collect.yml`의 메일 수집·
  LLM 파싱·알림 발송 3개 스텝은 실패해도 워크플로가 계속된다. 가격 수집을 막지 않으려는
  의도이므로 그 자체는 옳지만, **몇 주째 실패 중이어도 아무도 모른다.** 잡은 초록불이다.
  → 스텝 결과를 모아 마지막에 요약하거나(실패했으면 잡을 실패로 표시), 실패 시 이슈를
  자동 생성하는 방법. BE1 안에서 다룬다.
- ~~**BB16. 예약 링크가 도시 코드를 받는지 미확인.**~~ → **해결(2026-09-01, BE2 T5)**:
  브라우저로 예약처 4곳을 실제로 열어 확인했다. **전부 도시 코드를 받는다** —
  스카이스캐너 "도쿄 (모두)" · 트립닷컴 "모든 공항"(NRT·HND 동시 노출) · 구글 "도쿄도" ·
  네이버 "모든 공항". `dests.link_code()`로 목적지만 도시 코드로 넓혔다(23건 영향).
  **출발지는 그대로 실제 공항**이다 — 우리가 지정해 물은 값이라 알고 있고,
  실측상 인천·김포 응답의 가격 일치가 3%뿐이라 서로 다른 데이터다.
  `deals.json`의 `d`는 `NRT` 그대로 둔다(URL 딥링크·중복 제거 키라 계약 영향이 크다).
  (원문)  BB15로 도쿄·오사카·뉴욕이 들어왔는데
  이 셋은 공항이 둘이라 대표 공항(NRT·KIX·JFK)으로 링크를 건다. 광역 수집은 도시 단위라
  **그 딜이 실제로 그 공항인지 알 수 없다.** 하네다가 최저가였다면 나리타 검색에서 그 값이
  안 나온다. 예약처가 도시 코드(`TYO`)를 받아주면 그대로 넘기는 게 낫다(기획 지침).
  - 확인 방법: 브라우저로 4곳 실제 렌더링 확인. HTTP로는 판정 불가(캡차·JS).
  - 영향 범위는 도쿄·오사카·뉴욕 3곳뿐이다(나머지 8개 도시 코드는 우리 사전에 공항이 하나).
- ~~**BB15. ⚠️ 광역 수집이 도쿄·오사카·파리·런던·뉴욕을 통째로 버리고 있다.**~~
  → **해결(2026-08-28, BE1 T1)**. 수집 쪽은 끝났고 예약 링크 최적화만 남았다(아래 ▶).
  (2026-08-22 발견)
  - **원인**: `v2/prices/latest`는 **IATA 도시 코드**(`TYO` `OSA` `NYC` `PAR` `LON` `BJS` `SPK`)를
    주는데 `dests.DEST`는 **공항 코드**(`NRT` `HND` `KIX` `JFK` `CDG` `LHR` `PEK` `CTS`)로 돼 있다.
    `fetch_breadth.py:65`의 `is_destination()`이 못 알아보고 **조용히 `continue`한다.**
  - **실측(ICN 1회 호출)**: 응답 300개 중 **61개만 통과, 239개 폐기**. 사전 84곳 중
    **23곳은 광역 응답에 영원히 안 잡힌다** — 도쿄·오사카·뉴욕·파리·런던·로마·베이징·
    삿포로·상하이(PVG)·자카르타(CGK)·토론토·씨엠립·프놈펜·양곤·비엔티안 등.
  - **실제로 버려지는 값**: `TYO 123,851원` · `OSA 136,051원` · `SPK 275,110원` ·
    `BJS 267,952원` · `PAR 768,179원`. 전부 신선한 데이터인데 버린다.
  - **왜 아무도 몰랐나**: 도시 코드와 공항 코드가 같은 곳(`BKK` `FUK` `DAD` `CJU` `SHA` `JKT`)은
    통과해서 피드가 그럴듯해 보인다. 마쓰야마·가고시마는 있는데 도쿄가 없는 이유다.
  - 발견 서비스의 핵심 상품인 **일본 1·2위 도시가 지도에 없다.** 우선순위가 높다.
  - 고치는 방향(미확정): `dests.DEST`에 도시 코드 별칭을 추가하거나(`TYO`→도쿄),
    공항↔도시 코드 매핑표를 두고 정규화. **예약 링크는 실제 공항 코드가 필요**하므로
    (`affiliates.py`) 별칭만 넣으면 링크가 깨진다 — 매핑이 양방향이어야 한다.
  - **기획 지침(2026-08-22)**: 예약처가 도시 코드를 받아주면 **그대로 넘기는 쪽을 우선** 검토할 것.
    제품 원칙이 "목적지를 정해주는" 서비스이지 공항을 정해주는 게 아니고, 나리타냐 하네다냐는
    **예약처에서 고를 문제**라 선택지가 넓은 편이 낫다. 받아주지 않는 예약처만 **대표 공항
    하나로 고정**하며, 기준은 **그 도시의 주 국제공항**(도쿄=`NRT`, 오사카=`KIX`,
    뉴욕=`JFK`, 파리=`CDG`). → **예약처 4곳이 도시 코드를 받는지 조사한 뒤 기획에 확정 요청.**
  - 기획 확인: BB15가 고쳐지면 지도 투영이 달라지는데(뉴욕 유입으로 대서양 빈 구간이 쪼개짐),
    스펙을 **딜 좌표에서 매번 계산**하도록 고쳐뒀다(`ecba9cc`). **프론트 조율 불필요.**
  - 기획 확인: 태그 배정은 다시 할 필요 없다. BB15가 고쳐지면 그대로 살아난다.
  - **해결 방식**: `dests.CITY_TO_AIRPORT`(11개) + `canonical()`로 수집 시점에 정규화.
    값은 벤더 공식 참조(`api.travelpayouts.com/data/en/airports.json`의 `city_code`)에서
    뽑았고 전수 대조했다. 저장은 대표 공항 코드로 하므로 **계약·좌표·링크는 그대로**다.
  - **실측 효과**: 통과 169건 → 204건(**+21%**), 딜 102 → 116건. 도쿄·오사카가 전 출발지에서
    복귀했고 기획이 배정한 태그(도쿄 `야경·골목·쇼핑`)가 그제야 화면에 나온다.
  - ▶ **남은 것: 예약 링크에 도시 코드를 넘길 수 있는가.** 기획은 도시 코드를 우선 검토하라고
    했으나(공항은 예약처에서 고를 문제), **예약처 4곳의 수용 여부를 확인하지 못했다** —
    HTTP로는 스카이스캐너가 캡차로 막고 나머지는 JS라 판정 불가, 브라우저 확장은 미연결.
    현재는 안전한 기본값인 **대표 공항**으로 링크를 건다(도쿄→NRT). 동작하지만,
    실제 최저가가 하네다 편이면 나리타 검색 결과와 어긋날 수 있다. → **BB16**

- ~~**BB14. `(허브, 목적지)` 유일성이 계약 검증기에 없다.**~~ → **해결(2026-09-01, BE1 T4)**:
  검증기가 `(o, d)` 중복을 잡는다. 실제로 중복을 주입해 걸리는 것을 확인했다.
  (원문)  기획이 URL 딥링크(`#SEL-DAD`)를
  이 불변식 위에 설계했는데(2026-08-22), 검증기는 "딜 없는 허브가 `origins`에 남았는가"만
  보고 **`(o, d)` 조합 중복은 안 본다.** 중복 제거 로직을 건드리다 깨뜨리면 URL이 조용히
  엉뚱한 딜을 가리킨다. BE1 T2에서 `discover_data.py`를 만질 때 검사 항목으로 추가한다.
  - 키는 **`(o, d)`(IATA)**다. 내가 처음에 `(o, ko)`라고 적었으나 기획이 정정했다 —
    URL이 `#SEL-DAD` 형식이라 IATA를 쓴다.
  - `(o, ko)` 중복은 **구조적으로 불가능**하다(`build_deals_json`의 중복 제거 키가 `(허브, ko)`).
    실측으로도 23일치에서 복수 공항 도시의 동시 등장 0건, IATA가 날짜에 따라 바뀐 사례 0건.
    다만 **BB15 때문에** 도쿄·오사카·뉴욕은 애초에 피드에 등장한 적이 없다.
- ~~**BB1. 수집이 4일 연속 실패하면 사이트가 빈다**~~ → **해결(2026-09-01, BE1 T4)**:
  `build_deals_json`이 하한선 미달이면 **파일을 쓰지 않고 이전 산출물을 보존**한다.
  기준은 실측에서 뽑았다(30일 재현: 최소 99·중앙 110·최대 126, 전날 대비 최대 하락 -12.5%)
  → `MIN_DEALS = 30` 또는 **이전의 50% 미만**이면 사고로 본다. 이전 파일 자체가 하한선
  미만이면 검사를 걸지 않아 잠금(한 번 망가진 파일이 영원히 보존되는 것)을 피한다.
  실물 확인: 딜 0건일 때 126건짜리 파일과 `updated`가 그대로 남았다.
  (원문)  (2026-08-22 실측으로 조건 정밀화).
  `build_deals_json()`은 최근 3일 `broad_offers`가 비면 `deals: []`를 그대로 쓰고
  크론이 그걸 커밋·배포한다. 하한선도 이전 산출물 유지 장치도 없다.
  - **하루 실패로는 안 빈다.** 3일 창 덕에 이전 날짜 행이 남는다. 실측: 1~3일 연속
    실패 시 창 안에 목적지 52~56곳이 남고, **4일째에 0곳**이 된다.
  - **크론이 조용히 성공한다는 게 문제다.** `fetch_breadth.py:58-62`가 출발지별로
    예외를 삼키고 `continue`한다. 5개 공항이 전부 실패해도 스텝은 exit 0이라
    워크플로가 계속 진행돼 빈 산출물을 커밋한다. (스텝이 실패하면 오히려 안전하다 —
    잡이 멈춰 커밋 자체가 안 되고 사이트는 어제 상태로 남는다.)
  - 현실적 유발 경로: `TP_TOKEN` 만료·회수, API 응답 형식 변경, 4일 이상 장애.
- ~~**BB2. 크론 실패를 아무도 모른다.**~~ → **해결(2026-09-01, BE1 T3)**:
  `fetch_breadth`·`fetch_prices` 둘 다 전부 실패하거나 0건이면 **종료 코드 1**을 낸다.
  일부 실패는 그대로 넘어간다(나머지 데이터는 쓸모가 있다). 실측으로 실패 1 / 정상 0 확인.
  BB2는 `fetch_breadth`만 적어뒀지만 `fetch_prices`가 **글자 그대로 같은 결함**이라
  함께 고쳤다 — 한쪽만 고치면 파이프라인이 반만 보호된다.
  남은 것: 워크플로의 `continue-on-error` 스텝(메일·파싱·발송)은 여전히 조용히 넘어간다.
  그건 의도된 것이지만 **실패했다는 사실 자체를 알 방법이 없다** → BB18.
  (원문)  `collect.yml`에 실패 알림이 없다. 4~6단계는
  `continue-on-error`라 조용히 건너뛴 채 성공으로 보인다.
- **BB3. `data/prices.db`가 매일 커밋된다 — 보존 정책이 없다.**
  **2026-09-21 실측: 작업 파일 46MB · `offers` 72,270행 · 저장소 팩 29.6MiB.** 09-08(34MB·56,363행·38.98MiB) 대비
  파일은 +12MB인데 **팩은 -9MiB**다 — 그 사이 git이 다시 묶은 것으로 보이나 **확인하지 않았다.** 지켜볼 건 파일 크기가 아니라 팩이다.
  (원문) `offers` 56,363행이 무기한 축적.
  **2026-09-08 재측정**(원문은 23MB·36,581행 — 두 달 전 값이라 갱신한다):

  ```
  증가 속도   0.7 MB/일   (9/1 29MB → 9/8 34MB, 커밋 이력 실측)
  저장소 팩   38.98 MiB   (working file 34MB + 59일치 이력 ≈ 5MB)
  ```

  🔴 **이력이 5MB밖에 안 되는 게 핵심이다** — SQLite가 페이지를 뒤에 덧붙이는 동안은
  git이 델타를 아주 잘 잡는다. 그래서 「매일 30MB씩 쌓인다」가 아니다.
  **단 `VACUUM`이나 페이지 재배치가 한 번 일어나면 델타 사슬이 끊겨** 그날 하루가
  통째로 새 blob이 된다. 지켜볼 건 파일 크기가 아니라 **팩의 급증**이다.
  현 추세로 GitHub 권고치(1GB)까지 1~2년 여유.
  보존 정책(예: 90일)이 없어 저장소가 계속 무거워진다.

### BE2 — 데이터 품질·신선도
- ~~**BB4. `median` 산출에 기간 창이 없다.**~~ → **해결(2026-09-01, BE2 T3)**:
  30일 창 + 알갱이를 `(허브, 도시)`로 통일 + 이전 기간 기준으로 재정의.
  `median`·`low`·`obs_days`가 이제 **같은 것을 같은 기간에 대해** 잰다.
  (원문)  ⚠️ **기획 결정이 여기에 걸려 있다(2026-09-01)**:
  도장 티어 임계값 `15/25/35`를 유지하되, **"재는 자를 고치기 전에 눈금을 옮기지 않는다"**는
  이유로 BB4 수정을 기다리는 상태다(`DECISIONS.md`). 이력이 길어질수록 중앙값이 옛 가격에
  끌려가 할인율이 과대평가되면 지금 맞춘 눈금을 다시 맞춰야 한다.
  → ✅ **완료(2026-09-01)**: BB4 정리 후 30일 분포를 재측정해 기획에 전달했다.
  **트리거 발화** — T3가 피드의 4.8%(성숙 구간 4.1%)로 기준 2%를 두 배 넘고, 30일 중
  28일에서 2%를 초과했다. 다만 **뒤집힘은 10% → 0%로 오히려 개선**됐다(성숙 구간 0/15일).
  BB4가 고친 것은 위계이고 드러낸 것은 희소성이다 — 예전엔 median이 눌려 T3가 인위적으로
  드물어 보였다. 임계값 조정 여부는 기획 판단이며, 한 달 뒤 재측정하면 데이터 한계 셋
  (성숙 구간 15일뿐 · BB15 이전 대형 도시 부재 · 이력 33일이라 30일 창 효과 미발현)이
  해소된 분포가 나온다.
  - 재현 방법: 프로덕션 함수 `_prior_history`를 과거 각 수집일 인자로 호출한다.
    분석용으로 다시 구현하면 실제 코드와 갈라진다.
  (원래 요청) 
  재측정 스크립트는 과거 각 수집일의 `build_deals_json` 결과를 재현하는 방식이다
  (3일 창·미래 출발·중복 제거·그날까지의 중앙값). 트리거: 기획이 `15/28/42`를 채택했고(2026-09-01),
  **한 달 뒤 재측정**해 T3가 2.5%를 넘으면 다시 본다.
  기획 요청: **BB4 이후 한 번이면 충분하며 그전에는 더 뽑지 말 것.**
  (원래 내용)  `discover_data.py:86`이 `broad_offers` 전체 이력을
  쓴다. 지금은 24일치라 무해하지만, 몇 달 뒤 "평소 시세"가 옛날 값에 끌려간다.
  `detect_deals`는 `BASELINE_DAYS=30` 창을 쓰는 것과 대비된다.
- ~~**BB5. `found_at`이 저장돼 있는데 계약에 안 나간다.**~~ → **해결(2026-08-22, BE0 T3)**:
  `seen` 필드로 나간다. 표시가 빠져 있어 2026-09-01에 정정했다.
  (원문)  `broad_offers.found_at`은 있으나
  `build_deals_json()`의 SELECT에 없다. → **프론트 요청 B8과 같은 항목**(§8 참조).

- ~~**BB7. `_when_label`이 이번 달 출발을 이번 달 이름으로 표시한다.**~~
  → **해결(2026-09-01, BE2 T4)**: `이번 달` 신설. 실측 44건(35%)이었다.
  (원문)  8월에 8월 말 평일
  출발이면 `when`이 `"8월"`이 된다(주말도 이번 주도 다음 달도 아니라 월 이름으로 떨어짐).
  오늘이 8월인 사용자에게 "8월"은 정보가 없다. 2026-08-21자 `deals.json`에 3건
  (푸꾸옥 8/31, 로스앤젤레스 8/31, 오클랜드 8/30). 오늘이 월초면 그 달 22일치가 해당된다.
- ~~**BB8. `when` 라벨에 연도 구분이 없다.**~~ → **해결(2026-09-01, BE2 T4)**:
  `내년 N월` 신설 + 2년 이후 `YYYY년 N월` 폴백. 실측 13건(10%)이었다.
  (원문)  `f"{dep.month}월"`이라 이듬해 출발도 `"1월"`·`"3월"`로
  나간다. 2026-08-21자 `deals.json`의 2027년 출발 12건이 전부 이렇다(하노이 2027-01-01 →
  `"1월"`, 멜버른 2027-03-01 → `"3월"`). 사용자가 지나간 달로 읽을 수 있다.
- ~~**BB9. `dests.py` docstring의 통제 어휘와 실제 태그가 다르다.**~~ → **해결(2026-08-22)**:
  어휘 24종 확장 때 docstring을 함께 고쳤다. 표시가 빠져 있어 2026-09-01에 정정했다.
  (원문)  docstring은 9종
  (`해변 도시 자연 온천 쇼핑 미식 액티비티 휴양 문화`)을 선언하는데 실제로는 12종이 쓰인다.
  어휘 밖 3종은 `야시장`(TPE)·`유적`(REP/RGN/DEL/IST/FCO)·`트레킹`(KTM).
  `CONTRACT.md`가 태그를 "필터·사진 색 선택에 사용"이라 명시하므로 프론트에 영향이 갈 수 있다.
  → ~~**결정됨(2026-08-22, 기획)**: 실제 12종을 통제 어휘로 확정~~ → **해결**:
  기획이 어휘를 다시 24종으로 확장(`feb7c56`)하면서 `dests.py`를 `TAGS.md` 배정안으로
  전면 교체했고, docstring도 24종·상위/하위 구조로 다시 썼다. 12종을 거치지 않고
  한 번에 갔다(기획 권고).

- ~~**BB10. 광역 수집 창이 의도(3일)와 다르게 실제 4일이다.**~~ → **해결(2026-09-01, BE1 T2)**:
  `MAX_AGE_HOURS = 96`으로 이름과 동작을 일치시켰다. 96h 유지는 의도된 선택이며
  정책 변경이 아니다(72h로 조이면 딜 26%·소도시 롱테일 손실). 원문: `fetch_breadth.py:69-72`가
  `age = (now - found).days`로 나이를 재는데 `timedelta.days`는 **내림**이라
  `age > 3`은 95시간까지 통과시킨다. 실측으로도 수집분 최대 나이가 매일 91~95h이고
  96h를 넘는 행은 10일치에서 0건이다. `MAX_AGE_DAYS = 3`이라는 이름과 실제 동작(96h)이 어긋난다.
  `CONTRACT.md`가 "최근 3일 수집분"이라 적은 것도 엄밀히는 4일이다.
- ~~**BB11. ⚠️ `fetch_breadth`가 시간대가 다른 두 값을 빼서 매일 딜을 버린다.**~~
  → **해결(2026-09-01, BE1 T2)**: `timeutil`로 단일화. 재수집에서 8건이 살아났다. 원문:
  (2026-08-28 실측으로 심각도 상향)
  `now = datetime.now()`는 naive 로컬 시각인데 `found_at`은 UTC다. **9시간이 부풀려져**
  신선도 컷을 통과해야 할 딜이 잘린다.
  - **실측(2026-08-28, 5개 공항)**: 타임존 오차 **때문에만** 버려지는 딜이 **하루 11건**.
    ICN 도쿄 `148,796원` · 오키나와 `188,271원` · 샌프란시스코, GMP 베이징 `273,845원` ·
    홍콩 · 싼야 · 이스탄불 · 비엔티안 · 시애틀, PUS 도쿄 `244,768원` · 싼야.
  - **화면에 나가는 값이 틀어진다**: 지금 도쿄가 GMP `287,179원`으로 뜨는데,
    ICN `148,796원`이 이 버그로 잘렸다. **절반 값이 버려지고 비싼 쪽이 노출된 것이다.**
  - ✅ **크론은 정상이다.** 러너의 `datetime.now()`가 UTC 벽시계이고 `found_at`도 UTC라
    뺄셈이 우연히 맞는다. **틀리는 건 로컬 실행뿐**이고, 위 11건도 로컬 기준 손실이다.
    운영 데이터는 영향이 없다 — 심각도는 처음 생각보다 낮다.
  - 다만 **2026-08-28 커밋된 데이터는 내가 로컬로 수집한 것이라 11건이 빠져 있다.**
    다음 크론이 정상 수집한다. 그리고 "같은 명령이 환경에 따라 다른 데이터를 남긴다"는
    문제 자체는 남는다 — 로컬에서 재현·디버깅할 때 운영과 다른 결과를 본다.
  - BB10과 **같은 두 줄**이라 함께 고치는 게 자연스럽다.

> **`found_at`이 UTC라는 근거**(2026-08-22 백엔드 실측, `CONTRACT.md`의 서술을 정정):
> 기획은 "3일 창(72h)을 넘는 80h가 관측되므로 9h가 어긋난 것"이라 적었으나, 실제 창은
> BB10 때문에 **96h**다. 더 확실한 근거는 **최소 나이**다 — UTC로 해석하면 매일 가장 신선한
> 가격의 나이가 0h 부근(때로 -0.2h, 크론이 22:10 UTC보다 몇 분 늦게 도는 만큼)으로,
> 캐시가 방금 관측한 가격을 돌려준다는 자연스러운 그림이 된다. KST로 해석하면 가장 신선한
> 가격조차 8.8h 된 것이 되어 말이 안 되고, 최대 나이도 103h로 필터 자신의 컷(96h)을 넘어버린다.
> **결론(UTC 변환 필요)은 기획이 맞다. 근거 숫자만 정정하면 된다.**

- ~~**BB12. 이력이 없는 목적지에 `median`을 지어낸다.**~~ → **해결(2026-09-01, BE2 T3)**:
  이력이 없으면 `median: null`. 기획이 계약을 먼저 고쳤고(`c4eec37`) 그다음 구현했다.
  (원문)  `discover_data.py`의
  `med = _median(hist) or dd["price"]`가 이력이 0건일 때 **현재가를 평소 시세라고
  단언**한다. 우리가 모르는 값을 아는 척하는 것이라 "가짜 데이터 금지"(`DECISIONS.md`)와
  성격이 같다. 지금은 94건 중 1건뿐이고 `median == price`면 프론트가 비교 막대를
  생략해 화면상 피해는 없지만, 새 목적지를 추가할 때마다 늘어난다.
  - 정직한 값은 `median: null`이지만 계약상 `median`이 number라 **계약 변경이 필요**하다.
  - **기획이 미리 찬성 의사를 밝혔다(2026-08-22)**: 프론트가 이미 `dl.median || 0`으로
    방어하고 `median <= price`일 때 비교 막대를 생략하는 경로가 있어 화면이 안 깨진다.
    → BE2에서 **정식 제안만 하면 기획이 `CONTRACT.md`를 고친다.** 협의는 사실상 끝났다.
  - 참고: `discount=0`인 딜 23건 중 22건은 표본 4건 이상으로 **시세가 제대로 계산된**
    정직한 0이다(프론트 B24는 도장 문구 문제이지 데이터 문제가 아님을 실측으로 확인).

- ~~**BB13. `fetched_date`와 collect 커밋 라벨이 UTC 날짜라 하루 밀려 보인다.**~~
  → **해결(2026-09-01, BE1 T5)**: 커밋 라벨을 `TZ=Asia/Seoul`로 바꿔 KST로 찍는다.
  `fetched_date`는 UTC 유지이며 그 사실을 계약에 명시하도록 기획에 요청했다.
  (원문) 
  크론이 22:10 UTC에 돌아 `date.today()`(러너 UTC)로 `fetched_date`를 찍고,
  커밋 메시지도 `date -u`를 쓴다. 그래서 **KST 08-21 아침 7시에 성공한 수집이
  `2026-08-20`으로 기록**된다. 한국 전용 서비스라 읽는 사람은 전부 KST로 해석한다.
  - 2026-08-22 실제로 오해가 발생했다. 기획 세션이 "크론이 2일째 안 돈다"고
    사고 보고를 올렸으나, 실행 이력상 6일 연속 성공이었고 마지막 실행은 20시간 전,
    다음 실행은 4시간 뒤였다. `found_at`(BB11)에 이어 **같은 함정에 두 번째로 걸린 것**이다.
  - 고치려면 `fetched_date` 의미가 바뀌므로 `detect_deals`·`discover_data`의 날짜
    비교와 기존 행 전부에 영향이 간다. 되돌릴 수 없는 데이터라 신중히 — BE1에서 판단한다.
  - **✅ 방향 확정(2026-09-01, 기획 의견 + 백엔드 판단)** — 구현은 T5:
    **"기계가 쓰는 시각은 UTC, 사람이 읽는 라벨은 KST"**로 가른다.
    - `fetched_date`(데이터 필드) → **UTC 유지**. 기계가 빼는 값이고, 경계에서 aware로
      만드는 `timeutil` 규칙과 일관된다. 그 사실을 `CONTRACT.md`에 명시(기획이 반영).
    - collect **커밋 메시지 라벨** → **KST로 변경**. 라벨을 읽는 사람은 전부 한국 시간으로
      산다. UTC/KST 사고 3건 중 첫 번째가 정확히 이 라벨을 KST로 읽어서 났다.
    - 기존 행은 건드리지 않는다(되돌릴 수 없는 데이터).

- ~~**BB17. `fetched_date`·`date.today()`가 아직 naive다.**~~ → **해결(2026-09-01, BE1 T5)**:
  `timeutil.today_utc()`(기계용)와 `today_kst()`(제품용)로 갈랐다. 수집 라벨은
  UTC로 고정해 크론·로컬이 같은 값을 남기고, "오늘 이후 출발"은 KST로 판단한다.
  (원문)  T2에서 `found_at` 쪽 시간대는
  `timeutil`로 단일화했지만, 날짜 라벨은 여전히 러너 로컬 날짜다(BB13과 같은 뿌리).
  `timeutil.now_kst()`가 생겼으니 BB13을 정할 때 함께 정리할 수 있다.

- ~~**BB19. ⚠️ 계약 검증기가 필드 목록을 하드코딩해 계약을 따라가지 못한다.**~~
  → **해결(2026-09-01, BE2 T2)**: `contract_fields()`가 계약의 deal 필드 표를 파싱해
  필수 필드와 nullable 여부를 뽑는다(22개 필드, nullable 3개). 계약에 필드를 추가하는
  순간 검증기가 요구하기 시작한다 — 실제로 가짜 필드를 넣어 잡히는 것을 확인했다.
  (원문) 
  (2026-09-01 발견, **BE2 최우선**)
  태그 어휘는 `CONTRACT.md` 표를 파싱하는데 **필드 목록(`REQUIRED`)은 하드코딩**이다.
  그래서 기획이 계약에 필드를 추가해도 검증기가 모르고 **CI가 조용히 초록불**이다.
  - 실제로 발생: 계약에 `low`·`obs_days`가 추가됐는데(PH4) 생산자가 안 내보내고 있고,
    검증기도 안 잡는다. `seen` 때와 같은 상황인데 이번엔 **경보조차 울리지 않는다.**
  - 계약의 deal 필드 표는 파싱 가능함을 확인했다(22개 필드가 정확히 읽힌다).
  - → 필드 목록도 계약에서 읽게 고친다. 그래야 "계약이 단일 출처"가 진짜가 된다.

- ~~**BB20. 크론이 push 충돌에 대비하지 않아 그날 수집분을 버린다.**~~
  → **해결(2026-09-01, BE1 후속)**. 커밋 스텝이 `git push`만 하고 `pull`을 안 했다.
  체크아웃과 push 사이에 원격이 움직이면 거부되고, **그날 수집분이 통째로 버려진다.**
  - **2026-09-01 실제로 발생.** BE1 검증차 크론을 수동 실행했는데 내가 동시에 main에
    push해서 거부됐다. 평소엔 새벽 7시라 아무도 안 건드려서 안 터졌을 뿐이다.
  - 세 세션이 main에 push하는 체제라 언제든 재발할 수 있었다.
  - 해결: push 실패 시 `fetch` + `rebase` 후 최대 3회 재시도. 생성물 충돌은
    우리 것(방금 만든 산출물)을 취한다 — 다음 크론이 어차피 다시 만든다.
  - ⚠️ 구현 중 함정 하나: **rebase에서는 `--ours`/`--theirs`가 merge와 반대**다.
    `--ours`가 얹히는 대상(origin/main)이고 `--theirs`가 내 커밋이다. 처음에 `--ours`로
    썼다가 정정했다 — 그대로 뒀으면 충돌 시 오늘 수집분을 버렸을 것이다.

- ~~**BB21. `이번 주말` 라벨이 다음 주말에도 붙는다.**~~ → **해결(2026-09-01)**:
  계약을 달력 주 기준으로 바꾸고 `다음 주말`을 신설했다(기획 `1b57e6a`). 기획이
  같은 뿌리가 `이번 주`에도 있음을 찾아 함께 고쳤다 — 원인은 하나였다.
  **상대 거리(N일 이내)로 재면서 라벨은 달력 단위를 말한 것.**
  (원문)  (2026-09-01 코드 리뷰에서 발견,
  **계약 규칙이라 기획 결정 대기**)
  계약 §when 1번이 `9일 이내 + 금·토·일`인데, 그 9일 창이 **다음 주말까지 삼킨다.**
  - 요일별: 월·화 없음 / 수 +9(금) / 목 +8,+9 / **금·토·일은 다음 주말 3일 전부**.
    주 7일 중 5일에서 발생하고, **주말 접속자에게 가장 심하다.**
  - BB7·BB8과 같은 종류다 — 라벨이 사실보다 가깝게 말한다. 이건 **임박 신호**라
    사용자 행동에 직접 영향을 준다.
  - 제안: 9일 창 대신 "이번 주(월~일)의 금·토·일"로 판정. 또는 `다음 주말` 어휘 신설.
  - 2026-09-01(화) 기준 영향 0건. **금요일부터 실제로 보이기 시작한다.**
  - 기획이 계약 §when을 고치면 구현한다. 검증기의 `when` 정규식도 함께 고쳐야 한다.

- **BB22. 노선 커버리지** (진행 중 → **대부분 해결**, 남은 건 기획 `SPEC.md` §3 `BE8-1`)
  **2026-09-21 실측: 노선 43개 · 딜 138건 중 `route`가 있는 것 40건 = 29%**(원문 20%). 부산 9노선(2026-09-01)과
  BE8 1차 7개(2026-09-20)로 올랐다. 대구도 첫 노선이 생겼다. 「유지 하한」(넣을 때만 재고 그 뒤 안 재는 것)은 기획이 가져갔다.
  (원문) **노선 페이지가 서울 출발뿐이라 커버리지가 20%다.**
  → `route` 필드는 **해결(2026-09-01)**. 백엔드가 실제 공항으로 판정해 넣고,
  검증기가 허브·목적지 정합성까지 본다. 프론트는 `route`가 있을 때만 링크를 보인다.
  → **부산 9노선 추가 완료(2026-09-01)**. 커버리지 18% → **25%**(32/128).
  선정은 실측이다 — v3 API가 하루 10건 이상 주는 곳만 넣었고, 오키나와(8건)는 뺐다.
  표본이 얇으면 월별·요일별 차트가 비어 얇은 SEO 페이지가 된다.
  → **남은 것: 대구·제주.** 실측상 하루 1~6건뿐이라 지금 넣으면 빈 차트가 나온다.
  이력이 쌓이면 달라질 수 있으니 한 달 뒤 재측정 후 판단한다.
  → (참고) 원래 기획 지침: **출발지부터**(부산 → 대구·제주) 넓히라고
  판단했다. 부산이 딜의 29%인데 페이지가 0개이고, 목적지 확장은 페이지를 더하는 것이지만
  출발지 확장은 **틀린 것을 고치는 것**이기 때문이다. 부산 우선순위:
  제주·후쿠오카·오사카·상하이·세부·타이베이·도쿄·오키나와·다낭·푸꾸옥.
  **한 번에 다 늘리지 말고 부산부터 시작해 SEO 유입을 보고 늘릴 것**(기획 권고 —
  효과를 안 재고 늘리면 나중에 줄일 근거도 없다).
  (원문)  (2026-09-01 발견,
  **기획 결정 대기**)
  `config.ROUTES`의 출발 공항이 `ICN`·`GMP` 둘뿐이라 **부산·대구·제주 출발 노선 페이지가
  하나도 없다.** 그런데 딜의 45%가 서울이 아니다(부산 37 · 대구 13 · 제주 8 / 128건).
  - 기획이 IA-1(확장 상세 → 노선 페이지 링크)을 46%로 계산했는데 **목적지만 맞춘 값**이다.
    `(출발지, 목적지)` 쌍으로 재면 **20%(26/128)**. 차이 32건은 부산·대구 딜을
    인천 노선 페이지로 보내는 경우다 — 가격도 시세도 다른 노선이다.
  - 부수 문제: 허브 `SEL`이 실제 공항이 아니라 **서울 딜조차 `ICN-FUK`인지 `GMP-FUK`인지
    프론트가 알 수 없다.** `route` 필드(있으면 `"ICN-FUK"`, 없으면 `null`)를 계약에
    추가하면 깔끔하다 — 내가 `_oi`를 알고 있어 판정 가능하다.
  - 확장 여지는 있다: API 호출이 하루 31회로 제한(200회/시간) 대비 여유가 크다.
    후보(30일간 자주 잡히는데 페이지 없음): 푸켓 81 · 상하이 80 · 이스탄불 76 ·
    푸꾸옥 70 · 싼야 65 · 몰디브 58 · 칭다오 58 · 광저우 57 · 두바이 54 · 호놀룰루 52.
  - **두 방향의 성격이 다르다**: 목적지 확장은 SEO 자산이 늘고, 출발지 확장(부산·대구·제주)은
    위 32건 오링크를 직접 해결한다. 어느 쪽이 값어치 있는지는 제품 판단이라 기획 몫이다.

- ~~**BB23. `labels.CITY`와 `dests`에 같은 지식이 두 곳으로 있다.**~~
  → **해결(2026-09-01, BE3 T2)**. 부산 노선을 추가했더니 페이지 제목이
  **`"PUS → 도쿄"`**로 나갔다 — 부산·대구가 `dests.ORIGINS`에만 있었기 때문이다.
  `labels.city()`가 `dests`로 폴백하도록 이어 단일 출처로 만들었다. 두 사전이
  어긋나면(또는 `CITY`에만 있는 코드가 생기면) 테스트가 잡는다.
  확인해보니 지금은 이름이 전부 같고 `CITY`가 `dests`의 부분집합이라 폴백만으로 충분했다.

### 미분류
- ➡️ **BB6 — 프론트 저장소로 넘어갔다**(M3 T3에서 `build_index()`가 사라졌다). 백엔드는 HTML을 만들지 않는다. 프론트 `BACKLOG.md` B13.
  (원문) **`docs/index.html`이 278KB.** `deals.json`+`world.geojson` 인라인 때문.
  `file://`로도 열리게 한 의도적 선택이지만(`PROJECT.md`) 비용 측정은 안 됐다.
  인라인 주체가 `build_index()`(백엔드)라 여기 적어둔다. 프론트 `BACKLOG.md` B13과 같은 항목.

- **BB24. 예약처별 실시간 가격(Flight Search API)은 MAU 5만이 조건.** (2026-09-02 실측)
  `TP_MARKER` 등록 직후 현행 엔드포인트로 확인했다.
  - **엔드포인트**: `POST https://tickets-api.travelpayouts.com/search/affiliate/start`
    (2025-11-01 신버전. 구버전 `api.travelpayouts.com/v1/flight_search`로 먼저 쳐본 건 내 실수다.)
  - **결과**: `403 access denied`. 헤더(`x-signature`·`x-affiliate-user-id`·`x-user-ip`·`Referer`·
    `User-Agent`)와 서명·본문을 전부 문서대로 맞춘 뒤의 결과다. 코드 문제가 아니다.
  - **공식 조건**: *"We only provide access to Projects with MAU starting from 50000."* → 현재 트래픽 0.
  - **마커 자체는 유효**하다. 틀린 마커는 `401`, 우리 마커는 `403`으로 갈린다 — 신원은 인식되고
    권한만 없다는 뜻이다. 같은 토큰으로 v3 데이터 API는 `200`이다.
  - **파급**: 이 API가 예약처별 가격을 주는 **유일한 합법 경로**다(타 비교사이트 크롤링은 금지 —
    `CLAUDE.md`). 따라서 "어디서는 얼마" 비교 패널은 MAU 5만 전까지 불가능하고, 그때까지
    **서버(Cloudflare Workers) 도입도 명분이 없다** — 붙여도 호출할 API가 없다.
  - **신선도 쪽 이득도 상한이 낮다**(같은 날 실측, `broad_offers` 3,454건): v3를 실시간 호출해도
    캐시 나이가 **수집 순간 기준 중앙 45h**(25% 23h · 75% 68h)라, 지금의 `중앙 80h`에서
    `45h`로 줄이는 게 전부다. 캐시는 남들이 검색해야 갱신되므로 우리가 더 자주 불러도 못 내린다.
  - **언제**: BE5에서 Trip.com 재신청과 함께. 둘 다 조건이 트래픽이라 타이밍이 같다.

- ~~**BB25. `detect_deals.py`·`send_alerts.py`가 아직 `date.today()`를 쓴다.**~~ → **해결(2026-09-20, BE14 T1)**:
  셋 다 옮겼다. 용도로 갈랐다 — `compute_deals`의 빈 DB 대체값·`alert_log.sent_date`는 `fetched_date`와 같은 축이라
  `today_utc()`, 판정 리포트 머리(`[날짜] 특가 N건`)는 사람이 읽는 라벨이라 `today_kst()`(커밋 라벨과 같은 날짜가 된다).
  `KNOWN_DEBT` 목록과 그 정확성 테스트는 지웠고 **새 위반을 막는 테스트는 남겼다**(탐침: 위반을 심으면 실패).
  (원문) (2026-09-02, BE4 T1에서 발견)
  BB13·BB17에서 `timeutil`을 만들어 "새 코드는 반드시 여기를 쓴다"고 정했는데
  **기존 모듈 3개는 옮기지 않았다.** BE4에서 `build_site.py` 4곳만 옮겼고
  (`build_seo`의 sitemap `lastmod` 포함) 나머지 둘은 **안 고쳤다** — BE4 스코프 밖이다.
  - `detect_deals.py:27,67` · `send_alerts.py:77`
  - `detect_deals.py`는 코드 주석으로 위험을 이미 인지하고 있다 —
    *"Actions 러너는 UTC라 `date.today()`와 어긋날 수 있음"*.
  - **당장 안 터지는 이유**: 크론은 UTC 러너에서만 도니 운영 경로는 일관적이다.
    어긋나는 건 **로컬에서 재현·디버깅할 때**(KST 15:00~23:59 구간)다.
  - `tests/test_timeutil.py`의 `NaiveTodayDebtTest.KNOWN_DEBT`에 두 파일이 적혀 있다.
    **새 위반은 테스트가 막고**, 옮기면 목록에서 지우면 된다. 목록이 비면 테스트째 지운다.

- ~~**BB26. 지역 어휘가 두 개다 — `labels.REGION_NAME` vs `dests.REGION_NAME`.**~~ → **해결**(BE4 이후 어느 시점,
  2026-09-21 코드로 확인): `labels.py`에 자체 `REGION` 사전이 없다. `region_of()`가 `dests.DEST`만 보므로 어휘가 하나다.
  (원문) (2026-09-02, BE4 T3에서 발견)
  `labels`는 대양주를 **"미주·대양주"에 합치고** 괌을 **"국내·괌"에 넣는다.**
  `dests`에는 `island`(휴양·섬)·`oc`(대양주)가 따로 있다. `CONTRACT.md`가 계약으로
  들고 있는 enum은 **`dests` 쪽**이다.
  - 지금 어긋나는 건 **GUM·SYD 둘뿐**이다. `region_of()`가 `REGION`에 있는 코드는
    건드리지 않고 없는 것만 `dests`로 폴백하기 때문이다.
  - **안 고친 이유**: `"국내·괌"`·`"미주·대양주"`는 **사용자가 보는 문자열**이라
    `COPY.md` 소관이다. 통합하면 노선 페이지 브레드크럼 표시가 바뀐다 → 기획 판단.
  - `tests/test_labels.py`의 `test_known_taxonomy_divergence_is_only_these_two`가
    **새로 갈라지는 것만** 실패로 잡는다. 기획이 정하면 그 테스트를 지우면 된다.

- ~~**BB27. `build_site.py`에 죽은 코드가 있다.**~~ → **해결(2026-09-15, M3 T3)**:
  `build_site.py`를 통째로 지우면서 `deal_card`·`sparkline`·`mail_deal_rows`·`mail_rows`가
  같이 사라졌다. **따로 지우는 작업을 한 번도 안 했다** — 2026-09-08에 「이전이 곧 해결이라
  따로 챕터를 열 이유가 없어졌다」고 판단한 그대로다. 죽은 코드를 급히 지웠으면
  그 시간만 쓰고 결과는 같았을 것이다.

- ~~**BB28. 차트는 표본 임계를 지키는데 그 위의 문장은 안 지킨다.**~~ → **해결(2026-09-16, 프론트 M2)**:
  `site/route.py:61` `usable()` 하나를 차트와 문장에 **같이** 건다(`:140` `:143`). 계획대로
  「이전하면 `tips`는 프론트 구역이 되니 그때 고친다」가 그대로 됐다 — **급히 안 고친 게 맞았다.**
  (아래는 발견 당시 기록)
- **(원문) 차트는 표본 임계를 지키는데 그 위의 문장은 안 지킨다.** (2026-09-08, 기획의 P7 질의를 확인하다 발견)
  BE4에서 두 차트를 `len(rows) < 2`로 통일했는데(`charts.NOT_ENOUGH`), **정작 2026-09-03
  타이중에서 실제로 잘못 나갔던 건 차트가 아니라 문장이다** — `build_site.py:207`이
  `if best_month:`(참·거짓)일 뿐이라 **월 버킷이 하나여도 "9월 출발이 가장 저렴합니다"를 쓴다.**
  9월밖에 없으니 동어반복이다. `best_wd`(`:209`)도 같다.
  - **지금은 안 터진다** (2026-09-08 실측): 36노선의 월 버킷 수가 최소 3, 최빈 10이다.
    `month_min`의 `HAVING COUNT(*)>=3`이 얇은 달을 걸러주고 있어 우연히 가려져 있다.
  - **새 노선을 넣으면 그날 재현된다.** 그게 타이중이 지난 경로다.
  - **안 고쳤다** — 열린 챕터가 없고, 이전하면 `tips`는 프론트 구역(사이트 산출물)으로 간다.
    고칠 자리가 곧 바뀌므로 **계약에 임계를 표현할 자리를 만드는 쪽**이 맞다(`MIGRATION.md` §11.7).
  - `tests/test_charts.py`는 차트만 본다. 문장은 아무도 안 본다.

- ~~**BB29. 🔴 노선 페이지 5개가 지금 "이미 지난 달에 떠나라"고 말하고 있다.**~~ → **해결**(2026-09-21 확인):
  `route_stats.month_min`에 `AND depart_date>=?`(`today_kst()`) 창이 있다. 「창은 백엔드」 원칙대로 영구히 우리 일이다.
  (원문) (2026-09-08, 계약 v1 판정 중 발견 — **라이브 결함**)
  `month_min`에 **`depart_date` 창이 아예 없다.** 출발월로 묶기만 하고 지난 달을 안 버린다.
  2026-09-08 배포본 실측:

  ```
  ICN-GUM  "8월 출발이 가장 저렴합니다 (232,329원)"
  ICN-HKG  "8월 출발이 가장 저렴합니다 (216,817원)"
  ICN-LAX  "8월 출발이 가장 저렴합니다 (793,022원)"
  ICN-NGO  "8월 출발이 가장 저렴합니다 (162,715원)"
  ICN-KUL  "7월 출발이 가장 저렴합니다 (331,063원)"
  ```

  **살 수 없는 달을 추천하고 있다.** BB28은 가정이었지만 이건 지금 나가고 있다.
  - **왜 이제 보이나**: `offers`의 `depart_date`가 2026-07-09부터다. 이력이 두 달을 넘긴
    2026-09부터 지난 달이 버킷으로 남기 시작했다. **시간이 지나야 나타나는 종류**다.
  - **계약 v1이 이걸 악화시킨다**: `HAVING COUNT(*)>=3`·`LIMIT 10`을 빼면(기획 M1 1번)
    지난 달 버킷이 **51개**로 드러난다(전체 353개 중). ICN-NRT의 2026-08은 **915건**이라
    표본만 보면 가장 튼튼하다 — 임계로는 못 거른다. **창으로 잘라야 한다.**
  - **누구 몫인가**: 계약 v1의 규칙대로 **창은 백엔드**다. 이전 후에도 `month_min`은
    백엔드에 남으므로 **이건 영구히 우리 일이다.** 프론트 임계로는 해결되지 않는다.
  - `weekdays`는 다르다 — 요일은 순환하므로 지난 데이터도 유효하다. 월만의 문제다.
  - **안 고쳤다** — 열린 챕터가 없다. 한 줄(`AND depart_date >= :today`)이고 사용자 승인 대기.

- ~~**BB30. 🔴 시크릿 없는 환경에서 재빌드하면 제휴 링크가 조용히 빠진다.**~~ → **해결**(2026-09-21 확인):
  `publish.warn_if_unpaid()`가 빌드 처음과 끝에서 외치고, `tests/test_affiliates.py`가 **커밋된 발행물**을 딜마다 검사한다.
  (원문) (2026-09-08, 프론트 보고 → 실물 확인)
  `affiliates.py:154`가 `if _env("TP_MARKER"):`로 Aviasales 링크를 건다. 마커가 없으면
  **예외가 아니라 그냥 빠진다** — 딜 전건에서. 프론트가 `index.html` 충돌을 `CLAUDE.md`의
  「재빌드로 해결」 절차대로 풀다가 125건 전부에서 수익 링크를 날릴 뻔했다(커밋 직전 복구).
  - **함정은 절차 쪽이다.** `CLAUDE.md`의 충돌 해결 규칙은 재빌드를 시키는데, 그 규칙은
    **재빌드 환경에 시크릿이 있다고 가정한다.** 크론(Actions)은 있고 로컬은 사람마다 다르다.
  - 내 로컬엔 `TP_MARKER`가 있어 내 재빌드는 안전하다. **위험한 건 없는 쪽이고, 그쪽이
    실패를 못 알아챈다** — 사이트는 멀쩡히 뜨고 링크만 사라진다.
  - **막을 자리는 백엔드다**: `build_site.py`가 마커 없이 도는 걸 **경고 없이 통과시킨다.**
    빌드가 큰 소리로 알리거나 거부해야 한다. `charts.NOT_ENOUGH`·`R1`과 같은 부류다.
  - `CLAUDE.md`는 **공용 파일**이라 내가 못 고친다 → 사용자에게 보고했다.
  → **해결(2026-09-08, 사용자 승인)**: 두 겹으로 막는다.
    1. `build_site.warn_if_unpaid()` — 빌드 처음과 **끝**에서 외친다(사람은 출력의 끝을 읽는다).
       산출물은 안 바꾼다. **경고만**이라 이전의 diff-0 관문에 걸리지 않는다.
    2. `tests/test_affiliates.CommittedArtifactTest` — **커밋된 `deals.json`**을 검사한다.
       경고는 넘길 수 있지만 CI는 못 넘긴다. 실제 재현으로 확인했다(125/125건 검출).
    ⚠️ **`REQUIRED_SECRETS`에 `TP_TRIP_*`는 넣지 않았다.** 승인 전이라 **없는 게 정상**이고,
    정상 상태에 경고를 띄우면 사람이 경고를 무시하는 법을 배운다. 첫 구현이 그랬고 고쳤다.
    Trip.com이 승인되면(BE5) 그때 넣는다.
  - **방어 범위 (2026-09-08 실측 — 125건 중 3건만 마커를 남긴 산출물로 재현)**:

    | | 완전 손실 | 부분 손실 |
    |---|---|---|
    | `build_site` 경고 | ✅ | ❌ 개수를 안 세고 시크릿 존재만 본다 |
    | `grep -c '"ad":true'` (`CLAUDE.md`) | ✅(우연) | ❌ |
    | `grep -o … \| wc -l` | ✅ | ✅ |
    | **`CommittedArtifactTest`** | ✅ | ✅ 딜마다 보므로 122건이 걸린다 |

    🔴 `deals.json`은 **개행이 없는 한 줄**이라 `grep -c`는 건수가 아니라 **줄 수**를 센다 —
    정상(125건)일 때도 `1`이다. `CLAUDE.md:93`이 기대값을 125로 적어 **정상을 이상으로
    오인시킨다.** 공용 파일이라 못 고친다 → 기획·프론트·사용자에게 알렸다.
    **손으로 세는 건 선택이고 CI가 진짜 방어선이다.**

- ➡️ **BB31 — 소멸/이관**: `labels.fmt_month`를 BE13 T3에서 지웠다(호출자 0). 월 표시는 프론트가 만든다.
  (원문) **`fmt_month`에 연도가 없다 — BB29를 고치자 드러났다.** (2026-09-08, BB29 수정 직후 발견)
  `labels.py:71`이 `'2027-02' → '2월'`로 연도를 버린다. 지난 달을 자르기 전에는
  답이 대개 이번 달이라 안 보였는데, 창을 넣자 **`ICN-LAX`가 `"2월 출발이 가장 저렴합니다"`,
  `ICN-KUL`이 `"6월 출발"`**을 쓰기 시작했다 — 둘 다 실제로는 **2027년**이다.
  - **지금은 헷갈리지 않는다**: 미래만 남으므로 `"2월"`은 **다음에 오는 2월**로 유일하게 읽힌다.
    실측상 같은 월 이름이 두 번 나오는 노선도 없다(`LIMIT 10`이 12개월을 못 넘게 막는다).
  - 🔴 **M1이 `LIMIT 10`을 빼면 깨진다.** 버킷이 12개를 넘으면 `"9월"`이 2026·2027 둘을
    가리키게 되고, 막대 두 개가 같은 이름을 달게 된다.
  - **BB8과 같은 결함이다** — 그때는 `when` 라벨이었고 이번엔 월 라벨이다.
    "라벨이 사실보다 가깝게 말한다"는 같은 뿌리.
  - **안 고쳤다** — 표시 문자열이라 `COPY.md`(기획) 소관이고, 이전하면 프론트가 만든다.
    기획·프론트 양쪽에 알렸다.

- ~~**BB34. 산출물 대조 테스트가 「그날」에만 통과한다.**~~ → **해결(2026-09-17, BE9 T1 `115a44d`)**. (2026-09-17, M5 ②~③ 중 발견)
  `ReproducesTheCurrentScreenTest`·`test_backend_does_not_drop_thin_buckets` 등이
  **커밋된 `docs/v1/`**을 **테스트 시각의 즉석 계산**과 대조한다. 두 창(`today_utc()-30일`,
  `depart_date >= today_kst()`)이 시각에 따라 움직이므로 **산출물을 만든 날이 지나면 어긋난다.**
  - 실측: 산출물 생성 시각으로 시계를 고정하면 **16/16 통과**, 하루 지나면 57건 실패.
  - 구형에선 크론이 매일 산출물을 새로 만들고 같은 날 테스트해서 안 보였다.
    **신형 `test.yml`은 push마다 돈다** — 크론 다음 날 아침 전에 push하면 **CI가 빨간불**이다.
  - 수정 방향: 대조 테스트의 시계를 **`meta.generated`에 고정**한다(산출물이 만들어진 그 시각으로).
  - **안 고쳤다.** 이전 중 동작 변경 금지 사유는 M5로 끝났다 — 헛 빨간불이 반복되면 CI를 안 믿게 되므로 **다음 챕터 첫 태스크**(기획 동의 2026-09-17).

- ~~**BB35. `generated` 단일성이 코드 성질일 뿐 잠겨 있지 않다.**~~ → **해결(2026-09-17, BE9 T2 `45a4e05` · T3 `0bdf498`)**. (2026-09-17, 기획 요청)
  프론트 T6d가 받은 v1 응답들의 `generated` 일치로 「섞인 스냅숏」을 잡는다. 지금은 `publish()`가
  한 값을 모두에 넘겨 지켜지지만, `_envelope(generated=None)` 기본값이 함정이다 — 새 payload가
  빼먹으면 그 파일만 초 단위로 갈린다.
  - ⚠️ **「전부 같다」는 틀린 규칙이다**: 보존일엔 `deals.json`이 어제 `generated`로 남는다(BB1 합의).
    **확정(CONTRACT.md §공통 규칙)**: `meta`·`routes/index`·index에 실린 `routes/*` = G ·
    `deals` = G(`preserved=false`) / `deals` < G(`preserved=true`).
  - 할 일: `generated` 필수 인자화 + 확정된 규칙 그대로 테스트. **BB34와 같은 챕터.**
    🔴 **보존일 케이스 필수** — 하한선 미달을 인메모리로 만들어 `deals < G`·나머지 `== G`를 본다.
    규칙이 한 날(보존 아닌 날)의 관측에서 나왔다가 틀렸던 분기다. 빠지면 같은 일이 반복된다.
    **실제 위반 사례(프론트 제보)**: 구형 `05d0de9`의 `docs/v1` — `meta` 15:28:42 · `deals` 15:28:00 ·
    `preserved=false`. deals만 42초 이르다(M1 때 분 단위 `updated`에서 만들던 흔적). 반례 입력으로 쓴다.

- **BB33. `mail_ingest`가 메일함을 소비한다 — `subscriptions`와 다르게 읽는다.** (2026-09-16, M4 T6c 중 기획 발견)
  ```
  mail_ingest.py:67   select("INBOX")            readonly 아님
             :73   fetch(mid, "(RFC822)")        PEEK 아님 → \Seen 이 붙는다
             :68   search(UNSEEN)                다음 사람은 그 메일을 못 본다
  subscriptions.py:56 select("INBOX", readonly=True) + BODY.PEEK   ← 이쪽이 옳다
  ```
  **같은 메일함을 읽는 두 코드가 서로 다르게 짜여 있다.** 한 저장소만 돌 때는 안 보였는데,
  이전 중 **두 저장소가 같은 메일함을 보게 되면서** 드러났다 — 한쪽이 읽으면 다른 쪽이 빈손이 된다.
  - 지금은 `skip_side_effects`(T6c)로 수동 실행에서만 막아 뒀다. **근본은 안 고쳤다.**
  - 고칠 때: `mail_ingest`도 PEEK로 읽고 **파싱에 성공한 뒤에** 명시적으로 `\Seen`을 다는 게 맞다.
    지금은 **읽자마자 소비**해서, 파싱이 실패해도 그 메일은 다시 안 온다.
  - 🔴 **이전 때문에 발견됐을 뿐 이전과 무관한 버그다.** 한 저장소만 돌 때도
    **파싱이 실패하면 그 메일은 영영 다시 안 온다** — 상시 데이터 손실이다.
    M6 정리에서 「이전 흔적」으로 오인해 지우지 말 것(기획 지적).
  - M6 후 별도 챕터. 이전 중에는 동작을 바꾸지 않는다.

- ~~**BB32. 구독 메일 주소가 두 곳에 있다 — 갈리면 아무도 모른다.**~~ → **해결**(M3 T5, 2026-09-21 재확인):
  `subscriptions.SUBSCRIBE_ADDR`이 정본이고 크론 상태 점검이 매일 `MAIL_ADDRESS`와 대조한다.
  (원문) (2026-09-08, M3 준비 중 발견)
  ```
  theme.SUBSCRIBE_ADDR = "flightpromokr@gmail.com"   ← 화면·meta.json 이 쓰는 값
  .env MAIL_ADDRESS                                  ← subscriptions.py 가 IMAP으로 로그인하는 메일함
  ```
  **오늘은 같다**(2026-09-08 실측). 하지만 **대조하는 게 아무것도 없다.**
  갈리면 사이트는 A로 보내라 하고 우리는 B를 읽는다 — **반송도 안 온다**(A도 실재하는 주소라).
  사용자는 신청했다고 믿고 우리는 신청이 없다고 믿는다. `BASE_URL`/`SITE_URL`(R1b)과
  **글자 그대로 같은 유형**이고, 이번이 두 번째다.
  - 🔴 **`SUBSCRIBE_ADDR`은 `theme.py`에 있을 물건이 아니다.** 사이트 정체성이 아니라
    **우리 파서가 로그인하는 메일함**이다. 프론트가 `theme.py`를 가져가는 M3에서
    같이 딸려가면 안 되고 **`subscriptions.py`로 옮겨야 한다**(제목 두 상수 옆).
    그러면 `meta.json`의 `subscribe` 넷이 **전부 파서 한 모듈에서** 나온다.
  - 프론트는 `meta.json`에서 받아 쓰므로 화면 쪽 의존은 저절로 끊긴다.
    → **해결(2026-09-16, M3 T5)**: `subscriptions.py`로 옮겼다. `meta.subscribe`의 네 값이
    **전부 파서 한 모듈**에서 나온다. 테스트가 `SUBSCRIBE_ADDR`을 바꿔보고 발행값이
    따라오는지 확인한다 — 문자열을 복사해 두면 실패한다(변이 검증 완료).
    딸려온 것: `publish.py`·`tests/test_publish.py`가 **`theme`을 아예 안 쓰게 됐다**(import 제거).
    백엔드 출구가 화면 모듈에서 떨어졌고 T4 걸림돌이 둘 줄었다.
    ⚠️ **프론트 `site/shell.py:31`에 같은 주소가 literal로 남아 있다** — 화면 푸터의 문의처다.
    `meta.json`에서 읽어야 이 갈림이 끝난다. 프론트 구역이라 알리기만 했다.

### BE12 — 중간 점검에서 나온 것 (2026-09-19, 코드 전수 읽기 · **전부 안 고쳤다**)

> 읽은 범위: `collector/` 16개 · 워크플로 2개 · `contract/v1/` 2개 · `tests/`는 skip·네트워크·sleep 지점만.
> 안 한 것: 크론 파이프라인 통째 실행(부작용), 이 문서 §8~`MIGRATION.md` §15 정독.
> 기준선: 테스트 183건 `OK` · 딜 137 == 제휴 링크 137 · `.env`/`emails_raw.db` git 이력 없음 · 미사용 import 0.

- ~~**BB36. 🔴 크론이 UTC 자정에 걸쳐 `fetched_date`에 구멍이 난다.**~~ → **해결(2026-09-20, BE13 T1 / 2026-09-22 보강)**:
  🔴 **2026-09-21에 다시 걸렸다** — 20:10Z 예약이 **23:13Z에 시작**(지연 3h03m)해 자정 가드가 잡을 실패로 만들었다.
  데이터·발행·사이트는 전부 정상이었다(구멍 0) — **가드가 경보로 일한 것이다.** 그래서 2026-09-22에 둘을 고쳤다:
  ① 예약을 **14:10Z + 18:10Z 두 번**으로(23:10·03:10 KST). 최악 3시간 지연에도 자정까지 2h50m 남는다.
  ② 예비 실행은 **`meta.generated`가 오늘(KST)이면 즉시 끝낸다** — 정상인 날 두 번 발행·커밋하지 않고 프론트도 두 번 굽지 않는다.
     수집 여부가 아니라 **발행까지 끝났나**를 보므로 「수집은 됐는데 발행이 실패한 날」도 예비가 그날 안에 되살린다.
     수동 실행은 가드와 무관하게 언제나 돈다. 탐침 4가지 경우 확인.
  📏 **왜 지연되나(실측 + 문서)**: GitHub 문서 — *"The schedule event can be delayed during periods of high loads …
     **some queued jobs may be dropped**."* 커밋 80건으로 잰 중앙 지연: **7월 58분 · 8월 41분 · 9월 121분**.
     🔴 **22:10Z → 20:10Z 로 옮겨도 줄지 않았다**(9월 107~184분) — **시간대가 아니라 큐 사정이다.** 우리가 고칠 수 없다.
     설명 안 되던 수집일 구멍(08-26)은 「dropped」일 수 있다 — 그래서 예비 실행을 뒀다.
  (원문 해결)
  예약을 `10 20 * * *`로 옮기고, 「상태 점검」에 **실행 시각 점검**을 넣었다 — 예약 실행이 23시·00시대 UTC에
  돌면 잡을 실패로 만든다(수동 실행은 안 본다). 시각만 옮기면 GitHub 지연이 다시 커지는 날 **조용히** 되돌아오기
  때문이다. 탐침: 22·01·20시 통과 / 23·00시 실패 / 00시 수동 실행 통과. 전환일(09-19 라벨)은 한 번 더 덮일 뿐 구멍은 없다.
  ✅ 첫 예약 실행(2026-09-19 22:18:39Z 시작, 지연 2h08m, success) — 라벨 `09-19` 정상, 가드 통과.
  (원문) (발행물까지 나가는 **조용한 데이터 손실**)
  예약 22:10 UTC + GitHub 지연 ~1h50m = **실제 실행이 00:00 UTC 앞뒤**. `fetched_date`는
  `today_utc()`라 실행이 자정 앞이냐 뒤냐에 따라 라벨이 하루씩 흔들린다.

  ```
  커밋 시각(UTC)      fetched_date
  09-06 23:47         09-06
  09-08 00:14         09-08          ← 09-07 이 없다
  09-12 00:11         09-12
  09-12 23:57         09-12          ← 같은 날 두 번 — broad_offers 는 INSERT OR REPLACE 라 앞 실행을 덮는다
  09-14 00:06         09-14          ← 09-13 이 없다
  ```

  - **실측**: `broad_offers` 53일 중 49일, `offers` 73일 중 69일. 누락 `08-06 · 08-26 · 09-07 · 09-13`.
    뒤의 둘은 위 표로 원인을 확정했다. 앞의 둘은 같은 유형으로 보이나 **확정하지 않았다**(수동 실행이 섞인 구간).
  - **발행물에 그대로 나간다**: `routes/ICN-NRT.json`의 `trend` 28점에 09-07·09-13이 없다.
    `_prior_history`의 `obs_days`·`median`·`low`도 그만큼 표본을 잃는다. `STALE_DAYS` 창이 품는
    수집 횟수도 3회/4회로 흔들린다.
  - **왜 몰랐나**: `MIGRATION.md` §13 「T1 대조가 못 보던 것」이 "크론은 이미 UTC 자정에 걸쳐 있다"까지는 적었다.
    그때 문맥이 sitemap 날짜라 **데이터 쪽 귀결을 안 따졌다.** 구멍은 예외도 경고도 없이 난다.
  - 지금(09-18·19)은 시작이 00:13Z·00:17Z로 **우연히 자정 뒤쪽에 몰려 있을 뿐**이다.
  - **고칠 방향(사용자 승인 2026-09-19)**: 예약을 `10 20 * * *`(05:10 KST)로 옮긴다. 지연이 붙어도
    22시대 UTC라 자정과 멀다. 한 줄이고 계약 영향 없음. 라벨을 KST로 바꾸는 안은 BB13 결정
    (`fetched_date`는 UTC 유지)을 뒤집으므로 택하지 않는다. → **BE13 T1.** 옮기면 프론트에 알린다
    (프론트 `machine_date` 주석·공통 흐름도가 「크론 시각의 정본은 collect.yml」이라 적고 있다).
  - 이미 난 구멍은 메우지 않는다 — 없는 관측을 지어내지 않는다(§5 가짜 데이터 금지).

- ~~**BB37. 🔴 구독취소 회신이 조용히 실패한다.**~~ → **해결(2026-09-20, BE13 T2)**:
  **해지는 제목에서만 노선을 읽는다.** 본문에 코드를 적은 해지는 전체 해지가 된다 — 과잉 해지는 다시 신청하면
  되지만 과소 해지는 법 위반이라 그쪽으로 틀리게 골랐다. `ALL` 구독자의 노선 해지는 `"!ICN-FUK"` 제외로 남기고,
  발송은 집합을 직접 뒤지지 않고 `subscriptions.wants()`에 묻는다. 판정을 `apply_message`(순수 함수)로 떼어
  `tests/test_subscriptions.py` 15건을 새로 만들었다 — 인용문은 손으로 안 적고 **`build_mail()`이 실제로 만든 본문**을 쓴다.
  탐침: 옛 로직을 같은 테스트에 걸면 6건 실패(회신 해지 3 · ALL 노선 해지 2 · 발송이 `wants`를 안 부름 1).
  구독 쪽(본문에서 코드 읽기)은 안 바꿨다 — 계약 `route_token` 그대로라 `meta.subscribe` 영향 없음.
  푸터 안내를 「제목을 '구독취소 ICN-FUK'처럼」으로 고쳤다. 노선별 해지는 계약·`COPY.md`에 없는 백엔드 메일만의 기능이다.
  ⚠️ 남은 한계: 회신 제목을 안 고치고 `Re: (광고) …` 그대로 보내면 해지로 **인식조차 안 된다**(원래 그랬다). BE6에서 볼 것.
  (원문) (법적 위험 — 수신거부 의무)
  알림 메일 푸터(`send_alerts.py:81-82`)가 "제목 '구독취소'로 **회신**"하라면서 예시로 `ICN-FUK`를 적는다.
  회신은 원문을 인용하므로 `subscriptions.py:70`의 `_extract_route(html_body(msg))`가 **인용문 속 예시**를
  집어 「ICN-FUK만 해지」로 처리한다. 탐침으로 재현했다 — `{'ALL'}` 구독자는 해지 후에도 `{'ALL'}` 그대로다.
  - 같은 뿌리 하나 더: `ALL` 구독자가 **정말로** 특정 노선만 해지해도 `discard(route)`가 아무것도 안 지운다.
  - **지금 실해는 없다** — `alert_log` 0행(발송된 적 없음). BE6(알림 재개) **전에** 반드시 고친다.
  - `subscriptions.py`에는 테스트가 0건이다. 고칠 때 `tests/test_subscriptions.py`를 새로 만들고
    「푸터를 인용한 회신 → 전체 해지」를 탐침으로 잠근다. → **BE13 T2.**
  - 고칠 방향(미확정): 해지는 **제목만** 보고 본문에서 노선을 뽑지 않는다 / 푸터 예시에서 코드 모양을 뺀다.
    노선별 해지 규약을 바꾸면 `meta.subscribe`(전선 규약)에 닿을 수 있다 → 그 경우 기획 먼저.

- **BB38. 메일 파이프라인의 산출을 아무도 쓰지 않는다.** (기획 결정 필요)
  `mail_ingest` → `parse_mail`(Haiku 과금) → `mail_deals` 25행(2026-07-10 ~ **09-17**). 그런데 `publish.py`는
  `mail_deals`를 싣지 않고, 읽는 코드가 저장소에 없다(grep 확인). M3 T3에서 `build_site.mail_deal_rows`가
  사라지며 소비자가 없어졌다. 매일 메일함을 소비(BB33)하고 과금만 한다.
  - 끌지 / v1에 실을지(계약 **추가**) / 그대로 쌓을지는 제품 판단 → 기획에 묻는다. **안 껐다.**
  - **사용자 결정(2026-09-20): 수집은 계속 쌓는다.** 발행은 미정 — 기획이 「프로모션을 DB로 관리 + 유통기한」을 설계하려고 전수 측정을 요청했다.
  - 📏 **전수 측정(2026-09-20, 받은편지함의 항공사 메일 35통 · 2026-07-09~09-18 · 읽기 전용 PEEK)** — 다시 재지 않아도 되게 남긴다.
    ```
    종료일   판매·행사 기간의 끝을 텍스트에서 뽑을 수 있음  0/35   (탑승기간만 1 · 무슨 기간인지 모름 2 · 기간 표기 없음 32)
             느슨한 상한: 날짜 비슷한 토큰이 하나라도 8/35 · 「기간」이라는 말 3/35 · 날짜~날짜 범위 2/35
    목적지   DEST 도시명 1개 이상  11/35  (1~3개 10 · 4개 이상 나열형 1)
    둘 다    0/35  (느슨하게 「아무 기간 + 목적지」로 잡아도 0)
    형태     본문 텍스트 중앙 1,491자 · **500자 미만(이미지형) 16/35** · 12,000자 초과(LLM 입력에서 잘림) 8/35
    도메인   airbusan 12통 **전부 이미지형** · koreanair 5 · twayair 5 · jejuair 3 · flyasiana 3 · 나머지 7
    LLM 추출(mail_deals)과 비교  행 단위: 종료일 1/25 · 목적지 8/25 · 둘 다 0  |  메일 단위: 종료일 1/17 · 목적지 6/17 · 둘 다 0
    ```
    정규식과 LLM이 **같은 결론**이다 — 종료일은 메일 **텍스트에 없다**(이미지 속이거나 랜딩 페이지에 있다). 표본이 아니라 전수인데도 0이다.
  - ✅ **기획 결정(`DECISIONS.md` 2026-09-20 (5), 기획 `3dd6ed2`) — BB38은 당분간 「쌓기만」이다. 백엔드가 할 일 없음.**
    ① 메일 프로모션을 v1으로 **발행하지 않는다**(모수 0) ② 수집은 계속 ③ 「수신 후 N일」 기본 유통기한은 **기각** — 모르는 것을
    아는 척하는 것이라서(끝난 프로모션이 조용히 남는다) ④ 이미지 파싱은 근거 없어 보류, **항공사 공식 랜딩 페이지 읽기**는
    「사용자가 비용을 들일 뜻이 있을 때 챕터로」. **지금 착수하지 말 것** — 새 파싱·새 필드 작업 없음.
  - 🔴 **같이 드러난 것: 메일 본문은 어디에도 쌓이지 않는다.** 크론의 `emails_raw.db`는 러너와 함께 매일 사라지고, 로컬 사본은 7월의 12통뿐이다.
    쌓이는 건 메타데이터(`emails`)와 LLM 출력(`mail_deals`)이다. **원문의 유일한 보관처는 Gmail 받은편지함**이다 — 「수집은 계속 쌓는다」가
    성립하려면 메일함을 비우지 않아야 한다. 파싱 방식을 바꿔 과거분을 다시 돌리려면 `mail_ingest --all`로 메일함에서 다시 받는다.
  - 🔴 **정정(같은 날)**: 처음에 「마지막 2026-09-09」라고 적었다 — **틀렸다.** `received_at`이 RFC 2822 문자열
    (`"Wed, 9 Sep 2026 …"`)인데 SQL `MAX()`를 걸어 **사전순 최대**를 읽었다. 날짜로 파싱하면 마지막은 09-17이다.
    기획이 「안 온 건지, 조용히 0건인 건지」를 되물어서 드러났다. **파이프라인은 살아 있다**:
    09-09 이후 수신 4통(09-14·15·17·18) · 제목 사전필터 통과 3 · 그중 딜을 낳은 메일 2(09-14·09-17), 09-18 한 통은 통과했으나 0건.
    전체로는 수신 35 · 사전필터 통과 23 · 딜을 낳은 메일 17. 크론의 메일 3스텝은 09-18·19 모두 success.
    결론(소비자 없음)은 그대로지만 **「죽어 있다」가 아니라 「돌고 있는데 아무도 안 읽는다」**다.
    교훈: 이 테이블의 `received_at`은 **정렬·비교에 못 쓴다.** 날짜로 쓰려면 `email.utils.parsedate_to_datetime`.

- ~~**BB39. 비항공사 발신이 공개 DB로 들어갈 경로가 있다.**~~ → **해결(2026-09-20, BE15 T1)** — 스팸 폭주 대책과 한 묶음이다.
  사용자 질문(「스팸이 엄청 오면?」)에서 넓어졌다. 구독 주소가 공개라 폭주 시 ① 한국 스팸은 법적으로 `(광고)`를 달아
  제목 사전필터를 **오히려 통과**해 LLM 과금 ② 월 한도에 닿으면 진짜 항공사 파싱이 조용히 멈춤 ③ 스팸 발신자·제목이 공개 DB에 커밋
  ④ 항공사 사칭 메일이 `mail_deals`에 공격자 URL을 심음.
  - **차단 목록 → 허용 목록**(`mail_guard.ALLOW_DOMAINS`, 등록 도메인 10개·하위 도메인 포함). IMAP `FROM` 검색으로 **서버가 먼저 거른다** —
    허용 밖 메일은 내려받지도 않는다. 주소는 `parseaddr`로 읽는다(표시 이름에 도메인을 적는 속임수 차단).
  - **Gmail 인증 확인**: 첫 `Authentication-Results`(= `mx.google.com`)의 `dmarc=pass` + `header.from` == From 도메인.
    실측(읽기 전용·PEEK, 항공사 메일 35통 전부): 헤더 통당 1개 · **35/35 `dmarc=pass`**. **DKIM 기준이면 이스타항공 1통을 버렸다**(SPF 정렬로만 통과).
  - **상한 20통/실행을 수집에 건다**(파싱이 아니라). 읽는 순간 소비되고 본문 DB는 러너와 함께 사라지므로(BB33) 파싱에서 자르면 영영 잃는다.
    넘친 메일은 안 읽은 채 남아 다음 실행이 가져간다. 인증 실패·상한 초과는 **데이터 커밋 뒤 exit 1** → 상태 점검이 빨간불.
  - 전후 대조(읽기 전용, 실제 메일함 42통): 새 판정 통과 35 · 거부 0 · 허용 밖 7통은 전부 `google.com`·`gmail.com` = **옛 코드가 거르던 집합과 같다.**
  - `tests/test_mail_guard.py` 20건. 공개 DB의 과거 발신자 중 걸러지는 것이 0인지 **여집합**으로 센다. 탐침 3종: 부분 문자열 방식 → 2건 실패 /
    DKIM 기준 → 3건 실패 / 아무 헤더나 인정 → 1건 실패. (탐침 2는 처음에 셸이 백슬래시를 반으로 줄여 **안 걸린 채 통과**했다 — 「통과했다」가 아니라
    「변이가 실제로 들어갔나」부터 봐야 한다. `chr(92)`로 다시 걸어 확인.)
  - ⚠️ 허용 목록의 조용한 실패는 **새로 구독한 항공사를 목록에 안 넣는 것**이다. 실행마다 「허용 밖 N통」을 찍는다(실패로 만들지는 않는다).
  - 남은 것(범위 밖): 구독 스캔이 INBOX 전체를 돈다 · 가짜 구독(From 위조, double opt-in 없음) → BE6·기획 먼저. Gmail 필터/`+주소` 분리는 사용자 몫.
  (원문) (PII 잠재 — 지금은 0건)
  `mail_ingest.SKIP_SENDERS`가 `google.com`·`gmail.com`뿐이고, 구독 메일은 제목에 `구독신청`/`구독취소`가
  **정확히** 있어야 걸러진다. naver 주소로 "구독 신청"(띄어쓰기)처럼 보내면 발신 주소·제목이
  공개 `prices.db`의 `emails` 테이블에 커밋된다.
  - 실측: `emails` 35행의 발신 도메인은 **전부 항공사**(11종)다. 아직 안 터졌다.
  - 고칠 방향: 차단 목록이 아니라 **허용 목록**(항공사 도메인)으로 뒤집는다. `emails` 테이블도 읽는 곳이 없다(BB38과 같이 볼 것).

- ~~**BB40. 죽은 코드와 낡은 주석.**~~ → **해결(2026-09-20, BE13 T3)** — 단 둘은 남겼다.
  지운 것: `timeutil.parse_kst_stamp` · `labels.fmt_date/fmt_month/WEEKDAY/SQL_WEEKDAY` · `dests.meta/origin_name/HAUL_NAME` ·
  `daily_min(direct_only=)`(덕분에 `route_stats`가 `detect_deals`를 import하지 않는다). 낡은 서술: `discover_data` 머리 docstring
  (필드 사본을 없애고 스키마를 가리킨다)·`build_index()` 주석 · `publish`의 「4종」·`route_page()` · `collect.yml` 이전 중 주석 ·
  `API_URL` 기본값 `galmal.kr` → `api.galmal.kr`(두 곳). 전후 대조: 실 DB에서 36노선 × 통계 함수 5개 = **180/180 동일**.
  🔴 **내 오판 하나**: `route_stats` 기본값(3건·10개·8개)을 「죽었다」고 적었는데 **살아 있다** —
  `test_publish.py::test_front_rules_reproduce_every_number`가 「발행값에 이 임계를 걸면 옛 화면 숫자가 나오는가」를
  이 기본값으로 대조한다. 호출자를 `collector/`에서만 grep하고 `tests/`를 안 봤다. **지우지 않고 설명만 고쳤다.**
  남긴 것: `affiliates.booking_link`의 「(하위호환)」 표기 — `affiliates.py`가 BE13 스코프 밖이다. 죽지는 않았다(`send_alerts`가 쓴다).
  (원문) (이전이 끝나며 남은 것 — 전부 grep으로 호출자 0 확인)
  ```
  timeutil.parse_kst_stamp                      `updated` 문자열이 사라져 쓸 곳이 없다
  labels.fmt_date · fmt_month · WEEKDAY · SQL_WEEKDAY   화면용 — 화면은 프론트로 갔다(BB31도 같이 소멸)
  dests.meta · origin_name · HAUL_NAME
  route_stats.daily_min(direct_only=)           호출자가 안 넘긴다
  route_stats 기본값 min_samples=3·limit=10·limit=8   "이전이 끝날 때까지만" — 끝났고 publish 가 전부 끈다
  affiliates.booking_link "(하위호환)"           send_alerts 만 쓴다 — 죽지는 않았다, 표기만 낡음
  ```
  낡은 서술: `discover_data.py:1-18`(옛 스키마 `updated`, `route`·`links` 누락) · `:279`(`build_index()`) ·
  `publish.py:195`("v1 4종" — 5종이다) · `publish.py:28-29`·`route_stats.py:58-60`(`route_page()`) ·
  `collect.yml:66-69,119-124,234-235`(이전 중 주석) · `collect.yml:205,236-237` `API_URL` 기본값이
  `https://galmal.kr`(지금 API는 `api.galmal.kr` — 변수가 지워지면 **엉뚱한 곳을 점검하고 404로 실패**한다. 조용하진 않다).
  → **BE13 T3.** 지울 때 `tests/test_publish.py:423`의 P7 방어(「`fmt_month`를 부르면 걸린다」)가 같이 의미를 잃는지 본다.

- **BB41. 소소한 것 — 한 줄씩.** (2026-09-20 BE14에서 넷 해결 — ✅ 표시. 나머지는 열려 있다)
  - `.env` 파서가 5벌이다(`fetch_prices`·`fetch_breadth`·`parse_mail`의 `load_*`, `mail_ingest.load_env`, `affiliates._env`).
    `affiliates._env`는 **호출마다 파일을 다시 읽는다**(딜 137건 × 링크당 여러 번). 느리진 않다, 갈릴 자리가 많을 뿐.
  - 중앙값 구현이 3벌이다 — `discover_data._median`(짝수면 평균) · `detect_deals`(`statistics.median`) ·
    `route_stats.route_summary`(`prices[n//2]` = **상위 중앙값**). 통일하면 `routes/*.summary.median` 값이
    움직일 수 있어 **계약 의미 영향**을 먼저 본다.
  - ✅(BE14 T4) `parse_mail.py`가 `pydantic`을 import하는데 `requirements.txt`엔 `anthropic`뿐이다(전이 의존이라 지금은 깔린다).
  - ✅(BE14 T4, `cancel-in-progress: false`) `collect.yml`에 `concurrency`가 없다 — 수동 실행과 예약 실행이 겹칠 수 있다(09-01·09-16·09-19에 하루 2~3회 실행).
  - ✅(BE14 T2, `_drop_orphan_routes`) `publish()`가 빠진 노선의 `routes/{code}.json`을 지우지 않는다. 지금은 파일 36 == `config.ROUTES` 36이라 해당 없음.
    노선을 빼는 날 고아 파일이 옛 `generated`로 영원히 서빙된다(index엔 없어 스냅숏 규칙은 통과).
  - `publish.py:2` 의 `CONTRACT.md §v1` 참조 — 기획이 그 문서를 다시 쓰면서 **그 절 제목이 사라졌다**(2026-09-21 기획 통지).
    이제 문서 전체가 v1 이라 「`CONTRACT.md`」로 읽으면 된다. 그 파일을 다음에 만질 때 고친다(이것 때문에 챕터를 열지 않는다).
  - 로컬 Python 3.11.9 vs CI 3.12. `str | None`(3.10+)만 쓰여 지금은 무해.
  - `LICENSE`가 없다(공개 저장소). 사용자 결정.
  - ✅(BE14 T3, 없으면 실패) `CommittedArtifactTest` 둘(`test_contract.py:460`·`test_publish.py:267`)은 `docs/v1`이 없으면 **skip**이다.
    v1이 항상 커밋돼 있는 지금은 fail이 맞다 — 통째로 지워져도 CI가 초록이다.
  - 기계가 읽는 계약 정본은 `deals[]` 원소뿐이다. `meta`·`routes/*`의 모양은 `test_publish.py`와
    `CONTRACT.md` 산문에만 있다. 기획 소관 — 필요해지면 그쪽에 제안한다.

---

## 8. 다른 세션에서 받은 요청 / 보낼 것

### 프론트에서 받은 것 (출처: `BACKLOG.md`)
- **B8 → `deals[].found_at` 필드 추가 요청.** 프론트가 "N시간 전 발견" 배지와 신선순 정렬에 쓴다.
  필드 추가라 기존 프론트는 안 깨진다. → **BE2에서 처리.** `CONTRACT.md` 먼저 갱신.
- **B9 → 30일 가격 추이 필드 요청(조건부).** 진짜 히스토리가 쌓인 뒤에 재논의.
  현재 `offers`는 26개 노선만 깊게 수집하므로 **발견 딜의 82%는 히스토리가 없다**
  (2026-08-22 실측: 103건 중 19건만 보유). 이 숫자를 근거로 답해줄 것.

### 기획에서 받은 것 (대기 중, 급하지 않음)
- **대구·제주 노선 추가 재판단** — 2026-09-01 실측상 하루 1~6건뿐이라 보류했다.
  매일 수집하므로 한 달 뒤엔 대구 제주가 180건쯤 된다. **도장 임계값 재측정과
  같은 시점에 함께 재면 된다.** 재는 방법은 후보 노선을 `fetch_route`로 직접
  물어 하루 응답 건수를 보는 것이다(10건 이상이면 채택). 오키나와(8건)도 같이 본다.
- ~~**`d`를 도시 코드로 바꾸기**~~ → **해결(2026-09-01, BE3 T4)**. 계약이 `1b57e6a`에
  이미 올라와 있었는데 통지가 없어 잠시 대기했다. 프론트는 `d` 참조 0건이라 코드 변경 0.
  (원문) 
  `d = NRT`는 "이 딜은 나리타행"이라고 주장하는 셈인데 광역 수집은 도시 단위라 모른다.
  - **지금이 가장 싼 때다**: URL 딥링크가 프론트 CH4에서 아직 구현 전이라, 나중에 바꾸면
    이미 공유된 링크가 깨진다. `#SEL-NRT`("나리타?")보다 `#SEL-TYO`가 도쿄를 가리킨다.
  - `CONTRACT.md`가 이미 "`(허브, 도시)` 단위로 최저가 1건"이라 적고 있어 **키가 공항 코드인
    게 그 문장과 어긋난다.**
  - 순서: 기획이 **프론트 확인 → 계약 갱신** 후 내가 구현한다. `median`·`when` 때와 같다.
  - 영향: `d`(23건), URL 딥링크, 중복 제거 키, 검증기의 IATA 정규식(도시 코드도 3자리라
    패턴은 그대로 통과). `dests.link_code()`가 이미 있으므로 구현 자체는 작다.
- **F16 신선도 분포 실측 요청.** hero 큐레이션의 fresh 게이트를 정하려면 하루 중
  상·하한이 필요하다. 요청 시점은 **크론 직후 07:30 KST**와 **12시간 뒤 19:30 KST** 두 개.
  기획이 PH3에 도달했을 때 정식 요청할 예정이므로 **먼저 움직일 필요는 없다.**
  (분포는 하루 주기로 오르내린다 — 수집 직후 가장 신선하고 다음 수집 직전이 가장 낡다.)

- ~~**PH4 가격 히스토리 분포 실측 요청**~~ → **완료(2026-08-22)**, 결과는 아래.
  기획이 요청 범위를 축소했다(스파크라인 B9 보류, 요약값 `low`·`obs_days` 두 필드만).
  **실측 결과 잠정안 그대로면 41%에 붙어 도장 문제(F17)가 반복된다.** 기획 판단 대기 중.
  - 관측 일수: 155조합, 중앙 12일, 7일 이상 72% → **하한 7일은 아무것도 못 거른다**(딜의 94%)
  - `price == low` 비율: 하한 7일에서 **41%**, 14일로 올려도 26%
  - 원인: `deals.json`의 `price`가 이미 "최근 3~4일 최저"라 "역대 최저냐"를 묻는 게 동어반복.
    이력이 23일뿐인 것도 겹친다.
  - **대안 제시**: `low`를 "창 이전 기간 최저"로 정의하면 프론트가 `price < low`로 신기록
    판정 + 낙폭 계산까지 할 수 있다. "신기록 + 5% 이상 저렴"이면 **10%**로 적당히 희소해진다.
  - 구현은 BE2 이후. 기획이 철회할 수도 있어 **아직 아무것도 만들지 않았다.**
- **(구)PH4 요청 원문**(2026-08-22 접수, 기획이 "BE1 먼저" 명시).
  스파크라인(`B9`)과 `{N}일 중 최저` 문구를 어디에 둘지 정하는 데 쓴다. 필요한 셋:
  1. `broad_offers` 기준 **(허브, 목적지) 조합당 관측 일수 분포** — `{N}일 중 최저`의
     하한선(잠정 7일)을 정하기 위함
  2. 오늘 `deals.json`의 딜 중 **관측 7일 이상인 비율** — 문구가 몇 %에 붙는지
  3. `offers`로 그릴 수 있는 딜 비율 — `B9`를 26노선 페이지에만 둘지 발견 홈에도 둘지
  - 주의: 예전에 전달한 "발견 딜의 82%는 히스토리 없음"은 **`offers` 기준**이다.
    `broad_offers`를 쓰면 그 제약이 없다(2026-07-29부터 목적지당 하루 1행).
    두 테이블의 성격 차이를 섞지 말 것.

### 기획/프론트에 보낼 것
- **BB13 결정 통지.** `fetched_date`를 UTC로 두기로 정하면 기획이 그 사실을
  `CONTRACT.md`에 명시하겠다고 했다. BE1에서 정한 뒤 알릴 것.

### 기획 결정 대기 중 (2026-09-02 발신)
- **제휴 수수료 고지 문구 — 결정되면 `affiliates.py`를 내가 고친다.**
  내일 크론부터 Aviasales가 붙어 **실제로 수수료가 발생**하는데, 프론트가 예약처를
  전부 동등한 버튼으로 그려서 어느 것이 제휴인지 알 수 없다. 오늘까지는 배포본에
  `marker` 문자열이 **0건**이라(확인함) 고지가 "일부"로 뭉뚱그려져 있어도 문제가
  없었지만, 내일부터 정확히 1곳에 붙는다.
  - **데이터는 이미 있다**: `links[].tag`가 나가고 있고 Aviasales 값은 `"영어·수수료"`다.
    `CONTRACT.md:73`이 "현재 프론트는 미표시(중립성 위해 숨김). 필드는 유지"로 적어뒀다.
    새 필드가 아니라 **켜는 문제**다.
  - **경계 문제**: 이 5개 `tag` 값은 **화면 문자열인데 `collector/affiliates.py`에
    하드코딩**돼 있고 `COPY.md`에는 없다. "화면 문자열 전수"를 표방하는 문서에 구멍이다.
  - **경로**: 기획이 문구 확정(`COPY.md`) → 내가 `affiliates.py` 수정 → 프론트가 렌더.
  - 법적 충분성(정보통신망법·공정위)은 내 판단 범위 밖이라 기획에 넘겼다.

---

## 9. 세션 핸드오프 양식

```
## BEn 완료
- 한 것: (태스크별 1줄 + 커밋 해시)
- 안 고치고 남긴 것: BACKEND.md §7 BBn
- 데이터 영향: (딜 건수·스키마 변화, 없으면 "없음")
- 다음 챕터 첫 태스크: (다음 세션이 바로 시작할 수 있게)
- 프론트/기획에 전달: (계약 변경·결정 요청)
```

---

## 10. 운영 정보 (BE7)

### 도메인 — `galmal.kr` (2026-09-05 전환)

```
DNS          A 레코드 4개(GitHub Pages) · TTL 600 · 등록처 가비아
             AAAA는 등록처가 지원하지 않아 안 넣었다. 선택 사항이라 무방하다.
docs/CNAME   GitHub Pages 설정이 만든다. 우리가 쓰지 않는다.
정본 상수     collector/theme.py 의 BASE_URL 하나
```

⚠️ **`docs/CNAME`이 사라지면 사이트가 죽는 게 아니라 404가 된다.** `build_site.py`는
`docs/`를 지우지 않고 덮어쓰기만 하므로 크론이 날리지 않는다. `tests/test_site_url.py`가
파일 존재와 `BASE_URL` 일치를 검사한다.

옛 주소(`ryu-tomi.github.io/promo-ticket-site`)는 GitHub이 **페이지 단위로 301**을
걸어 준다 — SEO 자산이 새 도메인으로 넘어간다.

### 검색엔진 등록 — 사용자만 할 수 있다

| | |
|---|---|
| **구글** | ✅ **도메인 속성**(`galmal.kr`) · **DNS TXT**로 확인. 서브도메인·http/https를 전부 덮고 **코드 변경이 없다.** 값은 가비아 DNS에 있다 — 여기 옮겨 적지 않는다(갈라진다) |
| **네이버** | ✅ 서치어드바이저 · **HTML 메타 태그**. 파일 업로드도 되지만 태그를 골랐다 — `docs/`는 매일 통째로 재생성되는 자리라 **손으로 넣은 파일은 조용히 사라질 수 있다**(CNAME과 같은 위험). 값은 `theme.SITE_VERIFICATION` |

⚠️ **네이버는 루트(`https://galmal.kr/`)를 확인한다.** 홈(`docs/index.html`)은 `theme.page()`가
아니라 `discover_home.py`(프론트 구역)가 만들므로, 소유확인 태그가 노선 페이지에만 들어가면
확인이 실패한다. 2026-09-05에 실제로 그랬고 프론트가 `verification_meta()`를 호출하도록
배선해 풀었다. **`theme.py`에 태그를 추가할 때 홈에도 나가는지 확인할 것.**

값이 HTML 태그 방식이면 `theme.SITE_VERIFICATION`에 한 줄 넣으면 끝난다(BE7 T2에서
자리를 만들어 뒀다). **비밀이 아니다** — 서비스가 HTML에서 읽어가는 공개값이라 커밋한다.

### 운영 지표 — **GA는 안 넣는다**

쿠키 동의 배너를 부르고, 그건 발견 홈을 깨끗하게 두려는 결정과 충돌한다(기획 판단).
쿠키 없이 볼 수 있는 **두 숫자**만 쓴다.

| 지표 | 어디서 |
|---|---|
| 검색 유입(노출·클릭·질의) | 구글 서치콘솔 · 네이버 서치어드바이저 |
| 예약처 클릭·전환 | Travelpayouts 대시보드 |

**클릭 수가 곧 수익 신호다.** 마커가 붙은 2026-09-03부터 유효하다 — 그전 숫자는
경로가 없어서 구조적으로 0이었다(BB24 절 참조).

### 비용

```
도메인   galmal.kr        연 23,100원  (2026-09-05 구매)
메일 파싱 Claude API       연 약 2,600원 (월 지출 한도 설정됨)
호스팅   GitHub Pages     0원
────────────────────────────────────
                          연 약 25,700원
```

---

## 11~15. 저장소 분리 기록 → `MIGRATION.md`

M0~M6(2026-09-08 ~ 09-19)의 진행·실측·시행착오는 **[`MIGRATION.md`](MIGRATION.md)** 로 옮겼다(BE16 T2).
절 번호는 그대로라 `MIGRATION.md §11.3`처럼 찾으면 된다. 끝난 일이므로 평소에는 읽을 필요가 없다.

거기 남은 것 중 아직 쓰이는 값: **`MIGRATION.md` §11.2**(BE8 후보 85개 — 2026-09-20에 재측정했으니 이제 참고용) ·
**`MIGRATION.md` §11.3**(재개 전제 — BE8 1차에서 해소) · **`MIGRATION.md` §11.12**(서버 이식 표면) · **`MIGRATION.md` §13**(M3 대조 방법과 UTC 자정 함정).

---

## 16. 챕터 기록 (BE9~)

**BE9 먼저 → BE10.** 이유: 내일 크론 뒤 push부터 CI가 헛 빨간불(BB34)이고, BE10은 push가 많다.
헛 빨간불 속에서 옮기기 작업을 하면 진짜 실패를 못 가려낸다.

### BE9 — 테스트 신뢰성 (BB34 + BB35)
| | 내용 | DoD |
|---|---|---|
| T1 | 산출물 대조 테스트의 시계를 `meta.generated`에 고정 | 시계를 +1일 옮겨 돌려도 `^OK` |
| T2 | `_envelope`·payload 함수의 `generated` 기본값 제거 → 필수 인자 | 빼먹으면 `TypeError` · `^OK` |
| T3 | `generated` 규칙 테스트(CONTRACT §공통 규칙) — **보존일 케이스 필수** + `05d0de9` 반례(deals 42초 이름) | 반례에서 실패함을 한 번 보이고 `^OK` |

파일: `tests/test_publish.py` `tests/test_contract.py` `collector/publish.py` `BACKEND.md`

### BE10 — 계약 목록을 코드 옆 한 곳으로 (R8 C안, 기획 DECISIONS 2026-09-17)
| | 내용 | DoD |
|---|---|---|
| T1 | `contract/v1/deal.schema.json` — 필드·타입·nullable·description | **옛 파서가 CONTRACT.md에서 뽑은 `{필드: nullable}` == 새 로더 결과**를 한 번 보이고 옛 파서 삭제 |
| T2 | `contract/v1/vocab.json` — tags(상위·하위) · when · region · haul · tier | 옛 파싱 결과 == 새 파일 · **테스트 안 손 사본(`REGIONS`·`HAULS`·`TIERS`·`WHEN_*`)도 제거** |
| T3 | `test_dests`의 TAGS.md 대조 제거 — `DEST`가 정본, 태그 ⊆ vocab 검사로 | `^OK` |
| T4 | `test_labels`의 COPY.md 대조 제거 — `REGION_NAME`이 정본, vocab region 전부 이름 있음 | `^OK` |
| T5 | `CONTRACT.md`·`COPY.md`·`TAGS.md` 사본 삭제 | 문서를 여는 코드 0 · `^OK` |

파일: `contract/v1/*` `tests/test_contract.py` `tests/test_dests.py` `tests/test_labels.py`
`tests/test_affiliates.py`(주석만) `CONTRACT.md`·`COPY.md`·`TAGS.md`(삭제) `BACKEND.md`

- `jsonschema` 라이브러리 안 씀(의존성 0) — JSON Schema **모양**만, 자체 로더
- `DEST` 구조는 안 바꾼다(기획 `build_tags.py`가 기댄다)
- **새 규칙**: 기획 결정 없이 `contract/`를 바꾸지 않는다. 커밋 메시지에 그 결정을 인용한다
- 공존 구간(T1~T4)에 계약 변경이 생기면 기획이 양쪽 동시 반영을 요청한다
- 범위 밖(기획이 BE10 뒤 정리): `vocab.json`을 v1으로 발행해 프론트 `site/home.py` 필터 칩 사본 제거

### BE9 완료 (2026-09-17)

```
T1 115a44d  산출물 대조 3테스트 시계를 meta.generated 에 고정
            고정 전 +1일 55건 실패 → 고정 후 +1·+30·+365·-3일 OK
            고정 시각을 하루 틀리게 하면 55건 실패 (헛것 아님)
T2 45a4e05  generated 필수 인자 · naive 면 ValueError
            기본값 있던 publish.py 로 되돌리면 새 테스트 실패 확인
T3 0bdf498  스냅숏 규칙 — 평범한 날 · 보존일 · 05d0de9 반례 · 커밋된 산출물
            탐침: 노선 1초 어긋남 / 보존일 deals 에 오늘 도장 → 둘 다 잡음
```

- ⚠️ 시프트 도구로 +30일에 `GeneratedOutputTest` 2건이 실패하는데 **고정 전에도 같다**.
  테스트가 `date.today()`로 데이터를 넣고 도구는 `timeutil`만 옮기는 불일치라 실제 시간 의존이 아니다. 안 고쳤다.

### BE10 완료 (2026-09-19) — 계약 목록은 `contract/v1/` 한 곳

```
T1 9964efd  deal.schema.json (필드 23 + links 4, 타입·nullable·설명·예시)
            옛 파서 == 새 로더: 23 필드 · nullable [low median ret route seen], 순서까지 같음
T2 fa6e347  vocab.json (tags top6/sub18 · when 5+3 · region 9 · haul 3 · tier 2 · hub 4)
            옛 파싱·손 사본 8종 전부 같음 · 탐침(야시장·jp 제거) → 잡음
T3 42ca12c  test_dests: TAGS.md 대조 삭제, REGIONS·HAULS 를 vocab 에서
T4 60a59f8  test_labels: COPY.md 대조 삭제, vocab region 전체 ↔ REGION_NAME 양방향
T5 5c8315b  CONTRACT.md·COPY.md·TAGS.md 사본 삭제 — 여는 코드 0
```

- **새 규칙**: 기획 결정 없이 `contract/`를 바꾸지 않는다. 커밋 메시지에 그 결정을 인용한다.
- `$id`는 `urn:` — Pages는 `docs/`만 서빙해서 URL로 적으면 열리지 않는 주소가 된다.
- ⚠️ **잃은 것: 태그 순서 대조**(T3). 카드가 배열 순서대로 보이는데, 이제 `DEST`가 정본이라
  `sorted()` 같은 변경은 코드 리뷰만 막는다.
- 남은 주석 속 `CONTRACT.md` 언급은 출처 표기 — 「왜」는 기획 저장소에 있다.

### ✅ M6 T4 ⑤ 완료 (2026-09-19) — 새 폴더 `.env`로 재빌드 137 == 137, 되돌림·커밋 없음

폴더 재배치 뒤 **새 `galmal-backend/`에서** 한 번 재빌드하고 커밋 전에 확인한다:
`python collector/publish.py` → 딜 수 == 제휴 링크 수(CLAUDE.md의 한 줄). 다르면 `.env`가 안 넘어온 것이다.
- `.env`는 **사용자가** 옮긴다(시크릿 — 세션은 복사하지 않는다). 옮기기 전 사본은 옛 폴더에만 있었다.
- 새 클론 `.gitignore:3`이 `.env`를 무시함(기획 check-ignore 확인).

### BE11 완료 (2026-09-19) — `/v1/vocab.json` 참조 데이터 발행 (CONTRACT §5)

```
T1 86a2e31  vocab_payload — contract/v1/vocab.json 그대로 + region_name, $comment 중첩까지 제거
T2 dbe4cab  발행물 == 정본 - $comment (기대값 손으로 안 씀, 제거도 다른 방법으로)
            탐침: 정본에 태그 추가 → 따라옴 / 최상위만 제거 → 실패 / top 순서 뒤집기 → 실패
T4 run 35428313999  수동 실행(skip_side_effects=true) → api.galmal.kr/v1/vocab.json 200
            generated == meta.generated (16:04:43+09:00) · 라이브 == 커밋된 파일
T3 7a4983e  스냅숏 규칙에 vocab == G, **없으면 위반**. 탐침: 1초 어긋남 / 발행 누락 → 잡음
```

- **T4를 T3보다 먼저 했다.** T3는 커밋된 산출물에 vocab.json을 요구해서, 발행 전에 올리면 CI가 빨갛다.
  「있으면 본다」로 약하게 만드는 대신 순서를 바꿨다.
- 수동 실행도 프론트에 dispatch를 보낸다 — 프론트는 지금 39개만 받으므로 vocab은 무시된다.

- 🔴 **프론트 빌드가 `vocab.json`에 의존한다**(프론트 `d41996e`, 2026-09-19). 404거나 `generated != meta`면
  그날 배포가 멈추고 사이트는 직전본 — 신호는 프론트 잡 실패 메일(우리 상태 점검은 「어제까지 허용」이라 초록일 수 있다).
- **새 상위 태그·새 `haul`은 프론트 빌드를 멈춘다**(TAG_GRAD·HAUL2STAGE 포괄 검사, 의도된 동작).
  `contract/vocab.json`에서 그 둘을 바꾸는 커밋은 **프론트가 매핑을 먼저 넣은 뒤**에 올린다. 하위 태그 추가는 영향 없음.

### BE12 완료 (2026-09-19) — 중간 점검 · README 개편 (문서만, 코드 0줄)

```
T1 cdc607e  점검 발견을 §7 「BE12 — 중간 점검에서 나온 것」에 BB36~BB41 로 기록, 낡은 §4·§5 정정
T2 (이 커밋) README.md 전면 개편 — 41줄짜리 옛 promo-ticket-site 설명을 세 저장소 공통 뼈대로
```

- **README 뼈대는 세 저장소 공통이다**(프론트 제안 → 기획 동의, 2026-09-19). 절 순서:
  머리 → 한눈에 → 세 저장소 → 어떻게 도나 → 기술 스택 → 폴더 구조 → 로컬 실행·테스트 → 배포·운영 →
  설계에서 고른 것 → 문서 → 법적 고지. 백엔드 전용 절은 「API 써보기」·「계약 정본」·`.env` 키 표 셋.
- **복사되는 건 셋뿐**(소개 문장 · 저장소 3행 표 · 흐름도)이고 각 블록 위에 `<!-- 발췌: … -->` 주석이 있다.
  정본은 `galmal-plan`이다 — **고칠 땐 거기부터.** README에서도 「특가」를 쓰지 않는다(기획 F19).
- **README 말투는 「~합니다/~입니다」체다**(사용자 지시 2026-09-20 — 처음엔 「~다」체로 썼다). 세 저장소 공통.
  표·목록의 명사형 토막과 기획 정본에서 옮긴 발췌(3행 표·소개 문장)는 그대로 둔다. 이 문서(`BACKEND.md`)·코드 주석은 「~다」체 그대로.
- README에 **건수·테스트 수를 적지 않았다**(매일 바뀐다). 크론 시각도 숫자 대신 `collect.yml`을 가리킨다 —
  BB36으로 곧 옮길 값이다.
- 적은 주장은 실물과 대조했다: `Cache-Control: max-age=600`(curl -I) · 배너 URL 200 · 예시 JSON은 당일 발행물 ·
  P7 방어는 import 검사가 **아니라** 발행물 문자열 정규식(`DISPLAY_LEAK`)이다 — 처음에 틀리게 적었다가 고쳤다.
- ~~⚠️ 소개 문장은 `galmal-plan` 어디에도 **글자 그대로는 없다**~~ → 기획이 `PRODUCT.md` §한 줄 소개에 정본을 뒀다(같은 날).
  README 발췌 주석을 그 절로 바꿨다.

### BE13 완료 (2026-09-20) — BE12 점검에서 나온 것 중 셋을 고쳤다

```
T1 2c3edba  cron "10 22" → "10 20"(05:10 KST) + 상태 점검에 「예약 실행이 23·00시대 UTC 면 실패」   BB36
            탐침: 22·01·20시 통과 / 23·00시 실패 / 00시 수동 실행 통과. GitHub 가 워크플로를 active 로 읽음
T2 0444ea4  해지는 제목에서만 노선을 읽는다 · ALL 구독자의 노선 해지는 "!코드" 제외 · wants()          BB37
            tests/test_subscriptions.py 15건 신설. 탐침: 옛 로직 → 6건 실패
T3 (이 커밋) 죽은 코드 8개 · 낡은 서술 · API_URL 기본값. 전후 대조 180/180 동일                        BB40
```

- 테스트 183 → 198건 `OK`. 계약(`contract/`)·발행물 형식·`meta.subscribe`는 **안 바뀌었다.**
- **T1은 아직 반만 검증됐다.** 가드 논리와 YAML 유효성은 봤지만 **새 슬롯에서 실제로 몇 시에 도는지는 첫 실행을 봐야 안다.**
- 검증용 재빌드(`publish.py`)는 하지 않았다 — 발행 경로의 코드 변경이 `daily_min`의 죽은 분기 제거뿐이고 그건 실 DB 대조로 봤다.
- 통지: 크론 이동을 기획·프론트에 알렸고 기획이 `PROJECT.md`를 맞췄다(실제 시작 숫자는 비워 둠). T2·T3는 남에게 닿는 게 없어 안 알렸다.

### BE14 완료 (2026-09-20) — 점검 잔여 중 메일·계약과 무관한 것

```
T1 428f945  date.today() 3곳 → timeutil (기계용 UTC 2 · 사람용 KST 1). KNOWN_DEBT 목록 삭제, 새 위반 방지는 유지   BB25
T2 b1a9b3a  publish 가 index 에 없는 routes/{code}.json 을 지운다. OrphanRouteTest 4건                         BB41
T3 53ff334  커밋된 발행물이 없으면 skip → 실패 (3곳)                                                          BB41
T4 (이 커밋) collect.yml concurrency(group: collect, cancel-in-progress: false) · requirements 에 pydantic>=2   BB41
```

- 테스트 198 → 201건 `OK`(빚 목록 테스트 -1, 고아 노선 +4). 탐침은 태스크마다 했다: 위반 심기 / 지우는 호출 끄기 /
  `docs/v1` 치우기 → 각각 실패 확인 후 원복. T4의 YAML 은 PyYAML(scratchpad 설치)로 구조를 확인했다.
- 계약·발행물 형식은 **안 바뀌었다.** T2는 `CONTRACT.md:262`의 괄호 설명(「옛 파일이 남아 있을 수 있다 — `_write`는
  지우지 않는다」)을 낡게 만든다. 규칙 자체(index 에 실린 코드만 받는다)는 여전히 옳다 → 기획에 알렸다.
- ⚠️ T2 작업 중 원복에 `git checkout -p`(대화형)를 끼워 넣는 실수를 했다 — 이 환경은 대화형을 못 돌려 아무 일도
  안 했고, 결과는 `PROBE` 0건·호출 복원·`^OK`로 따로 확인했다. 원복은 `sed` 역치환이나 `git checkout -- 파일`로만 한다.

### BE15 완료 (2026-09-20) — 메일 방어 (사용자 질문 「스팸이 엄청 오면?」에서 시작)

```
T1 5f79d3f  mail_guard.py 신설 — 허용 목록(서버 FROM 검색) + Gmail 인증(dmarc=pass·header.from 일치) + 상한 20통/실행   BB39
            실측 35/35 dmarc=pass(DKIM 기준이면 이스타항공 1통 손실). 새 판정이 받는 집합 == 옛 코드가 받던 집합(35통)
T2 (이 커밋) parse_mail 이 API 실패를 세고 exit 1. 연결 오류로 루프가 통째로 죽던 것도 막음(anthropic.APIError 로 넓힘)
            실물: 가짜 클라이언트·임시 DB 로 main 배선 확인 — 4통 중 1통 연결 오류 → 3통 처리, 실패분 processed=0, exit 1
```

- 테스트 201 → 221건 `OK`. 계약·발행물 형식 **변화 없음.** 경보는 둘 다 「데이터 커밋 뒤 exit 1」이라 수집분을 버리지 않는다.
- **운영에서 달라지는 것**: 메일 스텝이 빨개질 수 있는 경우가 셋 생겼다 — 허용 도메인 메일의 인증 실패 / 상한 초과 / 파싱 API 오류.
  인증 실패 메일은 안 읽은 채 남으므로 **사람이 메일함에서 읽음 처리할 때까지 매일 알린다**(의도).
- ⚠️ 검증 한계: 수집기(`mail_ingest.main`)를 **실제로 돌려 보지는 않았다** — 돌리면 메일을 소비해 그날 크론이 못 본다.
  같은 함수(`_candidates`·`verdict`)를 읽기 전용으로 실제 메일함에 걸어 본 것까지다. 받아들인 뒤의 저장 경로는 옛 코드 그대로다.
  첫 예약 실행의 「프로모션 메일 수집」 로그(`안 읽은 메일 N통 · 허용 도메인 후보 …`)가 실물 확인이다.
- 사용자 몫으로 남긴 더 단단한 방어: Gmail 필터(`from:(항공사 도메인) → 라벨`) · 뉴스레터를 `주소+태그@gmail.com`으로 재구독해 공개 주소와 분리.

### BE8 T0 — 노선 확대 후보 측정 (2026-09-20, **커밋 없는 측정** · 코드 0줄 · 노선은 아직 하나도 안 넣었다)

`MIGRATION.md` §11.2(2026-09-08, 후보 85개)를 오늘 데이터로 다시 쟀다. 전제 셋(`MIGRATION.md` §11.3) 중 **둘이 풀렸고 하나(서치콘솔 질의)는 사용자 몫**이다.

```
후보 풀   광역 30수집일(08-18~09-19)에 잡힌 (공항,목적지) 280 · 페이지 없는 조합 244
          20일 이상 관측 52 (25일↑ 10 · 15일↑ 113 · 10일↑ 157)   출발지: GMP 26 · ICN 17 · PUS 6 · TAE 2 · CJU 1
v3 깊이   52개 전부 호출(fetch_route, DB 에 안 씀): 10건 이상 28 · 1~9건 24 · 0건 0 · 호출 실패 0
```

**10건 이상 28개를 둘로 가른다 — 행 수만 보면 안 된다.**

```
직항이 실제로 잡히는 것 (13)                     v3건/직항
  ICN-PQC 푸꾸옥 74/72 · ICN-CJU 제주 70/18 · GMP-SHA 상하이 63/43 · GMP-TPE 타이베이 56/34 · PUS-BKK 방콕 53/14
  ICN-SYX 싼야 28/13 · ICN-HNL 호놀룰루 25/12 · GMP-NGO 나고야 25/23 · ICN-CNX 치앙마이 23/12
  ICN-HIJ 히로시마 20/20 · ICN-TAO 칭다오 20/18 · TAE-CJU 제주 19/19 · PUS-HAN 하노이 11/7
직항 0건 (15)
  GMP-HKT 83 · GMP-IST 79 · GMP-SYD 77 · ICN-HKT 70 · GMP-HKG 69 · GMP-PQC 66 · GMP-MNL 52 · ICN-MLE 31
  GMP-SGN 27 · GMP-HNL 25 · GMP-MLE 24 · GMP-JKT 13 · ICN-SZX 11 · GMP-SZX 11 · GMP-FRA 11
```

- 🔴 **`GMP-*` 국제선 후보는 대부분 가짜 노선이다.** 김포엔 푸켓·시드니·이스탄불 직항이 없다 — v3가 김포 출발 **경유편**을 돌려줄 뿐이다.
  광역 피드에서는 인천+김포가 `SEL`로 합쳐져 무해하지만, **노선 페이지는 실제 공항을 제목에 단다** — 「김포 → 푸켓」 페이지는 아무도 안 타는 노선의 통계다.
- ⚠️ **「직항 0건 = 직항 없음」이 아니다.** v3는 `sorting=price`·`limit=100` — **가장 싼 100건**이다. 현행 노선도 6개가 직항 0건이다
  (ICN-KUL·DPS·CDG·LHR·FCO·BCN — 실제로는 직항이 있고 싼 경유편에 밀렸을 뿐). 직항 수는 **김포발처럼 직항이 애초에 없는 공항**을 거를 때만 쓴다.
  `ICN-HKT`·`ICN-MLE`가 0건인 것도 같은 이유일 수 있다 — **판정 보류**.
- 현행 36노선의 직항 비율: 최소 0% · 중앙 67% · 최대 100%. **현행 중 10건 미만이 4개**다 — ICN-GUM 2 · ICN-LHR 7 · PUS-TPE 4 · ICN-RMQ 1(2026-09-19 하루치).
  「하루 10건」은 넣을 때의 기준이었고 **넣은 뒤엔 아무도 다시 안 쟀다.** 30일 창으로 쌓이니 당장 빈 페이지는 아니다 → 곁가지(안 고쳤다).
- **전제 2(호출·시간) 해소**: 크론 전체가 1분 30~40초다(최근 3회). 노선 13~28개를 더해도 호출 49~64회(제한 200/시간), 늘어나는 시간은 수십 초.
  새 슬롯의 자정까지 여유(1h40m)에 영향 없다.
- **전제 3(경계) 해소**: 프론트는 `routes/index.json`에 실린 코드대로 페이지를 만든다(프론트 `site/route.py:290`, `snapshot.py:60`).
  백엔드가 `config.ROUTES`에 넣으면 **프론트 코드 변경 없이** 페이지가 생긴다. 얇은 버킷은 프론트 `usable()`이 거른다. 계약 변경 없음.
- **전제 1(서치콘솔 질의 2주치)은 사용자 계정이라 백엔드가 못 본다.** 이 측정은 「어느 후보가 성립하나」까지다 — 「그중 무엇을 고르나」는 질의 데이터와 기획 판단이다.
  기획 권고(BB22): 한 번에 다 늘리지 말고 **효과를 재며** 늘린다.

### BE8 완료 (2026-09-20) — 노선 1차 확대 36 → 43

```
T0 5f2780a  후보 52개 v3 전수 측정 — 10건↑ 28 · 직항이 실제로 잡히는 것 13 · 김포발 가짜 노선 진단
T1 (직전 커밋) config.ROUTES 에 7개: PUS-BKK · PUS-HAN · TAE-CJU / GMP-SHA · GMP-TPE · GMP-NGO / ICN-PQC
```

- 선정은 기획 결정(`DECISIONS.md` 2026-09-20 (6))이다: 새 출발지 3 + 김포 **실직항** 3 + 표본 최다 1. **서치콘솔은 안 기다렸다** —
  서치콘솔은 이미 있는 페이지의 노출을 알려줄 뿐 없는 노선의 수요는 못 알려주고, 색인에 몇 주 걸려 지금 넣어야 나중에 잴 것이 생긴다(기획).
- `MIGRATION.md` §11.3의 재개 전제 셋: ②호출·시간(크론 1분 30~40초, 호출 36→43) ③프론트 경계(index 대로 굽는다) **해소**, ①서치콘솔은 2차 기준으로 미룸.
- 실 DB 확인(읽기 전용): 새 7개는 `route_payload=None`(수집 이력 없음) · 기존 36개 그대로 → **오늘 발행물은 안 바뀐다.** 다음 크론부터 v1 응답 40 → 47.
- 계약 변경 없음. 테스트 221건 `OK`(노선 수를 고정한 테스트는 없고, `ROUTES[0]`·`[:2]`를 쓰는 테스트가 있어 **뒤에** 붙였다).
- 기획이 받아 간 곁가지: 현행 4노선이 하루 10건 미만(ICN-GUM·LHR·PUS-TPE·ICN-RMQ) → 기획 `SPEC.md` §3 `BE8-1`(유지 하한). 지우지 않는다.
