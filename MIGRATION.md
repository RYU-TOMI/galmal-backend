# MIGRATION.md — 저장소 분리 기록 (2026-09-08 ~ 09-19)

> **끝난 일의 기록이다.** `promo-ticket-site` 하나를 `galmal-plan`·`galmal-backend`·`galmal-frontend`
> 셋으로 나눈 M0~M6 과 그 직전 상황을 담는다. 2026-09-21에 `BACKEND.md`에서 이리로 옮겼다(BE16 T2) —
> 그 문서는 **지금 일하는 방식**만 담아야 하는데 절반이 이 기록이었다.
>
> **절 번호(§11~§15)는 그대로 둔다.** 옛 참조가 여전히 읽히게 하려는 것이다 — 이제 `MIGRATION.md §11.3`처럼 읽는다.
> 여기 있는 경로·명령·숫자는 **그때의 것**이다. 지금 사실은 `BACKEND.md`와 코드가 정본이다.
>
> 왜 안 지웠나: 「왜 그렇게 했나」가 여기 있다. 같은 함정(UTC/KST·diff-0 관문·dispatch 경합)을
> 다시 밟지 않으려면 결론만이 아니라 **틀렸던 과정**이 남아야 한다.

---

## 11. 저장소 분리 직전 기록 (2026-09-08)

> 사용자가 `promo-ticket-site`를 `galmal-backend`(백엔드)·`galmal-frontend`(프론트)로 나누기로
> (처음 이름은 `galmal-api`·`galmal-web`이었고 2026-09-11 사용자가 바꿨다. 도메인 `api.galmal.kr`은 무관)
> 정했다(기획 `SPLIT.md`). **이전이 시작되면 챕터 진행이 멈춘다.** 재개할 때 기억이
> 남아 있지 않을 것이므로, 지금 손에 쥐고 있는 것을 여기 적는다.
>
> ⚠️ 이 절은 **이전이 끝나면 §6·§7로 흡수하고 지운다.** 남겨두면 그 자체가 두 번째 출처가 된다.

### 11.1 어디서 멈췄나

BE7(배포·운영)까지 완료(`f1e70b9`, 테스트 175건). **BE8은 열지 않았다** — 사용자 승인 전이다.

멈춘 자리는 사용자의 질문 하나다:

> *"구글에서 특가를 검색했을 때 우리 사이트가 뜨게 하려면 어떻게 해야 할까"*

답을 절반 했다. 결론: **머리말(`항공권 특가`)은 못 이긴다** — 새 도메인·백링크 0.
이길 수 있는 건 **롱테일 노선 질의**(`인천 타이중 항공권 최저가`)이고, 그건 노선 페이지가
이미 겨냥하는 표면이다. 그래서 **노선 페이지 확대**를 다음 챕터 후보로 제안했고,
사용자는 아직 답하지 않았다.

### 11.2 그때 잰 값 — 다시 재지 않아도 되게 남긴다

2026-09-08 측정. `broad_offers` 30일치 기준.

```
사전의 목적지            89곳
출발 허브                 5곳
가능한 (허브,목적지) 조합  445개
현재 노선 페이지           36개   ← 8%

광역에 20일 이상 잡히는데 페이지가 없는 조합:  85개
  ICN-CJU 제주 31 · ICN-HNL 호놀룰루 30 · ICN-DLC 다롄 30 · GMP-HNL 28
  GMP-JKT 28 · ICN-SYX 싼야 28 · TAE-CJU 28 · GMP-CMB 27 · GMP-SYD 27
  GMP-BKK 27 · GMP-HKT 27 · CJU-SHA 26
```

🔴 **이 85개는 "페이지를 만들 수 있는 곳"이 아니라 "표본을 재볼 후보"다.**
광역(`broad_offers`)에 잡히는 것과 노선 페이지가 성립하는 것은 **다른 조건**이다.
노선 페이지는 `offers`(v3 `fetch_prices`, 노선별 심층)를 쓰고, 거기서 하루 10건이
안 나오면 월별·요일별 차트가 빈다(§7 BB22 · `tests/test_charts.py`).

**이 둘을 혼동한 적이 있다.** BE3에서 나는 BKI(표본 2)를 위험으로 짚고 RMQ(표본 11)를
"가장 강하다"고 했는데, 실제로는 BKI가 들어오고 **RMQ가 96h 신선도 필터에 154h로 잘렸다.**
`fetch_route`의 깊이와 광역 피드 등재 가능성은 같은 축이 아니다. 재개할 때 다시 틀리지 말 것.

### 11.3 재개 전제 — 지금 당장 시작하지 않는 게 나은 이유

서치콘솔·서치어드바이저를 **2026-09-05에 등록했다.** 며칠 뒤부터 *실제로 어떤 질의로
들어오는지* 찍힌다. **85개를 추측으로 고르는 것보다 그 데이터를 보고 고르는 게 낫다.**

재개 조건 세 가지:

1. 서치콘솔에 질의가 최소 2주치 쌓였는가 (2026-09-20 이후)
2. 노선을 늘리면 `fetch_prices` 호출이 36 → 최대 121회로 는다. 제한은 200회/시간이라
   여유는 있지만 **크론 시간이 길어지고 실패 지점이 는다** — 그것도 함께 재야 한다
3. **분리 후 노선 페이지는 프론트 구역이다.** 백엔드가 정하는 건 *어떤 노선을 수집하는가*
   (`config.ROUTES`)이고, *페이지를 만드는가*는 프론트다. 챕터를 열기 전에 경계를 확인할 것

### 11.4 이전과 부딪히는 곁가지

| BB | 이전 후 | 왜 |
|---|---|---|
| **BB6** (`docs/index.html` 278KB) | **프론트로 넘어감** | 인라인 주체가 `build_index()`인데 그게 프론트로 간다. 백엔드는 JSON만 낸다 |
| **BB27** (`build_site.py` 죽은 코드) | **대부분 소멸** | `deal_card`·`sparkline`·`mail_deal_rows`·`mail_rows`가 어차피 안 넘어간다. 이전이 곧 해결이다 — **따로 챕터를 열 이유가 없어졌다** |
| **BB22** (노선 커버리지 20%) | **쪼개짐** | 수집 노선 선정 = 백엔드 / 페이지 생성 = 프론트. 위 11.2~11.3이 백엔드 몫 |
| BB3 (`prices.db` 23MB) | 백엔드 유지 | 수집 산출물 |
| BB24 (MAU 5만) | 백엔드 유지 | 제휴·API 신원 |
| BB25 (`date.today()` 잔여) | 백엔드 유지 | `detect_deals.py:27,67` · `send_alerts.py:77` |
| BB26 (지역 어휘 두 개) | **백엔드 유지, 단 성격이 바뀜** | 표시 문자열이 아니라 **계약 enum**이 된다. `dests` 쪽이 정본이라는 게 더 분명해진다 |

### 11.5 기획 계획에 대한 사실 확인 — 셋 다 맞고, 둘이 더 있다

기획이 확인을 요청한 세 가지, 전부 **맞다**:

1. `send_alerts.main()`이 `compute_deals(conn)`으로 **DB에서 직접** 가져온다.
   `deals.json`을 안 읽는다 → 계약을 바꿔도 알림 메일은 안 깨진다. ✅
2. 테스트 11개(+`__init__.py`) 중 `discover.js|css`·`discover_home`을 읽는 것 **0개**. ✅
3. `collector/` 어디에도 브라우저 `fetch()`가 없다(IMAP `fetch`뿐) → CORS 무관. ✅

**다만 계획의 전제 두 개가 어긋난다.** 조치는 기획 몫이라 사실만 적는다.

- 🔴 **"백엔드는 사이트 주소를 모르게 된다"가 성립하지 않는다.**
  `send_alerts.py`가 백엔드에 남는데, 알림 메일 본문은 **사이트로 링크한다**
  (`SITE_URL = theme.BASE_URL + "/"`). 주소를 아는 모듈이 백엔드에 하나 남는다는 뜻이다.
  → 백엔드에 최소한의 `BASE_URL`이 남거나, 알림 메일이 프론트로 가거나 둘 중 하나다.
  `tests/test_site_url.py`가 잠그고 있는 바로 그 지점이라 조용히 갈라지진 않는다.
- 🔴 **"HTML을 태그 한 글자도 안 만든다"와 "메일이 남는다"가 충돌한다.**
  `send_alerts.deal_line()`은 `<li>`를, `build_mail()`은 메일 본문 HTML을 만든다.
  메일 HTML은 성격이 다르다(웹 페이지가 아니고 프론트가 렌더하지도 않는다) —
  예외로 두면 되지만, **예외라고 적어두지 않으면 나중에 누군가 옮기려 든다.**

### 11.6 중단되는 것

- **BE5(수익·제휴)·BE6(알림 v2)** — 원래 트래픽 대기였고, 이전과 무관하게 계속 대기다
- **한 달 뒤 묶어서 할 것**(2026-10-02 전후): 신선도 임계 재측정 · 대구·제주 노선 재측정 ·
  BKI 캐시 두께 관찰 · **타이중이 피드에 계속 안 뜨는지**(노선 페이지는 있는데
  사이트 안에서 그 도시로 갈 수 없는 상태)

### 11.7 계약 v1에 대한 백엔드 판정 — 표본 임계 (기획 P7 회신)

기획의 P7 「백엔드는 사실을, 프론트는 말을 낸다」에 **동의한다.** 임계 `n < 2`는
통계적 유효성이 아니라 **"하나는 비교가 아니다"**라는 비교의 논리이고, 그건 렌더 쪽 판단이다.
다만 기획 표의 두 곳이 실제 코드와 어긋난다.

**① 「표본 수 `n`을 모든 차트 블록에 넣는다」는 절반이 군더더기다.**
네 계열(`daily_min`·`month_min`·`weekday_min`·`airline_min`)은 전부 **배열**로 나간다.
차트 임계는 글자 그대로 `len(rows) < 2` — **프론트가 `arr.length`로 이미 안다.**
따로 `n`을 넣으면 배열 길이와 `n`이 갈릴 자리를 새로 만드는 것이다(이 프로젝트가 반복해 만난 그 유형).

**프론트가 못 구하는 건 "버킷 안에 몇 건이 들어갔나"다.** 그리고 그 정책이 지금 **셋으로 갈려 있다**:

| 함수 | 표본 정책 | 계약에 보이나 |
|---|---|---|
| `airline_min` | `COUNT(*)`를 **세 번째 값으로 내보냄** | ✅ 보임 |
| `month_min` | `HAVING COUNT(*)>=3` — **얇은 달을 조용히 버림** | 🔴 안 보임 |
| `daily_min`·`weekday_min` | 없음 | — |

`month_min`의 `>=3`이 지금 실제로 일하고 있다(2026-09-08: 36노선 월 버킷 최소 3·최빈 10).
**프론트는 "10월이 없다"가 자료가 없어서인지 3건 미만이라 버려진 건지 구분할 수 없다.**
기획이 말한 *"null로 얼버무리지 않는다"*가 정확히 여기다 — v1이 그 자리를 만들어야 한다.

**② 임계가 지배하는 건 차트만이 아니다 — 실제로 잘못 나간 건 문장이었다.** → **BB28**
타이중에서 나간 `"9월 출발이 가장 저렴합니다"`는 차트가 아니라 `tips` 문장이고,
`build_site.py:207`은 아직 `if best_month:`다. 기획 표의 「차트 블록」을 그대로 계약에 옮기면
**문장 쪽은 다시 무방비가 된다.** 임계의 적용 대상은 **"버킷 비교로 만든 모든 주장"**이다.

**③ `route_summary`는 이미 표본 수를 낸다** — `(최저가, 중앙값, len(prices))`.
이건 그대로 두면 된다. 새로 만들 게 아니라 **나머지 넷을 여기 맞추는** 일이다.

### 11.8 계약 v1 판정 — 낼 수 있다. 단 빠진 게 하나 있고, 창 하나가 틀렸다

`CONTRACT.md` §v1 4) `routes/{code}.json`을 현행 `route_page()`(`build_site.py:191~`)와
한 줄씩 대조했다.

**숫자는 전부 덮는다.** 5개 블록으로 다음이 전부 나온다:

| 화면 | 출처 | 계약 |
|---|---|---|
| 태그라인 `가격 {n}건` | `route_summary[2]` | `summary.n` ✅ |
| 히어로 `최근 30일 최저가` | `route_summary[0]` | `summary.cheapest` ✅ |
| 히어로 `평소 시세(중앙값)` | `route_summary[1]` | `summary.median` ✅ |
| `언제 가면 싼가` 문장 | `min(months)` · `min(weekdays)` | 프론트 파생 ✅ |
| 추이 차트 | `daily_min(…,30)` | `trend` ✅ |
| 월별·요일별 차트 | `month_min` · `weekday_min` | `months` · `weekdays` ✅ |
| 항공사 표(이름·최저가·건수) | `airline_min` | `airlines[]` ✅ |
| 빵부스러기 지역 | `region_of(dest)` | `region` + `COPY.md` §2b ✅ |
| 다른 노선 목록 | `config.ROUTES` | `routes/index.json` ✅ |

**🔴 빠진 것 하나 — 숫자가 아니라서 놓쳤다: 구독 메일 규약.**

페이지 하단 구독 CTA는 `mailto:`다(`build_site.py:96`, 홈에도 `:366`). 계약은 이걸
*"프론트가 파생"*으로 넘겼는데, **이건 표시가 아니라 백엔드와의 전선(wire) 규약이다.**

```
주소   theme.SUBSCRIBE_ADDR = "flightpromokr@gmail.com"
       → subscriptions.py 가 IMAP으로 로그인해 읽는 바로 그 메일함
제목   "구독신청" / "구독취소"      ← subscriptions.py:26-27 이 문자열로 판정
본문   "노선: ICN-FUK (…)"        ← ROUTE_RE = ([A-Z]{3})\s*[-→~]\s*([A-Z]{3})
```

`theme.py`가 사라지면 프론트가 이 넷을 다시 적게 되고, **아무도 대조하지 않는다** —
`SITE_URL`에서 R1b로 막은 것과 **같은 함정이 하나 더 있는 것이다.**

⚠️ **틀리면 조용한 정도가 아니라 반대로 동작한다.** `_extract_route()`가 노선을 못 찾으면
`None`을 반환하고 `load_subscribers()`는 `route or "ALL"`로 받는다(`subscriptions.py:64`) —
프론트가 본문 형식을 바꾸면 **구독 실패가 아니라 전 노선 구독**이 된다. 실패보다 나쁘다.

→ **`meta.json`에 넣자**(백엔드 소유. 우리 IMAP 파서가 정본이므로):
`{"subscribe": {"address": …, "subject_subscribe": "구독신청", "subject_unsubscribe": "구독취소",
"route_token": "{code}"}}`. 공개 연락처라 PII가 아니고, 이미 전 페이지에 노출돼 있다.
R1 상태 점검이 어차피 `meta.json`을 받아보므로 **점검 대상이 공짜로 하나 는다.**

**🔴 창 하나가 틀렸다 — `months`의 「전 기간」은 의도가 아니라 결함이다** → **BB29**

기획이 확인을 요청한 그 지점이 정확히 문제였다. `month_min`에 `depart_date` 창이 없어서
**지금 5개 페이지가 이미 지난 달에 떠나라고 말하고 있다.** 임계로는 못 거른다
(ICN-NRT의 2026-08은 915건으로 가장 튼튼하다). **창으로 잘라야 하고, 창은 백엔드다.**

- `months` 창 → **`depart_date >= 오늘`**로 고쳐야 한다. 계약에 「전 기간」으로 못박지 말 것.
- `weekdays` 창 → **「전 기간」이 맞다.** 요일은 순환해서 지난 데이터도 유효하다.
- `trend` 30일 · `airlines` 30일 · `summary` 30일 → **유지.** 기획 판단대로 창이다.

**나머지 질의 회신**

- `trend`에 `n` 없음 → **동의.** 하루=한 점이라 `n`은 항상 1에 수렴한다. 넣지 말 것.
- `wd` 정수 + "순서를 믿지 말라" → **동의.** `weekday_min`이 월요일부터 재정렬해 내보내는데
  (`build_site.py:66`) 그 재정렬이 표시 결정이었다. 정수로 넘기는 게 맞다.
- `airlines[].name`·`o_name`·`d_name`을 참조 데이터로 → **동의.** `labels.py`가 정본이고
  `deal.ko`와 같은 성격이다.
- M1 변경 5개 → **1·2·3·5 동의.** 4번(`daily_min` 30일 유지)도 동의하되, **`months`는
  창을 빼는 게 아니라 새로 넣어야 한다**는 게 위 BB29다.

### 11.9 M1이 월별 차트를 얼마나 바꾸는가 — 실측 (2026-09-08)

프론트가 「`LIMIT 10` 제거로 15개 노선이 바뀐다」고 재 왔다. **맞다. 다만 계약은 둘을 뺀다.**

```
LIMIT 10 만 제거        →  15 / 36 노선이 달라짐   ← 프론트 실측 (정확)
HAVING >=3 만 제거      →   5 / 36
둘 다 제거 (계약 M1)    →  22 / 36               ← M2가 실제로 볼 diff
```

🔴 **그리고 BB29의 창을 넣으면 방향이 뒤집힌다.** 프론트는 「막대가 **늘어난다**」로 계획을
세웠는데, `depart_date >= 오늘`을 함께 적용하면:

```
M1 + BB29 창  →  22 / 36 노선 변화 (늘어남 10 · 줄어듦 12)
  ICN-NRT 8→6   ICN-KIX 9→7   ICN-TPE 9→7   ICN-SGN 9→7
  ICN-HAN 10→8  ICN-CEB 10→8  ICN-GUM 9→7   GMP-CJU 9→7
```

**절반 이상이 오히려 줄어든다.** 지난 달 버킷이 잘려 나가기 때문이다. M2가 「늘어날 것」을
전제로 diff를 검수하면 **줄어든 15장을 회귀로 오인한다.** 순서상 BB29를 M1에 함께 넣는 게
맞다 — 나중에 넣으면 노선 페이지가 두 번 흔들린다.

**요일 `n>=3`**(M1 2번)은 프론트 실측대로 **`ICN-RMQ` 5→3 하나뿐**이다. 재현 확인했다.

### 11.10 이전 순서 규칙 (기획, 2026-09-08) — 내 곁가지 처리 시점을 정한다

> **이전 중에는 동작을 바꾸지 않는다.** 고칠 것은 **이전 전에** 현행 코드에서 고쳐 크론이
> 한 번 배포하게 하거나, **M3 이후** 별도 챕터로 한다. 이전은 **옮기기만** 한다.

M2 T5의 관문이 「새 화면 37장이 현 배포본과 diff 0」이기 때문이다. 이전이 화면을 바꾸면
**diff가 났을 때 이전이 깨뜨린 건지 우리가 고친 건지 구분할 수 없다.**

**내 곁가지에 대입하면:**

| BB | 언제 | 왜 |
|---|---|---|
| **BB29** (지난 달 추천) | 🔴 **이전 전** | 화면을 바꾼다. **비교 기준이 먼저 옳아야 한다** — 틀린 화면을 기준으로 diff-0을 맞추면 틀린 걸 보존한다. 순서: 수정 → 크론 1회 배포 → 그 화면이 M2 기준 |
| **BB30** (마커 없는 빌드) | 이전 전 가능 | **경고만 추가하면 산출물이 안 바뀐다** → diff-0을 안 건드린다. 거부까지 가면 이전 후로 미룬다 |
| BB25·BB26·BB28 | M3 이후 | 동작이 바뀌거나 남의 구역으로 간다 |
| BB27 | M3 T3에 흡수 | 죽은 코드라 옮기지 않고 지운다 |

**M4 T4 주의**: `tests/`를 통째로 가져가지 말 것. **`test_charts.py`는 프론트로 간다**
(`charts.py`를 따라간다). `test_route_pages.py`·`test_site_url.py`도 사이트 산출물을 보므로
같이 검토할 것.

**(참고) 창 적용 후에도 `LIMIT 10`이 물리는 노선은 5개다** — 실측:
`ICN-DPS 12 · ICN-CDG 12 · ICN-LHR 12 · ICN-SYD 12 · ICN-JFK 11`.
기획 실측과 노선 집합은 같고 개수만 한둘 다르다(기획: CDG·DPS를 11로 셈). 프론트가
임계로 재현하기로 했으므로 M1 지시는 그대로다.

### 11.11 M1 착수 전 확인 — 통계 함수의 **호출자가 둘**이다 (기획 지적, 2026-09-08)

M3 T1까지 크론이 `build_site.py`로 사이트를 만든다. 그동안 `month_min` 등은
**`route_page()`(라이브)와 v1 발행(신규)이 함께 쓴다.** M1 지시를 그대로 적용하면
**라이브 화면이 바뀌고 M2 기준선이 그날 무효가 된다** — 「이전 중 동작 변경 금지」 위반.

**기획 제안(기본값으로 현행 동작 보존)은 필터에는 통한다. 그런데 M1은 필터만 바꾸는 게 아니다.**

🔴 **계약이 요구하는 `n`은 반환 **모양**을 바꾼다 — 기본 인자로는 못 덮는다.**

```python
month_min   → 현재 (m, price)          계약 (m, price, n)
weekday_min → 현재 (요일명, price)      계약 (wd, price, n)
airline_min → 현재 (code, price, n)    이미 3-튜플 ✅
```

`charts.bar_chart`는 `vals = [v for _, v in rows]`로 **2-튜플을 못박고 있다**(`charts.py:44`).
그리고 `route_page:287`이 `bar_chart(weekdays)`로 **가공 없이 그대로 넘긴다** →
`weekday_min`에 `COUNT(*)`를 더하는 순간 `ValueError`로 **빌드가 죽는다.**
조용히 틀리는 게 아니라 크게 죽는 건 다행이지만, 기본 인자로는 안 막힌다.

**그래서 이렇게 한다 (M1 T1 예정):**

1. **셋**을 **항상 `(라벨, 가격, n)` 3-튜플**로 통일한다. `n`은 필터가 아니라 **사실**이라
   조건부로 낼 이유가 없다(계약 §「백엔드는 사실을, 프론트는 말을」).
   ⚠️ **`daily_min`(trend)은 빼야 한다** — 계약이 `trend`에 `n`을 안 둔다(하루=한 점).
   처음에 "넷 다"라고 적은 건 내 착오다.
2. **필터만** 기본 인자로 현행을 보존한다 — `min_samples=3, limit=10` / `airline_min`은 `limit=8`.
3. `route_page`의 소비 지점 세 곳을 3-튜플에 맞춘다(`:281` `:287` — `:220`은 `r[1]`이라 무관).
   **산출물은 한 글자도 안 바뀐다** → diff-0 유지.

`weekday_min`은 표시명(`SQL_WEEKDAY[wd]`)이 아니라 **정수 `wd`**로 내야 계약과 맞는데,
그 변환도 `route_page` 쪽으로 옮겨야 한다(계약 §「배열 순서를 믿지 말고 `wd`를 읽어라」).

⚠️ **M1은 챕터다. 사용자 승인 없이 시작하지 않는다**(`CLAUDE.md` 스코프 잠금).

### 11.12 서버 이식성 — M3 설계 시 참고 (2026-09-08 실측)

기획이 「M6 후 서버를 실제로 붙인다(동기는 포트폴리오)」를 알려오며 셋을 짚었다.
**둘은 이미 됐고 셋째는 생각보다 작다.** 지금 서버용으로 설계할 이유는 없다(YAGNI) —
갈림길에서 고를 때만 쓴다.

| 기획이 짚은 것 | 현 상태 |
|---|---|
| 통계가 파일 경로를 아는가, 커넥션만 받는가 | ✅ **다섯 함수 전부 `conn`만 받는다.** `DOCS` 참조 0 |
| 응답 만들기와 파일 쓰기가 갈려 있는가 | ✅ `*_payload()`가 만들고 `_write()`가 쓴다. **핸들러는 `_write`만 안 부르면 된다** |
| sqlite에 묶여 있는가 | ⚠️ **딱 두 곳** — 아래 |

```
build_site.py:76    strftime('%Y-%m', depart_date)          → Postgres: to_char(…, 'YYYY-MM')
build_site.py:101   CAST(strftime('%w', depart_date) AS INT) → Postgres: EXTRACT(DOW FROM …)
```

(`discover_data.py:286`의 `strftime`은 **파이썬** 쪽이라 무관하다.)

**이식 표면이 SQL 두 줄이다.** 지금 추상화 계층을 세울 이유가 없다는 뜻이기도 하다 —
그날 두 줄을 고치는 게 오늘 어댑터를 만드는 것보다 싸다. **M3에서 통계를 별도 모듈로
뺄 때 이 두 줄이 한 파일에 모이게만 두면 된다.**

`publish_v1`이 화면을 모르는 것과 같은 성질이다 — 계층이 자기 위쪽을 모르면 나중에
그 위에 무엇을 얹든 상관없어진다.

### 11.13 M2 동등성을 백엔드가 독립 검증했다 (2026-09-09)

프론트가 「기준선 `05d0de9` 대비 39/39 바이트 동일」을 냈다. **지우기 전에 직접 확인했다** —
M3은 원본을 삭제하는 단계라 남의 확인을 그대로 믿고 지울 일이 아니다.

```
python site/build.py --api docs/v1 --out <임시폴더>
→ 오늘(09-09) 크론 산출물과 대조:  불일치 0 / 39
```

**프론트보다 강한 조건으로 봤다** — 그쪽은 기준선(09-08) 대비였고, 나는 **오늘 데이터**로
돌려 오늘 크론이 만든 화면과 맞췄다. 즉 「그날만 맞는 것」이 아니라 **다른 날 데이터로도
같은 화면이 나온다.**

🔴 **함정 하나 — 처음엔 39/39 전부 「다름」으로 나왔다.**
윈도우 체크아웃이라 `docs/`는 **CRLF**인데 갓 생성한 파일은 **LF**다. `cmp`가 전부 다르다고
했다. 내용은 동일했다.

```bash
cmp -s <(tr -d '\r' < a) <(tr -d '\r' < b)     # 줄바꿈 정규화 후 비교
```

**M4에서 레포를 옮기며 또 대조하게 된다.** 그때 「전부 다르다」가 나오면 **먼저 줄바꿈을
의심할 것** — 내용 차이로 오인해 되돌리면 멀쩡한 이전을 뒤집는다. git은 커밋 시 정규화하므로
`git status`는 깨끗한데 `cmp`만 다르게 보이는, **눈으로는 설명이 안 되는** 종류다.

---

## 12. ⏸ 여기서 멈춰 있다 — 이전 보류 (2026-09-09, 사용자)

> 사용자: *"해야 할 일 정리해 놓고 좀 미뤄야 할 듯. 먼저 해야 할 일이 있음."*
> **M3는 승인 안 났다. 착수하지 않았다.** M1까지 끝난 상태로 멈춘다.

### 12.1 지금 상태 — 아무것도 안 깨져 있다

```
크론이 부르는 것   collector/build_site.py    ← 옛 경로 그대로 (collect.yml:83)
스위치(M3 T1)      안 했다
산출물             옛 HTML 36장 + 새 v1 39개가 나란히 나간다
사이트             평소와 동일. 사용자가 볼 변화 0
크론               계속 돈다. 보류는 이전 작업만이고 운영이 아니다
```

**지금까지 한 일은 전부 「더하기」였고 지운 게 없다.** M3가 첫 「되돌리기 어려운」 단계인데
그걸 안 밟았으므로 **무기한 방치해도 안전하다.**

### 12.2 🔴 보류 중 지켜야 할 것 — 두 경로를 함께 고친다

`build_site.py`(HTML)와 `publish_v1.py`(JSON)가 **같은 통계 함수를 쓴다.**
보류가 길어질수록 위험한 건 이것 하나다:

> **한쪽만 고치면 갈린다.** 지금은 `ReproducesTheCurrentScreenTest`가 잡지만,
> 새 기능이 한쪽에만 들어가면 **M3의 diff가 커지고** M2에서 증명한 동등성이 흐려진다.

- **보류 중에는 두 경로 중 어느 쪽도 건드리지 않는다.**
- 라이브 결함이 새로 나오면 **BB29 때처럼 양쪽에 같이** 반영하고, 기획에 먼저 알린다.

### 12.3 M3 착수 전 상태 — 각 태스크가 무엇을 건드릴 계획이었나

| 태스크 | 계획 | 상태 |
|---|---|---|
| T1 | `collect.yml:83`을 `publish` → `site/build.py` 순서로 | ⬜ 미착수 |
| T2 | **하루 돌려 정상 확인** (의도적 멈춤) | ⬜ |
| T3 | `build_site.py`의 HTML 생성부 삭제 → `publish.py`로 개명. `docs/data/deals.json` 제거. **BB27 죽은 코드(`deal_card`·`sparkline`·`mail_deal_rows`·`mail_rows`)가 여기서 같이 사라진다** | ⬜ |
| T4 | `theme.py` 삭제. `send_alerts`가 env `SITE_URL`을 받게(P2). `test_site_url.py`는 프론트로, 백엔드엔 형식 검사만 | ⬜ |
| T5 | 🔴 **`SUBSCRIBE_ADDR` → `subscriptions.py`** (BB32). **아직 안 했다** | ⬜ |
| T6 | 크론 「상태 점검」에 `subscribe.address` == `MAIL_ADDRESS` 대조 (R1·R1b와 한 스텝) | ⬜ |

**M3 첫 태스크로 잡아둔 것**(`SPLIT.md`엔 없고 내 판단): `publish_v1` → `build_site` **역의존
해소**. 살아남을 쪽이 사라질 쪽에 얹혀 있다(§11.11). 통계를 별도 모듈로 빼면 저절로 풀리고,
그때 **sqlite 전용 SQL 두 줄이 한 파일에 모이게** 둔다(§11.12).

### 12.4 재개할 때 먼저 볼 것

```bash
# 1. 건강 확인 — 이 셋이 통과하면 방치 기간 동안 아무 일도 없었다는 뜻이다
python -m unittest discover -s tests -t .          # 199건
python -c "import json; d=json.load(open('docs/v1/deals.json',encoding='utf-8')); print(len(d['deals']), sum(any(l.get('ad') for l in x['links']) for x in d['deals']))"
                                                    # 두 숫자가 같아야 한다
find docs/v1 -type f | wc -l                        # 39
```

- **2. `§12.2`를 먼저 읽는다** — 방치 중 누가 한쪽 경로만 고쳤는지 본다.
  `ReproducesTheCurrentScreenTest`가 실패하면 그게 일어난 것이다.
- **3. 기준선 `05d0de9`는 다시 안 잡아도 된다.** M2 증명이 끝났고 SHA로 못박혀 있다.
  (`§11.13` — 다시 대조할 일이 생기면 **줄바꿈부터 의심할 것**.)
- **4. `SPLIT.md` §M3부터 읽고 사용자 승인을 받는다.**

🔴 **재개 때 하지 말 것 — 프론트 픽스처를 「최신으로 갱신」해 주지 않는다.**
`fixtures/v1/`은 기준선 `05d0de9`에서 받아 적은 것이고 **크론은 `docs/v1/`만 갱신한다.**
그래서 재개할 땐 픽스처가 낡아 보인다(09-08 · 딜 125 vs 오늘 · 딜 133) — **그게 정상이다.**
내가 v1을 새로 발행해 건네면 **픽스처와 기준선 산출물의 커밋 쌍이 깨져** 동등성을
다시 증명할 수 없게 된다. 갱신이 필요하면 **입력·출력을 같은 커밋에서 함께** 가져가야 한다.

> 2026-09-09에 내가 「크론이 매일 갱신하니 픽스처는 안 낡는다」고 기획에 말했는데
> **틀렸다.** 크론이 만지는 건 `docs/v1/`뿐이다. 기획이 실측으로 잡았다.

### 12.5 미룰수록 나아지는 것

- **BE8(노선 확대)** — 서치콘솔 데이터가 쌓인다. 85개 조합을 **추측이 아니라 실제 유입 질의로**
  고를 수 있게 된다(§11.2·§11.3). 재개 시점이 「서치콘솔 2주치」에서 「이전 후」로 바뀌었지만
  **측정값과 전제 3개는 그대로 유효하다.**
- **계약 §v1** — 양쪽이 실물로 증명했다(M1 DoD·M2 39/39). 다시 검증할 필요 없다.

---

## 13. ▶ M3 진행 (2026-09-11 재개)

> 사용자: *"이제 레포 옮기기 작업 갑시다"* — 9/9에 올린 M3 목록(T0~T6)으로 진행.
> §12의 재개 건강 확인 전부 통과(테스트 199 · 딜/제휴 138=138 · v1 39 · 보류 중 코드 변경 0).

| 태스크 | 상태 | 커밋 |
|---|---|---|
| T0 통계를 `route_stats`로 분리 | ✅ 산출물 무변경 확인 | `897e056` |
| T1 크론이 새 경로로 화면을 그린다 | ✅ 오늘 데이터로 blob 대조 39/39 | (이 커밋) |
| T2 하루 운영 관찰 | ⏳ **2026-09-12 07:10 크론** | — |
| T3~T6 | ⬜ T2 통과 후. **T3 커밋 직전 기획에 알린다**(T7 동시 반영) | — |

### T1을 「덮어쓰기」가 아니라 「대조 후 교체」로 만든 이유

옛 경로(`build_site.py`)가 아직 HTML도 쓴다. 새 경로를 그냥 `docs/`에 덮어쓰면
**새 경로가 빠뜨린 파일을 옛 파일이 가려준다** — 스위치된 것처럼 보이지만 아무것도
검증되지 않는다. 그러면 T2의 「하루 돌려서 정상 확인」은 「안 죽었다」만 확인하게 된다.

그래서 크론의 `site` 스텝은 새 경로를 **임시 폴더에** 그리고, 옛 경로가 방금 쓴 것과
**운영 러너(리눅스, LF)에서 매일** 대조한 뒤에만 `docs/`로 옮긴다.

```
같다   → 새 경로 산출물로 교체, 배포
다르다 → 새 경로 산출물은 버리고 옛 경로 것을 배포(사이트는 신선하게 유지)
         + 상태 점검이 잡을 실패로 표시 → 메일. 차이 파일 목록이 로그에 남는다
```

**T1의 DoD(「오늘 데이터로 두 경로 대조」)가 T2 동안 매일 자동으로 반복된다.**
T3에서 옛 HTML이 사라지면 대조 상대가 없어지므로 이 스텝은 한 줄로 줄어든다.

스텝 로직은 네 경우로 확인했다(같음 → 0 / 한 장 다름 · 노선 누락 · sitemap 다름 → 1,
실패 메시지가 원인 파일을 짚는다). YAML은 격리 venv의 pyyaml로 파싱 확인.

⚠️ **다름이 떴을 때 — 무엇이 달랐는지로 가른다:**

| 증상 | 판정 |
|---|---|
| 「언제 가면 싼가」 문장 하나가 사라짐 | BB28 임계. 새 노선이 들어온 날. **새 경로가 더 맞다** |
| 🔴 노선 36장 + sitemap이 **한꺼번에**, `dateModified`·`lastmod` 한 줄씩 | **버그.** 아래 |

### 🔴 T1 대조가 못 보던 것 — 새 경로가 기계용 날짜를 KST로 잘랐다 (2026-09-11, 프론트 발견)

```
옛 경로  build_site.py:272·336   timeutil.today_utc()          → 2026-09-06
새 경로  site/route.py:261       meta["generated"][:10]         → 2026-09-07  (KST 시각의 앞 10자)
         site/build.py:76        (sitemap lastmod, 같은 방식)
                                 ↑ 크론이 23:46 UTC(= 08:46 KST 다음날)에 돌면 하루 갈린다
```

**크론은 이미 UTC 자정에 걸쳐 있다.** `gh run list` 최근 16회 중 4회가 자정 전
(09-03~06 23:46~23:58 UTC). **v1이 생긴 뒤 4회가 우연히 전부 자정 후였다** — 그래서
M2 동등성도, 내 T1 blob 대조도 못 봤다.

**내 T1 DoD가 구조적으로 눈멀었던 이유**: 로컬에서 KST 오후에 돌렸다. 그 시각엔
KST 날짜 == UTC 날짜라 **두 경로가 같은 날짜를 낼 수밖에 없었다.** 「오늘 데이터로
대조했다」는 참이지만 **경계 조건을 입력에 넣지 않았다.** 동등성 검사는 **입력이
경계를 밟는지**까지 봐야 한다 — 하루치 데이터로 된 증명은 그날의 시각만 증명한다.

- 처음에 나는 이걸 「새 경로가 더 맞는 잠복 변경」으로 적었다(프론트 설명을 받아 적음).
  **틀렸다** — BB17 위반이다. 워크플로 주석도 같이 고쳤다.
- 수정은 프론트 구역(`site/route.py` `machine_date()` — `generated`를 UTC로 바꿔 날짜를 뗀다).
  **새 경로를 옛 경로와 같게 만들 뿐이라** 「이전 중 동작 불변」에 맞는다: 자정 후 날은
  출력 그대로, 자정 전 날은 실패 → 통과. 통과를 실패로 바꾸는 경우는 없다.
- `site/home.py:84·211`의 `[:16]`은 **화면 표시용** 갱신 시각이라 KST가 맞다. 옛 경로도 같다.

**수정 검증 — 경계를 일부러 밟았다** (2026-09-12 00:07 KST, 크론 전).
프론트가 `machine_date()`로 고쳐 push했다(`b471d0d`). 크론을 기다리지 않고 **시계를
23:46 UTC(= 08:46 KST 다음날)로 바꿔** 옛 경로를 돌리고, 새 경로 수정 전·후를 크론과 같은
방식(git blob)으로 대조했다:

```
                        dateModified   옛 경로 대비
옛 경로 (today_utc)      2026-09-11     —
수정 전 새 경로 ee7b4a6   2026-09-12     2/39  (36장 + sitemap 다름)  ← 오늘 밤 터졌을 것
수정 후 새 경로 99a1984   2026-09-11     39/39
```

**게이트가 버그를 잡고, 수정이 게이트를 통과한다** — 둘 다 확인됐다. 수정 전 버전으로 돌린 게
요점이다. 수정 후 39/39만 봤으면 「이 대조가 애초에 그 버그를 잡을 수 있었나」는 모른다.
(시계를 바꾼 빌드 산출물은 커밋하지 않았다.)

### 크론 복구의 폴더 단위 `--theirs` — 안전하다 (2026-09-11 실측, 기획 질의)

기획이 `collect.yml:161` `git checkout --theirs data/ docs/`가 **충돌 안 난 경로가 섞인 폴더**에서
어떻게 도는지 물었다. 틀리면 `|| true`가 오류를 삼키고 다음 `git add`가 **충돌 표식을 스테이징**한다.
추측하지 않고 임시 저장소에서 rebase 충돌을 만들어 크론 절차를 그대로 돌렸다:

| 경우 | checkout | 결과 |
|---|---|---|
| 내용 충돌(`UU`) + 충돌 없는 수정(`M`) + 안 건드린 `assets/` | exit 0 | 충돌 경로만 크론 것, 나머지 그대로. 표식 0. rebase 완료 |
| **수정/삭제 충돌(`DU`)** — 원격은 지웠고 크론은 고침 | exit 0 | 크론 것을 남긴다. 표식 0. rebase 완료 |

**폴더 단위가 문제 되지 않는다.** `--theirs`는 unmerged 경로에만 작용하고 이미 병합된 경로는
그대로 둔다. `docs/assets/`는 크론이 안 바꾸므로 거기서 충돌이 생길 수 없다.
(사람의 충돌 해결 절차는 다르다 — 기획이 T7에서 생성물만 이름으로 집는 형태로 쓴다.
사람은 `assets/`를 고치다 충돌할 수 있기 때문이다.)

### M4 T0 — `galmal-backend` 기본 보안 설정 ✅ (2026-09-12, 사용자 지시)

기준은 `SPLIT.md` §M4 T0(두 저장소 공통). 적용 후 `gh api`로 **다시 읽어 21항목 대조 — 전부 일치.**

```
이미 켜져 있던 것   1 비밀 스캐닝·푸시 차단 · 2 GITHUB_TOKEN 읽기 전용·PR 승인 불가
바꾼 것            3 Actions: GitHub 제작 액션만 (all → selected)
                   4 외부 기여자 PR 워크플로: 전원 승인 (first_time → all_external)
                   5 main ruleset `main-protection`: 대상 `refs/heads/main` · 삭제·강제 push 금지
                     (우회 주체 0, PR·상태체크 규칙 0)
                   6 Dependabot 경보 + 보안 업데이트 PR
                   7 비공개 취약점 신고
                   8 위키·프로젝트 끔 (이슈 유지)
```

**두 저장소 정합 (기획 결정 반영 후 재조회)**: 처음엔 ruleset 대상이 backend `~DEFAULT_BRANCH` /
frontend `refs/heads/main`으로 갈려 있었다. 기획이 **`refs/heads/main`**으로 정했다 — 우리가 의존하는 건
**이름** `main`이다(`collect.yml:160-161`·`CLAUDE.md`가 박아 쓴다). 기본 브랜치가 실수로 바뀌면
`~DEFAULT_BRANCH`는 보호를 딴 브랜치로 옮기는데 크론은 계속 `main`에 쓴다 — **보호가 빠진 자리에
매일 쓰는 셈**이다. 바꾼 뒤 두 저장소 전 항목을 나란히 읽었다: **다른 건 6번 보안 업데이트 PR 하나뿐이고
그건 기준표대로(백엔드만)다.**

**3번 전에 확인한 것**: 우리 워크플로의 `uses:`는 `actions/checkout`·`actions/setup-python`뿐 — 전부 GitHub 제작.

**5번 — 크론이 이 규칙 아래서 사는지 재현했다.** 기획 요청이 「PR 필수를 걸면 크론 push가 막혀
그날 수집분이 버려진다(BB20)」였다. 강제 push 금지는 거는데, 크론의 **거부 → fetch → rebase → push**가
정말 fast-forward인지 추론만으로 두지 않았다. 로컬 bare 저장소에 `receive.denyNonFastForwards`·
`denyDeletes`(같은 규칙의 git 쪽)를 걸고 `collect.yml:158-168` 절차를 그대로 돌렸다:

```
다른 세션이 먼저 push → 크론 push 거부(1회) → rebase → push 성공(2회)  → 수집분 생존 ✅
대조군: 강제 push → rejected · 브랜치 삭제 → rejected                  → 규칙은 실제로 막는다 ✅
```

⚠️ **M4에서 알아야 할 것 둘 — 이 설정 때문에 생긴다**
- **T4 첫 이력 push는 빈 저장소에 `main`을 새로 만드는 것**이라 규칙에 안 걸린다. 하지만
  **잘못 올려 다시 올려야 하면** 강제 push도 삭제도 막혀 있다 → **사용자가 ruleset을 잠시 끄고**
  다시 올린 뒤 켠다. 우회 주체를 미리 넣지 않은 건 의도다 — 세 세션이 main에 push하므로
  사람도 실수로 강제 push할 수 있고, 그걸 막는 게 이 규칙의 목적이다.
- **T6 `repository_dispatch`는 `gh api`를 `run:` 스텝에서 부른다.** 마켓플레이스 액션
  (`peter-evans/repository-dispatch` 등)은 3번 설정에 막힌다.

### M3 T3 — 백엔드가 HTML을 그만 만든다 ✅ (2026-09-15)

```
삭제   collector/build_site.py            (HTML 생성 전부 + BB27 죽은 코드)
       docs/data/deals.json               (중간 재료였고, 이제 아무도 안 읽는다)
개명   collector/publish_v1.py → publish.py   백엔드의 유일한 출구
크론   publish.py(JSON) → site/build.py(화면)  두 스텝 다 성공해야 docs/ 를 커밋
```

**DoD**: **백엔드가 만드는 산출물에 HTML이 없다.** `publish.py`가 내는 건 JSON 4종뿐이다.

⚠️ `collector/`에 HTML 문자열이 **아직 남아 있다** — DoD를 「태그 0건」으로 적으면 거짓이 된다.
남은 자리와 각각의 예정:

| 파일 | HTML | 어떻게 되나 |
|---|---|---|
| `theme.py` | 57줄 | **T4에서 삭제** (`send_alerts`의 `BASE_URL`만 env로 남긴다) |
| `discover_home.py` | 134줄 | 원래 **프론트 소유**. 프론트가 `site/home.py`로 인수함 → M4 T5에서 사라진다 |
| `charts.py` | 24줄 | 프론트가 `site/charts.py`로 인수함. **`site/route.py`는 자기 폴더 것을 쓴다**(실측) — 여기 남은 건 `tests/test_charts.py`만 쓰는 짝이고 **M4 T4에서 둘이 같이 간다** |
| `send_alerts.py` | 16줄 | **남는다.** 메일 본문은 사이트 산출물이 아니다 — P1 개정(「백엔드는 **사이트에 올라가는 것**을 만들지 않는다」)이 정확히 이 자리를 위한 것이다 |

즉 T3의 DoD는 **「생성 경로에서 HTML이 빠졌다」**이고, 파일 단위 청소는 T4·M4에서 끝난다.

#### 🔴 함정 ① — 순서를 지켰고, 지킨 걸 확인했다

`docs/data/deals.json`은 옛 산출물이 아니라 **v1의 중간 재료**였다(기획 발견).
그냥 지웠으면 `deals_payload()`가 `if not src.exists(): return None`으로 **예외 없이**
v1 딜 발행을 멈춘다. 게다가 **하한선 방어(BB1)의 기준**도 그 파일이었다 —
안 옮겼으면 `prev=None`이 되어 **수집이 무너진 날 빈 산출물이 그대로 나간다.**

옮긴 뒤 **보존 경로를 실제로 돌려** 확인했다(`build_deals_json`이 `None`을 주게 해서):

```
preserved 반환값        True
deals.json 내용 유지    True      다시 쓰이지도 않았다(mtime 불변)
meta.preserved          true
meta.counts.deals       140       ← 어제 것을 그대로 센다. **나가는 것**을 세야 사실이다
노선 발행               36개      딜이 보존돼도 노선은 계속 나간다
```

`_previous_deal_count()`도 `docs/v1/deals.json`을 본다 — 하한선이 꺼지지 않았다.

#### 확인한 것

- 테스트 **199건 통과**. 옛 파일을 지운 상태로도 통과
- 옛 파일 없이 전체 파이프라인: v1 발행(노선 36 + 딜 140) → 화면 36장 생성
- **BB30 두 겹 다 살아남음** — `.env`를 치우니 경고가 뜨고, 그 산출물을 커밋본 검사가 잡았다
- `docs/data/world.geojson`은 **남긴다**(프론트 지도가 쓴다). 지운 건 `deals.json`뿐
- 산출물 검증용 재빌드는 전부 `git checkout -- docs/`로 되돌렸다

#### 딸려온 것

- **BB27 해결** — 죽은 코드가 `build_site.py`와 함께 사라졌다
- 계약 검증기의 봉투 검사를 뺐다. v1의 `schema`·`generated`는 `test_publish.py`의
  `EnvelopeTest`가 `docs/v1/*.json` 전부에 대해 본다 — **두 곳에서 보면 갈린다**
- 크론 커밋 게이트가 **발행·화면 둘 다 성공**을 요구하게 됐다. T3 전에는 옛 경로가
  HTML을 먼저 써서 화면 실패 시에도 배포할 것이 남아 있었다

### M4 T4 준비 — `collect.yml`을 옮길 때 빠뜨리면 조용히 죽는 것 (2026-09-15 실측)

사용자가 `galmal-backend`에 **`production` 환경**을 만들고 시크릿을 **환경에** 넣었다.
레포 시크릿이 아니다 — `test.yml`이 `on: push`라 어느 브랜치에서든 돌고, 레포 시크릿이면
그 워크플로도 전부 읽는다. 브랜치가 셋이고 세션 셋이 각자 push하므로 `main`으로 묶는 이득이 있다.

```yaml
jobs:
  collect:
    runs-on: ubuntu-latest
    environment: production     # 🔴 이 줄이 없으면 시크릿이 주입되지 않는다
```

⚠️ **없으면 에러가 아니라 빈손이다.** `_env()`가 전부 `None`이 되고 수집 스텝은
`continue-on-error`라 **초록불로 끝난다.** BB30 경고와 마지막 상태 점검이 잡긴 하지만
**M4 체크리스트에 넣어 둔다** — 이 줄 하나가 하루치 수집을 날린다.

**크론이 멈출 설정이 없는지 직접 봤다**(승인자·대기시간이 있으면 매일 멈춘다):

```
protection_rules   branch_policy 하나뿐 — Required reviewers 없음 · Wait timer 없음 ✅
배포 브랜치        main (스케줄 실행은 항상 기본 브랜치라 안 걸린다)
환경 시크릿 5종    ANTHROPIC_API_KEY · MAIL_ADDRESS · MAIL_APP_PASSWORD · TP_MARKER · TP_TOKEN
저장소 시크릿 0종 · 저장소 변수 2종   API_URL=https://galmal.kr · SITE_URL=https://galmal.kr
```

🔴 **값의 길이를 재라 — 이름이 맞는 것과 값이 정확한 것은 다르다** (2026-09-15, 기획 발견)

`API_URL`에 복붙하다 `

`이 딸려 들어가 **19자**였다. 그대로면 T6 점검이
`https://galmal.kr⏎/v1/meta.json`을 부른다. **GitHub 화면에서는 정상으로 보인다** —
`gh api …/actions/variables --jq '.value|length'`로 재야 보인다. 기획이 고쳐 둘 다 17자다.

⚠️ **나는 그 값을 출력해 놓고 못 봤다.** 내 출력에 두 변수 사이 **빈 줄**이 찍혔는데
그게 바로 그 `

`이었다. 눈에 보이는 자리에 있었는데 「값을 봤다」로 넘어갔다 —
**본 것과 검사한 것은 다르다.**

→ **T6 설계 요구**: 점검이 실패하면 **부른 URL을 그대로 로그에 찍는다.** 그래야
「주소가 틀렸다」와 「배포가 멈췄다」를 가른다. 변수가 `vars`라 마스킹 없이 남는다 —
secrets로 뒀으면 `***`로 가려져 이 구분이 불가능했을 자리다.

**변수는 환경이 아니라 저장소 레벨이다** — 공개값이고, 환경에 넣으면 환경을 안 쓰는 잡에서
못 읽어 T6 점검이 꼬인다. `API_URL`은 **M5에서 `https://api.galmal.kr`로 이것 하나만** 바꾼다.

#### 🔴 시크릿은 5종이다 — `TP_TRIP_*` 셋은 등록된 적이 없다

기획이 「8종」으로 세어 전달했다가 정정했다(워크플로가 **참조하는 이름**을 세고 **등록된 것**으로
전한 것). 그래서 **Trip.com 링크는 한 번도 수수료 링크였던 적이 없다.** 오늘 산출물 실측:

```
Aviasales   141건  ad=true   marker 붙음     ← 유일한 수익 경로
Trip.com    141건  ad=false  민짜 URL
스카이스캐너·네이버·구글  ad=false  민짜 URL
```

**고지는 정확하다** — 수수료가 안 붙는 링크에 「(광고)」를 달지 않았다.
`test_ad_is_true_exactly_when_the_url_earns`가 지키는 불변식이 그대로 성립한 것이다.
`_trip_configured()`가 넷 중 하나만 없어도 민짜로 폴백하도록 처음부터 짜여 있었고,
**Trip.com 제휴 승인 자체가 아직 안 났으므로 지금 상태가 맞다**(BE5 · BB24).
승인이 나면 그때 셋을 등록한다 — M4에는 필요 없다.

### M3 T5·T6 ✅ (2026-09-16)

**T5 — 구독 메일함 주소를 파서 옆으로** (BB32). `meta.subscribe`의 네 값이 전부
`subscriptions.py`에서 나온다. 변이 검증: 주소를 문자열로 복사하면 테스트가 실패한다.
딸려온 것 — `publish.py`·`tests/test_publish.py`가 `theme`을 **아예 안 쓰게 됐다**(import 제거).
`theme` import가 6개 → 4개로 줄어 T4 표면이 작아졌다.

**T6 — 상태 점검에 API 점검** (R1·R1b·R1c + BB32 대조).

```
url = $API_URL/v1/meta.json          vars.API_URL 없으면 https://galmal.kr
① generated 날짜   오늘/어제 → 통과 · 그보다 오래 → 실패
② subscribe.address == $MAIL_ADDRESS → 아니면 실패
```

🔴 **「오늘 것인가」로 안 짠 이유 — 실측**: 이 스텝은 push 직후에 도는데 **Pages 배포가
~25초 더 걸린다**(`pages-build-deployment` 실측: 00:12:03 시작 → 00:12:28 완료).
그 순간 라이브는 대개 **어제 것**이다. 「오늘」로 짰으면 **매일 헛실패**했고,
매일 실패하는 점검은 무시당한다(BB30 늑대 소년). 어제까지 통과시키면
**배포가 진짜 멈춰도 다음 날 걸린다** — 몇 주를 모르던 BB18보다 훨씬 낫다.

**확인**: 블록을 뽑아 `curl`을 가짜로 바꿔 5경우를 돌렸다 — 오늘 ✅ / 어제 ✅ /
3일 전 ❌ / 못 받음 ❌ / 주소 불일치 ❌. **실물 라이브 + 진짜 `MAIL_ADDRESS`로도 통과.**
실패 메시지는 **부른 URL을 그대로** 찍는다(2026-09-15 `\r\n` 사건의 교훈).

### M3 T4 ✅ — `theme.py`가 사라졌다 (2026-09-16). **M3 완료**

```
삭제   collector/theme.py            (프론트 site/shell.py 가 인수 완료)
       tests/test_route_pages.py     (프론트 산출물을 검사하던 것 — 프론트로)
바뀜   send_alerts  SITE_URL = env    ← 배포 설정(vars.SITE_URL)에서 받는다 (P2)
       collect.yml  알림 발송 스텝에 SITE_URL 주입
```

**백엔드는 이제 사이트 주소를 모른다.** 아는 유일한 모듈이 `send_alerts`인데(메일이
사이트로 링크한다) 그 값도 **하드코딩이 아니라 배포 설정**에서 온다.

#### 빈 값으로 조용히 가지 않게 했다

`SITE_URL`이 비면 본문 링크가 `href="/"`가 되어 **어디로도 가지 않는 메일**이 나간다.
받는 사람은 링크가 죽었다고만 느끼고 우리는 보냈다고 믿는다 — 조용한 실패라
`require_site_url()`이 **발송 전에 멈춘다.** 발송 스텝은 `continue-on-error`라
파이프라인은 안 막힌다. 값 끝의 `
`·`/`도 흡수한다(2026-09-15 사건).

#### 테스트를 좁혔다 — 남의 산출물을 검사하지 않는다

`test_site_url.py`가 `sitemap`·`robots`·노선 HTML·`CNAME`을 보고 있었다. **전부
프론트가 만드는 것**이다. 백엔드 테스트가 남의 산출물을 검사하면 **그쪽이 고칠 때
우리 테스트가 깨진다.** 남긴 건 하나다 — **백엔드 코드 어디에도 도메인이 없다.**
예전엔 `theme.py`를 예외로 뒀는데 **이제 예외가 없다.**

변이 검증: 백엔드 파일에 도메인을 한 줄 넣으면 잡고(`dests.py:307`), `SITE_URL`을
env 대신 하드코딩하면 5건이 깨진다.

#### `collector/`에 남은 HTML — DoD 대로다

```
charts.py     24줄  tests/test_charts.py 와 짝. M4 T4에서 같이 프론트로
send_alerts   16줄  **영구 잔류** — 메일은 사이트 산출물이 아니다 (P1 개정)
parse_mail 2 · publish 1    사소
```

`theme.py` 57줄 · `discover_home.py` 134줄이 사라졌다. **M3 끝.**

#### M4 T4a — `.gitattributes`는 **새 저장소 첫 커밋**에 넣는다 (기획, 2026-09-16)

CRLF를 세 번 만났으니 구조로 끝낸다. 다만 **지금 이 저장소에 넣지 않는다.**
커밋된 blob 기준 문서 7개 4,604줄이 **전부 CRLF**라(기획 실측), 지금 넣으면 한 번에
재정규화된다 — 그런데 **바로 다음 태스크가 파일을 다른 저장소로 옮기는 일**이다.
**이동 diff와 정규화 diff가 섞이면 무엇이 옮겨졌는지 아무도 못 본다.**

```
새 저장소 첫 커밋:   * text=auto eol=lf   +   *.png *.db binary
```

어차피 전량 새 blob으로 들어가므로 **정규화할 게 없다.** 처음부터 LF로 앉는다.

⚠️ **그때까지 이 저장소의 `.md`에 bash `cat >>`·`>`·`sed -i`를 쓰지 않는다.**
파이썬 `io.open(..., newline="")` 경로로만 고친다 — 섞인 파일은 git이 정규화를 못 한다.

#### M4 T3·T6 — PAT 권한이 직관과 다르다 (기획 확인)

```
POST /repos/{owner}/{repo}/dispatches   fine-grained 토큰 요구 권한: Contents: Read and write
```

**Actions 권한이 아니다.** Actions만 주면 `403 Resource not accessible`이 난다.
시크릿 이름은 **`DISPATCH_TOKEN`** 으로 맞춘다(사용자가 그 이름으로 등록한다).
T6 배선에서 이 이름과 권한을 전제로 짠다.

#### 🔴 M4에서 이 점검이 반쪽이 된다 — T6b (기획 발견, 2026-09-16)

지금 `$API_URL/v1/meta.json` **하나**로 충분한 이유는 **한 저장소가 v1과 화면을 같은 커밋으로
배포**하기 때문이다. API가 신선하면 화면도 신선하다.

**M4에서 그 등식이 깨진다.** Pages가 둘로 갈리면 PAT가 죽어 프론트가 몇 주째 안 구워져도
**백엔드 자기 API는 매일 신선**하다 → 점검은 매일 초록불 → **BB18 재발 조건 그대로**다.

```
M4 T5b (프)  site/build.py 가 docs/build.json 에 api_generated(= meta 의 generated)를 쓴다
M4 T6b (백)  상태 점검에 $SITE_URL/build.json 의 api_generated
             == $API_URL/v1/meta.json 의 generated  대조 추가
```

**두 값의 일치**를 본다 — 절대값이 아니다. PAT가 죽으면 사이트가 뒤처져 불일치,
`SITE_URL`이 틀리면 404. `SITE_URL` 변수는 이미 등록돼 있다.

🔴 **「못 받음」은 통과가 아니라 실패다** (기획, 2026-09-16). `$SITE_URL/build.json`이
404인데 「점검을 못 했다」로 넘어가면 **그 점검은 아무것도 안 막는다.** 404는 흔하다 —
`build.json` 위치, Pages 루트, `SITE_URL` 오타가 전부 404로 온다.
M3 T6의 API 절반은 이미 그 형태다(`if [ -z "$meta" ]; then … fail=1`). **사이트 절반도
같은 형태로 쓴다** — `curl … || true` 류를 쓰지 않는 것만 지키면 된다.

🔴 **dispatch 실패도 상태 점검이 잡게 한다.** `403 Resource not accessible`은
`continue-on-error` 아래서 조용하다 — 그러면 R1 시나리오가 **만료가 아니라 권한
오설정으로** 재현된다. 만료는 1년 뒤지만 **오설정은 배선을 켜는 그날**이고 M4가 그날이다.
→ **즉시(dispatch 스텝) + 다음 날(T6b)** 둘 다 둔다.

⚠️ **지금 짜지 않는다.** `build.json`은 프론트가 M4에서 만든다 — 지금 만들면 이전 중
산출물에 새 파일이 생긴다. **M4 T6에서 같이 한다.**

#### 내 커밋이 파일을 통째로 재기록하고 있었다 (기획 지적, 2026-09-16)

`94b1924`가 `BACKEND.md` 한 줄 취지인데 diff가 **1418+/1402-**였다. 원인은
`core.autocrlf=true` + 파이썬 기본 텍스트 모드 — 읽을 때 CRLF를 `\n`으로 바꾸고
쓸 때 되돌리는 과정에서 blob의 줄바꿈이 뒤집혔다. 추적해 보니 **`BACKEND.md` 한 파일이
한 번 뒤집혔다 돌아온 것**이고(`94b1924` CRLF → `5ca789e` LF) 코드 파일은 영향이 없었다.

→ **앞으로 파일을 스크립트로 고칠 땐 `io.open(..., newline="")`로 읽고 쓴다.**
패턴도 파일의 실제 줄바꿈에 맞춰야 매치된다. 이 커밋의 워크플로 diff는 **52+/0-**다.
**리뷰가 불가능한 diff는 없는 리뷰와 같다** — 내용이 맞아도 아무도 확인할 수 없다.

---

## 14. M4 완료 — 저장소가 갈렸고 배선이 실제로 돌았다 (2026-09-16)

```
galmal-backend    666+커밋(해시 보존) · 80파일 · Pages 로 v1 서빙
                  T4 이동 · T4a .gitattributes · T4b schedule 제거
                  T6 dispatch · T6b 사이트 점검 · T6c 부작용 게이트
```

### 수동 실행 1회로 확인한 것 (`skip_side_effects=true`)

**「조용히 실패할 수 있는」 자리를 실물로 밟았다.** 이게 이 실행의 목적이었다.

```
시크릿 주입        수집 성공  → environment: production 이 실제로 먹는다
DISPATCH_TOKEN     dispatch 성공 → Contents 권한이 맞다 (403 아님)
프론트 워크플로     repository_dispatch 로 깨어나 success
부작용 3스텝       skipped (메일함·발송·과금 안 건드림)

신형 백엔드 발행   15:49:59 · 딜 129
프론트 build.json  15:49:59  ← **정확히 일치.** 새 데이터로 다시 구웠다
구형(라이브)       09:11:55 · 딜 127  ← 그대로. 사용자 영향 0
```

**전 구간이 이어졌다.** M5에서 도메인만 옮기면 된다.

### 🔴 M5까지 상태 점검은 빨간불이다 — 정상이다

```
::error::https://galmal.kr/build.json 을 받지 못했다
```

`SITE_URL`이 아직 **구형 사이트**를 가리키는데 `build.json`은 **신형 프론트만** 낸다.
M5에서 galmal.kr이 `galmal-frontend`로 넘어가는 순간 **저절로 초록불**이 된다.

**끄지 않는다.** 「못 받음은 실패」를 여기서 예외로 만들면 그 점검은 다시는 아무것도
안 막는다. **신형에는 `schedule:`이 없어 매일 울지 않으므로**(T4b) 늑대 소년도 아니다 —
수동으로 돌릴 때만 보이고, **M5에서 초록불로 바뀌는 것 자체가 배선이 끝났다는 신호**다.

### M5에서 내 몫 — 순서가 바뀌었다 (기획, 2026-09-16)

DB 인계가 **2b → 5번(거의 마지막)**으로 갔다. **롤백 때문이다** — 3·4(도메인 이전)가
어그러지면 되돌아갈 곳은 구형인데, **크론을 먼저 끄면 되돌아갈 구형이 낡아 있다.**

마지막에 두면 구형이 **도메인이 넘어간 걸 확인할 때까지 계속 수집**하고, 복사가
그때까지 모은 것을 전부 가져간다 → **수집 공백 0.** 3·4 사이에 구형 크론이 한 번 더
돌아도 괜찮다 — **복사가 그 결과까지 가져간다.**

```
5  구형 schedule 끄기 → 구형 최신 data/prices.db·deals_latest.txt 를 신형으로 복사 → 신형 schedule 켜기
   ⚠️ 이 셋의 내부 순서는 그대로다. 켜 놓고 복사하면 그날 신형이 옛 DB로 한 번 돌아 덮는다
   확인: 두 저장소의 `git rev-parse HEAD:data/prices.db` 가 글자 하나까지 같아야 한다
         (전체 이력을 공유하므로 같은 바이트면 반드시 같은 SHA다)
6  상태 점검이 **초록불로 바뀌는지** 확인 — 그게 배선 완료 신호다
```

### 「비용 0」을 두 번 틀리게 셌다

수동 실행을 앞당기자고 하면서 근거를 둘 댔는데 **둘 다 실제 대상이 아니었다**(기획이 잡음):

| 내가 센 것 | 실제 | 왜 틀렸나 |
|---|---|---|
| 「메일 추가분 거의 없음」 | `mail_ingest:67` `select("INBOX")`가 readonly가 아니고 `:73` `fetch(RFC822)`가 PEEK가 아니다 → **\Seen이 붙어 메일을 뺏는다** | 추측이었다 |
| 「`alert_log` 0행」 | 구독자는 `alert_log`가 아니라 **메일함**에서 온다(`load_subscribers`) | **프록시**였다 |

직접 재니 **구독자 0명**이라 결론은 같았다. **결론이 맞은 것과 근거가 맞은 것은 다르다** —
틀린 근거로 맞은 답을 내면 다음번엔 틀린 답이 나온다.
→ 그래서 `skip_side_effects`를 넣었다(T6c). **이번만의 조치가 아니라 수동 실행이
언제든 부작용 없이 돌 수 있어야 한다.**

---

## 15. M5 완료 — 도메인이 넘어갔고, 크론을 맞바꿨다 (2026-09-17)

```
api.galmal.kr   galmal-backend  · cert approved · https 강제
galmal.kr       galmal-frontend · (사용자 3·4번)
구형 크론        disabled_manually (Actions 비활성 — 코드 커밋 없음, R9 동결)
DB 인계          a29eaee · prices.db blob 905febb… 구형 main 과 동일
신형 크론        schedule 복원 39e133e · 첫 수동 실행 전 스텝 success
```

### 🔴 인증서가 안 나온 이유 — DNS보다 먼저 등록하면 요청이 아예 안 걸린다

GitHub 권장 순서(레포 먼저, DNS 나중)대로 했더니 `https_certificate: null`로 30분 넘게 그대로였다.
**느린 게 아니라 안 걸려 있었다** — 등록 시점 DNS 확인이 실패하고 GitHub가 재시도를 안 한다.

```
같은 값 재PUT     5분간 null        ← 값이 안 바뀌면 재확인을 안 건다
제거 → 재등록     25초 만에 approved
```

→ **`null`이면 기다리지 말고 즉시 제거 → 재등록.**

### 🔴 dispatch 경합 — 6번 초록불이 거짓이었다

```
02:45:25Z 발행·커밋 → 02:45:32Z 프론트 시작 → 02:45:40Z 프론트가 API 받음
                    → 02:46:09Z 백엔드 Pages 배포 완료   ← 29초 늦음
```

**신호가 백엔드 배포보다 먼저 가서 프론트가 어제 API로 구웠다**(라이브 딜 129 = 어제).
그런데 상태 점검은 **초록불**이었다 — 사이트 점검의 「어제까지 허용」 관용이 **배포 지연뿐 아니라
영구 지연도 통과시킨다.** 내 배선(T6)과 내 관용(T6b)이 합쳐져 BB18의 얼굴을 만들었다.

- 신호를 수동 재전송해 라이브를 오늘 것(딜 135)으로 복구했다
- **`654dc70`**: 신호 전에 API가 **방금 발행한 `generated`를 실제로 서빙하는지** 확인한다.
  캐시 우회 없이(`max-age=600`이라 우회하면 확인이 거짓이 된다), 최대 12분, 안 오면 신호 안 보내고 실패.
  `client_payload.generated`를 같이 보낸다 — 프론트가 받은 값과 대조하게(CDN 엣지 차이는 받는 쪽만 잡는다)
- ⚠️ **내일 아침 확인할 것은 초록불이 아니다**: `galmal.kr/build.json`의 `api_generated` ==
  `api.galmal.kr`의 `generated` **그리고 둘 다 내일 날짜**여야 수정이 먹은 것이다.

### 🔴 내 실수 — 테스트 게이트가 파이프에 먹혔다

`39e133e`를 **테스트 76건 실패 상태로 push**했다. `unittest … | tail -2 && git add` —
파이프의 종료코드는 **`tail`의 것**이라 실패가 게이트를 통과했다. **초기에 같은 실수를 하고
`grep -q "^OK"`로 고쳤는데 반복했다.** `654dc70`부터 출력에서 `^OK`를 확인하고 커밋한다.

실패 원인은 코드가 아니었다(아래 BB34) — 올라간 게 우연히 무해했을 뿐 **게이트가 안 걸린 건 실수다.**

---
